# yaml-language-server: $schema=/usr/share/ypkg/schema/schema.json
name       : spirv-tools
version    : 1.4.341.0
release    : 36
source     :
    - git|https://github.com/KhronosGroup/SPIRV-Tools.git : fbe4f3ad913c44fe8700545f8ffe35d1382b7093
license    : Apache-2.0
component  : programming.tools
emul32     : true
homepage   : https://www.khronos.org/vulkan/
summary    : The SPIR-V Tools project provides an API and commands for processing SPIR-V modules
description: |
    The SPIR-V Tools project provides an API and commands for processing SPIR-V modules.
patterns   :
    - 32bit-devel : /usr/lib32/cmake
builddeps  :
    - pkgconfig(SPIRV-Headers)
rundeps    :
    - devel : spirv-headers
clang      : true
libsplit   : false
setup      : |
    %cmake_ninja \
        -DCMAKE_INSTALL_LIBDIR=lib%LIBSUFFIX% \
        -DSPIRV-Headers_SOURCE_DIR=%PREFIX% \
        -DBUILD_SHARED_LIBS=ON \
        -DSPIRV_TOOLS_BUILD_STATIC=OFF
build      : |
    %ninja_build
install    : |
    %ninja_install
    %install_license LICENSE
