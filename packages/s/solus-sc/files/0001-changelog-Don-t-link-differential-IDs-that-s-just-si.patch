From c5bb5d701fff9284076f9ff10eb892d92be91c5c Mon Sep 17 00:00:00 2001
From: Ikey Doherty <ikey@solus-project.com>
Date: Sun, 28 May 2017 21:22:26 +0100
Subject: [PATCH 1/2] changelog: Don't link differential IDs, that's just
 silly.

Signed-off-by: Ikey Doherty <ikey@solus-project.com>
---
 solus_sc/changelog.py | 10 ----------
 1 file changed, 10 deletions(-)

diff --git a/solus_sc/changelog.py b/solus_sc/changelog.py
index 2b98325..df24c71 100755
--- a/solus_sc/changelog.py
+++ b/solus_sc/changelog.py
@@ -26,9 +26,6 @@ CVE_URI = "https://cve.mitre.org/cgi-bin/cvename.cgi?name={}"
 BUG_HIT = re.compile(r"T(\d+)")
 BUG_URI = "https://dev.solus-project.com/{}"
 
-DIFF_HIT = re.compile("D(\d+)")
-DIFF_URI = "https://dev.solus-project.com/{}"
-
 # I know, it's evil. From:
 # http://daringfireball.net/2010/07/improved_regex_for_matching_urls
 GENERAL_URI = re.compile(r"""(?i)\b((?:[a-z][\w-]+:(?:/{1,3}|[a-z0-9%])|www\d\
@@ -88,13 +85,6 @@ class ScChangelogEntry(Gtk.EventBox):
                         BUG_URI.format(bug_id), bug_id)
                     ret += href + i[len(bug_id):] + " "
                     continue
-                diff = DIFF_HIT.match(i.upper())
-                if diff is not None:
-                    diff_id = diff.group(0)
-                    href = "<a href=\"{}\">{}</a>".format(
-                        DIFF_URI.format(diff_id), diff_id)
-                    ret += href + i[len(diff_id):] + " "
-                    continue
                 uri = GENERAL_URI.match(i)
                 if uri is not None:
                     uri_href = uri.group(0)
-- 
2.13.0

