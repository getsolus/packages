--- a/src/sudo/mod.rs
+++ b/src/sudo/mod.rs
@@ -52,6 +52,10 @@ pub(crate) fn candidate_sudoers_file() -> PathBuf {
     if !path.exists() {
         path.set_file_name("sudoers");
     };
+    // Solus stateless: fall back to vendor defaults if /etc/sudoers doesn't exist
+    if !path.exists() {
+        path = PathBuf::from("/usr/share/defaults/etc/sudo/sudoers");
+    };
 
     dev_info!("Running with {} file", path.display());
     path
