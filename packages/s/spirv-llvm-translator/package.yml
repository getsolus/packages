# yaml-language-server: $schema=/usr/share/ypkg/schema/schema.json
name       : spirv-llvm-translator
version    : 20.1.8
release    : 23
source     :
    # Branch: llvm_release_200
    - git|https://github.com/KhronosGroup/SPIRV-LLVM-Translator.git : f8153d00cb9e17300212c8ab315d311eb7f63cc8
license    : Apache-2.0 WITH LLVM-exception
component  : programming.tools
homepage   : https://github.com/KhronosGroup/SPIRV-LLVM-Translator/
summary    : Tool/library for translation between SPIR-V and LLVM IR
description: |
    A tool and a library for bi-directional translation between SPIR-V and LLVM IR.
clang      : true
emul32     : true
builddeps  :
    - pkgconfig32(SPIRV-Tools)
    - llvm-32bit-devel
setup      : |
    %cmake_ninja -DBUILD_SHARED_LIBS=ON \
                 -DLLVM_EXTERNAL_SPIRV_HEADERS_SOURCE_DIR=/usr/include/spirv/
build      : |
    %ninja_build
install    : |
    %ninja_install
