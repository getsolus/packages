name       : simplenote
version    : 1.2.1
release    : 10
source     :
    - https://github.com/Automattic/simplenote-electron/releases/download/v1.2.1/Simplenote-linux-1.2.1-amd64.deb : 091ad0d0d83dca0893bef70989dc94ae618ac72fc1c1fe57c01e62ad1ba026d1
license    : GPL-2.0
component  : office.notes
summary    : The simplest way to keep notes.
description: |
    A Simplenote React app packaged in Electron.
extract    : no
builddeps  :
    - pkgconfig(alsa)
    - pkgconfig(fontconfig)
    - pkgconfig(gconf-2.0)
    - pkgconfig(gtk+-2.0)
    - pkgconfig(xscrnsaver)
    - pkgconfig(xtst)
    - cups-devel
install    : |
    ar xf $sources/Simplenote-linux-${version}-amd64.deb
    mkdir root
    tar xf data.tar.xz -C root
    install -D -d -m 00755 $installdir/usr/bin
    cp -Rv root/usr/* $installdir/usr
    cp -Rv root/opt/* $installdir/usr/share
    ln -sv ../share/Simplenote/simplenote $installdir/usr/bin/simplenote
