releases:
  id: 5440
  rss: https://github.com/systemd/systemd-stable/tags.atom
  ignore:
    # Can't update until /usr merge is done
    - 255\..*
security:
  cpe:
  - vendor: freedesktop
    product: systemd
  - vendor: systemd_project
    product: systemd
