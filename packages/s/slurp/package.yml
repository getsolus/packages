# yaml-language-server: $schema=/usr/share/ypkg/schema/schema.json
name       : slurp
version    : 1.6.0
release    : 5
source     :
    - https://github.com/emersion/slurp/archive/refs/tags/v1.6.0.tar.gz : 143000675194352b37392f6109f4932f1ae9a1505de755ed298dcad72142687b
homepage   : https://wayland.emersion.fr/slurp/
license    : MIT
component  : multimedia.graphics
summary    : Select a region in a Wayland compositor and print it to the standard output.
description: |
    Select a region in a Wayland compositor and print it to the standard output.
builddeps  :
    - pkgconfig(cairo)
    - pkgconfig(scdoc)
    - pkgconfig(wayland-client)
    - pkgconfig(wayland-protocols)
    - pkgconfig(xkbcommon)
setup      : |
    %meson_configure
build      : |
    %ninja_build
install    : |
    %ninja_install
    %install_license LICENSE
