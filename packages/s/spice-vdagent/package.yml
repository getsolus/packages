# yaml-language-server: $schema=/usr/share/ypkg/schema/schema.json
name       : spice-vdagent
version    : 0.23.0
release    : 21
source     :
    - https://www.spice-space.org/download/releases/spice-vdagent-0.23.0.tar.bz2 : 63e0f9bd5597c4638acfd6f10d7a2354f599bd9df5b31e443270cacf07e16a40
homepage   : https://www.spice-space.org/
license    : GPL-3.0
component  : desktop.library
summary    : Spice agent for Linux guests
description: |
    Spice agent for Linux guests offering the following features:
    * Client mouse mode (no need to grab mouse by client, no mouse lag)
      this is handled by the daemon by feeding mouse events into the kernel
      via uinput. This will only work if the active X-session is running a
      spice-vdagent process so that its resolution can be determined.
    * Automatic adjustment of the X-session resolution to the client resolution
    * Support of copy and paste (text and images) between the active X-session
      and the client
builddeps  :
    - pkgconfig(alsa)
    - pkgconfig(dbus-1)
    - pkgconfig(glib-2.0)
    - pkgconfig(libdrm)
    - pkgconfig(pciaccess)
    - pkgconfig(spice-protocol)
    - pkgconfig(xfixes)
    - pkgconfig(xinerama)
    - pkgconfig(xrandr)
setup      : |
    %patch -p1 -i ${pkgfiles}/0001-Only-start-vdagent-under-virtualisation.patch

    %reconfigure \
        --with-session-info=systemd \
        --with-init-script=systemd
build      : |
    %make
install    : |
    %make_install
    %install_license COPYING

    # Preenable service
    install -Dm00644 -t ${installdir}/%libdir%/systemd/system-preset/ ${pkgfiles}/20-spice-vdagent.preset
    install -Dm00644 -t ${installdir}/%libdir%/systemd/user-preset/ ${pkgfiles}/20-spice-vdagent-user.preset

    mv $installdir/etc/xdg $installdir/usr/share/xdg
    rmdir $installdir/etc
