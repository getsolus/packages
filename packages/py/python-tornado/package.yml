# yaml-language-server: $schema=/usr/share/ypkg/schema/schema.json
name       : python-tornado
version    : 6.5.8
release    : 22
source     :
    - https://github.com/tornadoweb/tornado/archive/refs/tags/v6.5.8.tar.gz : a6ac2e970dd480aa312dd11090b894a462dde03da1034b1fc21eafa2219520f8
homepage   : https://www.tornadoweb.org/
license    : Apache-2.0
component  : programming.python
summary    : Tornado is a Python web framework and asynchronous networking library, originally developed at FriendFeed
description: |
    Tornado is a Python web framework and asynchronous networking library, originally developed at FriendFeed. By using non-blocking network I/O, Tornado can scale to tens of thousands of open connections, making it ideal for long polling, WebSockets, and other applications that require a long-lived connection to each user.
builddeps  :
    - pkgconfig(python3)
    - python-build
    - python-installer
    - python-packaging
    - python-setuptools
    - python-wheel
build      : |
    %pyproject_build
install    : |
    %pyproject_install
    %install_license LICENSE
#check      : |
#    # https://github.com/tornadoweb/tornado/issues/2947
#    %python3_test -m tornado.test.runtests --verbose
