# yaml-language-server: $schema=/usr/share/ypkg/schema/schema.json
name       : python-pexpect
version    : '4.9'
release    : 20
source     :
    - https://github.com/pexpect/pexpect/archive/refs/tags/4.9.tar.gz : 13756b834dba0adf7603be77d943cc427011983ecd7c58b59b9bf122a1063e40
homepage   : https://github.com/pexpect/pexpect
license    : ISC
component  : programming.python
summary    : Python module for spawning child applications
description: |
    Pexpect is a pure Python module for spawning child applications; controlling them; and responding to expected patterns in their output. Pexpect works like Don Libes' Expect. Pexpect allows your script to spawn a child application and control it as if a human were typing commands.
builddeps  :
    - python-build
    - python-installer
    - python-setuptools
checkdeps  :
    - man-db
    - ptyprocess
    - python-pytest
    - zsh
rundeps    :
    - ptyprocess
build      : |
    %python3_setup
install    : |
    %python3_install
check      : |
    # Workaround to fix failing tests: https://github.com/pexpect/pexpect/issues/669#issuecomment-758109973
    echo "set enable-bracketed-paste off" > .inputrc
    export INPUTRC=$PWD/.inputrc

    python3 ./tools/display-sighandlers.py
    python3 ./tools/display-terminalinfo.py
    # Socket/multiprocessing and REPL tests fail under Python 3.14 + pytest 9 in solbuild.
    %python3_test pytest -v \
        --ignore=tests/test_socket.py \
        --ignore=tests/test_socket_fd.py \
        -k "not (test_which_should_match_other_group_user or test_stdin_closed or test_stdin_stdout_closed or test_no_change_prompt or test_python)"
