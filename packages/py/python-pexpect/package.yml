name       : python-pexpect
version    : 4.7.0
release    : 9
source     :
    - https://github.com/pexpect/pexpect/archive/4.7.0.tar.gz : 38b68a6463b8ce308bed637c1658e67e7f2cdb53d47babc528707296fc324c2e
license    : ISC
component  : programming.python
summary    : Python module for spawning child applications
description: |
    Pexpect is a pure Python module for spawning child applications; controlling them; and responding to expected patterns in their output. Pexpect works like Don Libes' Expect. Pexpect allows your script to spawn a child application and control it as if a human were typing commands.
builddeps  :
    - man-db
    - ptyprocess
    - python-pytest
rundeps    :
    - ptyprocess
setup      : |
    %python_setup
    %python3_setup
install    : |
    %python_install
    %python3_install
check      : |
    pushd $PKG_BUILD_DIR/py2build
        python ./tools/display-sighandlers.py
        python ./tools/display-terminalinfo.py
        py.test -v -k "not test_which_should_match_other_group_user"
    popd

    pushd $PKG_BUILD_DIR/py3build
        python3 ./tools/display-sighandlers.py
        python3 ./tools/display-terminalinfo.py
        py.test3 -v -k "not test_which_should_match_other_group_user"
    popd

