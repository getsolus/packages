# yaml-language-server: $schema=/usr/share/ypkg/schema/schema.json
name       : python-pytest
version    : 9.1.1
release    : 38
source     :
    - https://files.pythonhosted.org/packages/source/p/pytest/pytest-9.1.1.tar.gz : 1088fbde8f2b49d95a549a195707afa7a76a3ce9bcadc26b6d71f0ffda5fe313
homepage   : https://pytest.org/
license    : MIT
component  : programming.python
summary    : Simple powerful testing with Python
description: |
    The pytest framework makes it easy to write small tests, yet scales to support complex functional testing for applications and libraries.
builddeps  :
    - lsof
    - numpy
    - py
    - python-build
    - python-iniconfig
    - python-installer
    - python-packaging
    - python-pluggy
    - python-setuptools-scm
    - python-wheel
checkdeps  :
    - pygments
    - python-argcomplete
    - python-decorator
    - python-hypothesis
    - python-jinja
    - python-mock
    - python-twisted
    - python-xmlschema
rundeps    :
    - py
    - python-attrs
    - python-iniconfig
    - python-packaging
    - python-pluggy
build      : |
    %python3_setup
install    : |
    %python3_install

    # Symlinking pytest3 and py.test3 to prevent check failures
    pushd ${installdir}/usr/bin
    ln -sv pytest pytest3
    ln -sv py.test py.test3
    popd
check      : |
    # Mimic the terminal type that the build server uses so we see test failures that would fail there
    export TERM=dumb

    %python3_test ${installdir}/usr/bin/pytest -v
