# yaml-language-server: $schema=/usr/share/ypkg/schema/schema.json
name       : python-anyio
version    : 4.14.2
release    : 12
source     :
    - https://files.pythonhosted.org/packages/source/a/anyio/anyio-4.14.2.tar.gz : cfa139f3ed1a23ee8f88a145ddb5ac7605b8bbfd8592baacd7ce3d8bb4313c7f
homepage   : https://github.com/agronholm/anyio
license    : MIT
component  : programming.python
summary    : High level compatibility layer for multiple asynchronous event loop implementations
description: |
    AnyIO is an asynchronous networking and concurrency library that works on top of either asyncio or trio. It implements trio-like structured concurrency (SC) on top of asyncio and works in harmony with the native SC of trio itself.
builddeps  :
    - python-build
    - python-installer
    - python-setuptools
    - python-setuptools-scm
    - python-wheel
checkdeps  :
    - python-hypothesis
    - python-psutil
    - python-pytest
    - python-pytest-mock
    - python-pytest-timeout
    - python-trustme
rundeps    :
    - python-idna
build      : |
    %pyproject_build
install    : |
    %pyproject_install
check      : |
    export PYTEST_DISABLE_PLUGIN_AUTOLOAD=1

    _pytest_args=(
        -m "not network"
        --ignore=tests/test_pytest_plugin.py
        -W ignore::pytest.PytestRemovedIn10Warning
        --deselect tests/test_sockets.py::TestTCPStream::test_happy_eyeballs
        --deselect tests/test_sockets.py::TestTCPStream::test_connection_refused
    )

    %pytest -v "${_pytest_args[@]}"
