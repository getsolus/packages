name       : cups-filters
version    : 1.13.4
release    : 12
source     :
    - http://openprinting.org/download/cups-filters/cups-filters-1.13.4.tar.xz : 94c7d6624f16a8c11683c863e6457c5daa70e60f9a739c355349b45c3abc8dbd
license    : GPL-2.0
component  : desktop.core
summary    : Core CUPS Filters
description: |
    The CUPS Filters package contains backends, filters and other software that was once part of the core CUPS distribution but is no longer maintained by Apple Inc.
builddeps  :
    - ghostscript
    - pkgconfig(libpng)
    - pkgconfig(glib-2.0)
    - pkgconfig(avahi-glib)
    - pkgconfig(gio-2.0)
    - pkgconfig(gio-unix-2.0)
    - pkgconfig(lcms2)
    - pkgconfig(freetype2)
    - pkgconfig(fontconfig)
    - pkgconfig(poppler)
    - pkgconfig(zlib)
    - pkgconfig(libqpdf)
    - pkgconfig(dbus-1)
    - pkgconfig(libtiff-4)
    - pkgconfig(ijs)
    - libjpeg-turbo-devel
    - cups-devel
replaces   :
    - foomatic-filters
setup      : |
    %patch -p1 < $pkgfiles/0001-Use-absolute-paths-for-font-directory-and-do-not-mak.patch
    autoreconf -vfi
    %configure --without-rcdir \
               --disable-static \
               --disable-mutool \
               --with-gs-path=/usr/bin/gs \
               --with-pdftops-path=/usr/bin/gs \
               --with-fontdir=/usr/share/fonts/conf.d
build      : |
    %make
install    : |
    %make_install
