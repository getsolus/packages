name       : cppcheck
version    : '2.3'
release    : 12
source     :
    - https://github.com/danmar/cppcheck/archive/2.3.tar.gz : 3c622628ebf46f68909c7a96743b07b6207894a0772fbe802603732152ab1035
license    : GPL-3.0-or-later
component  : programming.tools
summary    : Static source code analysis tool for C and C++ code
description: |
    Cppcheck is a static analysis tool for C/C++ code. It provides unique code analysis to detect bugs and focuses on detecting undefined behaviour and dangerous coding constructs. The goal is to detect only real errors in the code (i.e. have very few false positives).
builddeps  :
    - pkgconfig(z3)
setup      : |
    %cmake_ninja . -DBUILD_TESTS=ON -DHAVE_RULES=ON -DUSE_MATCHCOMPILER=ON -DUSE_Z3=yes
build      : |
    %ninja_build
install    : |
    %ninja_install
