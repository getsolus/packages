name       : cantata
version    : 2.3.3
release    : 12
source     :
    - https://github.com/CDrummond/cantata/releases/download/v2.3.3/cantata-2.3.3.tar.bz2 : 0d7f90c8f448adcdb1e013a6eb95147d9169d5b2023357ff8e34c29ab803ed1d
license    : GPL-3.0-or-later
component  : multimedia.audio
summary    : Cantata is a graphical client for MPD
description: |
    Cantata is a (yet another!) client for the music player daemon (MPD). Originally started as a fork of QtMPC, the code is now *very* different. The interface is very configurable - most views can be shown as either a list or tree structure.
builddeps  :
    - pkgconfig(Qt5Multimedia)
    - pkgconfig(Qt5Svg)
    - pkgconfig(Qt5UiTools)
    - pkgconfig(avahi-client)
    - pkgconfig(libavcodec)
    - pkgconfig(libcddb)
    - pkgconfig(libcdio_paranoia)
    - pkgconfig(libmpg123)
    - pkgconfig(libmtp)
    - pkgconfig(libmusicbrainz5)
    - pkgconfig(taglib)
    - media-player-info
rundeps    :
    - mpd
    - perl-uri
setup      : |
    %cmake_ninja -DENABLE_REMOTE_DEVICES=OFF
build      : |
    %ninja_build
install    : |
    %ninja_install
