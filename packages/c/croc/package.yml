# yaml-language-server: $schema=/usr/share/ypkg/schema/schema.json
name       : croc
version    : 11.0.0
release    : 11
source     :
    - https://github.com/schollz/croc/archive/refs/tags/v11.0.0.tar.gz : 6a147e765f5e47d7022cd43f72fcc42e59333a2be0ff09f98bac1d12215f4af0
homepage   : https://github.com/schollz/croc
license    : MIT
component  : network.clients
summary    : Easily and securely send things from one computer to another
description: |
    croc is a tool that allows any two computers to simply and securely transfer files and folders.
    It provides end-to-end encryption and enables easy cross-platform transfers.
builddeps  :
    - golang
networking : true
clang      : true
environment: |
    export CGO_CPPFLAGS="${CPPFLAGS}"
    export CGO_LDFLAGS="${LDFLAGS}"
    export GO111MODULE=auto
    export GOFLAGS="-buildmode=pie -trimpath -mod=readonly -modcacherw -ldflags=-linkmode=external"
build      : |
    go build -v -o croc
install    : |
    %install_license LICENSE
    %install_bin croc
    %install_file croc.service ${installdir}/%libdir%/systemd/system/croc.service
