name       : curl
version    : 7.63.0
release    : 45
source     :
    - https://curl.haxx.se/download/curl-7.63.0.tar.xz : 9600234c794bfb8a0d3f138e9294d60a20e7a5f10e35ece8cf518e2112d968c4
homepage   : http://curl.haxx.se
license    : MIT
component  : system.base
emul32     : yes
summary    : Get a file from an HTTP, HTTPS or FTP server
description: |
    curl is a client to get files from servers using any of the supported protocols. The command is designed to work without user interaction or any kind of interactivity.  curl offers a busload of useful tricks like proxy support, user authentication, ftp upload, HTTP post, file transfer resume and more.
patterns   :
    - /usr/share/man
    - devel : /usr/share/aclocal/libcurl.m4
builddeps  :
    - pkgconfig32(com_err)
    - pkgconfig32(krb5)
    - pkgconfig32(libssh2)
    - pkgconfig32(libssl)
    - pkgconfig32(zlib)
    - pkgconfig(libnghttp2)
    - groff
    - openssh-server
setup      : |
    %configure --disable-static \
               --enable-threaded-resolver \
               --without-gnutls \
               --enable-libcurl-option \
               --with-ca-bundle=/etc/ssl/certs/ca-certificates.crt \
               --with-libssh2 \
               --with-gssapi
build      : |
    %make
install    : |
    %make_install
check      : |
    %make -k check || :
