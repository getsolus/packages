# yaml-language-server: $schema=/usr/share/ypkg/schema/schema.json
name       : chafa
version    : 1.18.3
release    : 4
source     :
    - https://github.com/hpjansson/chafa/archive/refs/tags/1.18.3.tar.gz : 677f48671d75b82875ade32a2a1ff971c3f44088e6a158beecca51260e0e616e
homepage   : https://hpjansson.org/chafa/
license    :
    - LGPL-3.0-or-later
    - GPL-3.0-or-later
component  :
    - system.utils
    - libs : programming.library
summary    :
    - Image-to-text converter for terminal display
    - libs : Shared libraries for Chafa
description: |
    Chafa is a command-line utility that converts image data, including animated GIFs,
    into graphics formats or ANSI/Unicode character art suitable for display in a
    terminal. It supports a wide range of symbols and palettes, transparency,
    animations, and many image formats (JPEG, PNG, WebP, SVG, TIFF, AVIF, HEIF, JPEG XL).
builddeps  :
    - pkgconfig(freetype2)
    - pkgconfig(glib-2.0)
    - pkgconfig(libavif)
    - pkgconfig(libheif)
    - pkgconfig(libjpeg)
    - pkgconfig(libjxl)
    - pkgconfig(libjxl_threads)
    - pkgconfig(librsvg-2.0)
    - pkgconfig(libtiff-4)
    - pkgconfig(libwebp)
    - pkgconfig(libwebpdemux)
    - docbook-xml
rundeps    :
    - devel :
        - chafa-libs
setup      : |
    %autogen \
        --disable-static \
        --enable-man
build      : |
    %make
install    : |
    %make_install
    %install_license COPYING*

    # Install completions
    %install_file tools/completions/bash-completion.sh ${installdir}/usr/share/bash-completion/completions/chafa
    %install_file tools/completions/zsh-completion.zsh ${installdir}/usr/share/zsh/site-functions/_chafa
    %install_file tools/completions/fish-completion.fish ${installdir}/usr/share/fish/vendor_completions.d/chafa.fish
patterns   :
    - libs :
        - /usr/lib64/lib*.so.*
    - devel :
        - /usr/lib64/chafa/include/chafaconfig.h
