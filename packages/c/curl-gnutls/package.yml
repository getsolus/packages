name       : curl-gnutls
version    : 7.51.0
release    : 3
source     :
    - https://curl.haxx.se/download/curl-7.51.0.tar.gz : 65b5216a6fbfa72f547eb7706ca5902d7400db9868269017a8888aa91d87977c
homepage   : http://curl.haxx.se
license    : GPL-3.0+
component  : binary.compat
emul32     : yes
summary    : curl-gnutls (GnuTLS version of curl)
description: |
    This package provides binary compatibility only and enables the running of content expecting a GNUTLS version of curl. It is forbidden to build against it.
builddeps  :
    - pkgconfig32(gnutls)
    - pkgconfig32(zlib)
    - glibc-32bit-devel
    - libgcc-32bit
    - libtool-32bit
setup      : |
    %patch -N -p1 < $pkgfiles/0001-Use-gnutls.patch
    %reconfigure
    %configure --disable-static \
        --disable-libcurl-option \
        --enable-threaded-resolver \
        --with-gnutls \
        --without-openssl \
        --without-ssl \
        --with-ca-bundle=/etc/ssl/certs/ca-certificates.crt
build      : |
    %make
install    : |
    %make_install
    # Only ship libraries
    for i in /usr/bin /usr/share %libdir%/pkgconfig /usr/include; do
        rm -rfv "$installdir/$i"
    done
    # Forbid linking too
    rm $installdir/%libdir%/*.so
    # Skip any potential .la files
    rm -f $installdir/%libdir%/*.la || :
