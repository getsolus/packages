name       : opam
version    : 2.0.8
release    : 1
source     :
    - https://github.com/ocaml/opam/releases/download/2.0.8/opam-full-2.0.8.tar.gz : 7b9d29233d9633ef50ba766df2e39112b15cd05c1c6fedf80bcb548debcdd9bd
homepage   : https://opam.ocaml.org/
license    : LGPL-2.1-only WITH OCaml-LGPL-linking-exception
component  : programming.tools
summary    : Source-based package manager for OCaml
description: |
    opam is a source-based package manager for OCaml. It supports multiple simultaneous compiler installations, flexible package constraints, and a Git-friendly development workflow.
builddeps  :
    - ocaml
rundeps    :
    - bubblewrap
    - ocaml
setup      : |
    %configure
build      : |
    %make lib-ext -j1 OCAMLC="ocamlc -unsafe-string" OCAMLOPT="ocamlopt -unsafe-string"
    %make -j1 OCAMLC="ocamlc -unsafe-string" OCAMLOPT="ocamlopt -unsafe-string"
install    : |
    %make_install
