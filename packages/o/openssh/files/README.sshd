Solus supports configuration snippets for openssh.

Instead of creating /etc/ssh/sshd_config you should create config files in /etc/ssh/sshd_config.d ending in .conf

If you create /etc/ssh/sshd_config it will DISABLE all Solus default configurations. That is not recommended unless you know what you're doing.
