Solus supports configuration snippets for openssh.

Instead of creating /etc/ssh/ssh_config you should create config files in /etc/ssh/ssh_config.d ending in .conf

If you create /etc/ssh/ssh_config it will DISABLE all Solus default configurations. That is not recommended unless you know what you're doing.
