name       : just
version    : 0.8.3
release    : 12
source     :
    - https://github.com/casey/just/archive/v0.8.3.tar.gz : 31c837cb028a9e32e98c3d7071d80dbbaba7e6f35b3a33496aa39d5d8370d9ba
license    : CC0-1.0
component  : programming.tools
homepage   : https://github.com/casey/just
summary    : Just a command runner
description: |
    Just is a handy way to save and run project-specific commands. As it's a command runner, not a build system, it avoids many of Make's complexity and idiosyncrasies.
networking : true
builddeps  :
    - cargo
build      : |
    cargo build --release
install    : |
    install -Dm00755 target/release/just $installdir/usr/bin/just
