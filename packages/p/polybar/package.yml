name       : polybar
version    : 3.5.6
release    : 34
source     :
    - git|https://github.com/jaagr/polybar.git : 3.5.5
license    : MIT
component  : system.utils
summary    : A fast and easy-to-use status bar
description: |
    A fast and easy-to-use status bar
builddeps  :
    - pkgconfig(alsa)
    - pkgconfig(cairo-fc)
    - pkgconfig(jsoncpp)
    - pkgconfig(libcurl)
    - pkgconfig(libmpdclient)
    - pkgconfig(libpulse)
    - pkgconfig(xcb-cursor)
    - pkgconfig(xcb-image)
    - pkgconfig(xcb-icccm)
    - pkgconfig(xcb-proto)
    - pkgconfig(xcb-xrm)
    - pkgconfig(xcomposite)
    - pkgconfig(xft)
    - i3-devel
    - libboost-devel
    - wireless-tools-devel
rundeps    :
    - python3
setup      : |
    %cmake .
build      : |
    %make
    %make userconfig
install    : |
    %make_install
