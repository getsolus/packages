diff '--color=auto' -pruN a/meson.build b/meson.build
--- a/meson.build	2026-05-14 01:14:47.000000000 -0400
+++ b/meson.build	2026-06-21 12:36:18.731217462 -0400
@@ -13,7 +13,7 @@ plymouth_soversion = '5.0.0'
 plymouth_theme_path = get_option('prefix') / get_option('datadir') / 'plymouth' / 'themes/'
 plymouth_plugin_path = get_option('prefix') / get_option('libdir') / 'plymouth/'
 plymouth_policy_dir = get_option('prefix') / get_option('datadir') / 'plymouth/'
-plymouth_conf_dir = get_option('prefix') / get_option('sysconfdir') / 'plymouth/'
+plymouth_conf_dir = get_option('prefix') / get_option('datadir') / 'defaults' / 'plymouth/'
 plymouth_time_dir = get_option('prefix') / get_option('localstatedir') / 'lib' / 'plymouth'
 
 plymouth_runtime_dir = get_option('runstatedir') / 'plymouth'
diff '--color=auto' -pruN a/src/main.c b/src/main.c
--- a/src/main.c	2026-05-14 01:14:47.000000000 -0400
+++ b/src/main.c	2026-06-21 12:38:24.297324360 -0400
@@ -449,9 +449,11 @@ find_system_default_splash (state_t *sta
         if (state->system_default_splash_path != NULL)
                 return;
 
-        if (!load_settings (state, PLYMOUTH_CONF_DIR "plymouthd.conf", &state->system_default_splash_path)) {
-                ply_trace ("failed to load " PLYMOUTH_CONF_DIR "plymouthd.conf");
-                return;
+        if (!load_settings (state, "/etc/plymouth/plymouthd.conf", &state->system_default_splash_path)) {
+                if (!load_settings (state, PLYMOUTH_CONF_DIR "plymouthd.conf", &state->system_default_splash_path)) {
+                        ply_trace ("failed to load " PLYMOUTH_CONF_DIR "plymouthd.conf");
+                        return;
+                }
         }
 
         if (state->system_default_splash_path != NULL)
