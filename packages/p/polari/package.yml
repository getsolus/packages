name       : polari
version    : '42.0'
release    : 29
source     :
    - https://download.gnome.org/sources/polari/42/polari-42.0.tar.xz : 58f15bbf5b42c0b0123efded775711d3b0f244997573094038ca556461e0493c
license    : GPL-2.0-or-later
component  : network.irc
summary    : Polari is an IRC client which enables you to chat with people around world through large chatrooms or via private messaging.
description: |
    Polari is an IRC client which enables you to chat with people around world through large chatrooms or via private messaging.
builddeps  :
    - pkgconfig(gjs-1.0)
    - pkgconfig(gobject-introspection-1.0)
    - pkgconfig(gspell-1)
    - pkgconfig(gtk4)
    - pkgconfig(libsecret-1)
    - pkgconfig(telepathy-logger-0.2)
    - desktop-file-utils
    - itstool
rundeps    :
    - gjs
    - libadwaita
    - libgtk-4
    - telepathy-idle
    - telepathy-logger
    - telepathy-mission-control
setup      : |
    %meson_configure
build      : |
    %ninja_build
install    : |
    %ninja_install
