From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Reilly Brogan <reilly@reillybrogan.com>
Date: Thu, 20 Aug 2026 15:27:48 -0500
Subject: [PATCH] ftbfs protobuf 36.0

---
 vendor/adb/adb.cpp                            | 16 +++----
 vendor/adb/adb_listeners.cpp                  |  2 +-
 vendor/adb/adb_trace.cpp                      |  4 +-
 vendor/adb/adb_trace.h                        |  2 +-
 vendor/adb/adb_utils.cpp                      |  4 +-
 vendor/adb/client/adb_client.cpp              | 10 ++--
 vendor/adb/client/adb_wifi.cpp                |  4 +-
 vendor/adb/client/auth.cpp                    | 32 ++++++-------
 vendor/adb/client/commandline.cpp             |  6 +--
 vendor/adb/client/file_sync_client.cpp        | 12 ++---
 vendor/adb/client/main.cpp                    | 10 ++--
 vendor/adb/client/mdns_tracker.cpp            |  2 +-
 vendor/adb/client/openscreen/mdns_service.cpp |  4 +-
 .../client/openscreen/platform/logging.cpp    |  6 +--
 .../client/openscreen/platform/udp_socket.cpp | 36 +++++++--------
 vendor/adb/client/pairing/pairing_client.cpp  |  8 ++--
 vendor/adb/client/transport_emulator.cpp      |  2 +-
 vendor/adb/client/transport_usb.cpp           |  2 +-
 vendor/adb/client/usb_libusb.cpp              | 10 ++--
 vendor/adb/client/usb_libusb_device.cpp       | 10 ++--
 vendor/adb/client/usb_libusb_hotplug.cpp      | 10 ++--
 vendor/adb/client/usb_linux.cpp               | 14 +++---
 vendor/adb/client/usb_osx.cpp                 | 46 +++++++++----------
 vendor/adb/client/usb_windows.cpp             |  6 +--
 vendor/adb/compression_utils.h                | 32 ++++++-------
 vendor/adb/crypto/key.cpp                     |  4 +-
 vendor/adb/crypto/rsa_2048_key.cpp            |  6 +--
 vendor/adb/crypto/x509_generator.cpp          | 14 +++---
 vendor/adb/daemon/abb.cpp                     |  2 +-
 vendor/adb/daemon/abb_service.cpp             |  4 +-
 vendor/adb/daemon/adb_wifi.cpp                | 40 ++++++++--------
 vendor/adb/daemon/auth.cpp                    | 32 ++++++-------
 vendor/adb/daemon/file_sync_service.cpp       | 10 ++--
 vendor/adb/daemon/jdwp_service.cpp            | 18 ++++----
 vendor/adb/daemon/logging.h                   |  2 +-
 vendor/adb/daemon/main.cpp                    |  8 ++--
 vendor/adb/daemon/mdns.cpp                    | 20 ++++----
 vendor/adb/daemon/property_monitor_test.cpp   |  2 +-
 vendor/adb/daemon/restart_service.cpp         |  8 ++--
 vendor/adb/daemon/services.cpp                | 14 +++---
 vendor/adb/daemon/shell_service.cpp           | 24 +++++-----
 vendor/adb/daemon/usb.cpp                     | 34 +++++++-------
 vendor/adb/daemon/usb_ffs.cpp                 |  2 +-
 vendor/adb/daemon/watchdog.cpp                | 10 ++--
 vendor/adb/fdevent/fdevent.cpp                |  2 +-
 vendor/adb/fdevent/fdevent_epoll.cpp          |  2 +-
 .../adbconnection/adbconnection_client.cpp    | 32 ++++++-------
 .../adbconnection/adbconnection_server.cpp    | 10 ++--
 vendor/adb/pairing_auth/aes_128_gcm.cpp       |  4 +-
 vendor/adb/pairing_auth/pairing_auth.cpp      | 12 ++---
 .../pairing_connection/pairing_connection.cpp | 38 +++++++--------
 .../adb/pairing_connection/pairing_server.cpp | 16 +++----
 vendor/adb/services.cpp                       |  2 +-
 vendor/adb/sockets.cpp                        | 16 +++----
 vendor/adb/sysdeps/errno.cpp                  |  4 +-
 vendor/adb/sysdeps/posix/network.cpp          |  4 +-
 vendor/adb/sysdeps_win32.cpp                  | 24 +++++-----
 vendor/adb/tls/adb_ca_list.cpp                |  2 +-
 vendor/adb/tls/tls_connection.cpp             | 32 ++++++-------
 vendor/adb/transport.cpp                      | 22 ++++-----
 vendor/adb/transport_benchmark.cpp            | 12 ++---
 vendor/adb/transport_fd.cpp                   |  8 ++--
 vendor/libbase/include/android-base/logging.h |  1 +
 63 files changed, 394 insertions(+), 393 deletions(-)

diff --git a/vendor/adb/adb.cpp b/vendor/adb/adb.cpp
index f134288..c173f6c 100644
--- a/vendor/adb/adb.cpp
+++ b/vendor/adb/adb.cpp
@@ -175,7 +175,7 @@ std::string to_string(ConnectionState state) {
 apacket* get_apacket() {
     apacket* p = new apacket();
     if (p == nullptr) {
-        LOG(FATAL) << "failed to allocate an apacket";
+       LOG_SAFE(FATAL) << "failed to allocate an apacket";
     }
 
     memset(&p->msg, 0, sizeof(p->msg));
@@ -338,7 +338,7 @@ void send_connect(atransport* t) {
     // Connect and auth packets are limited to MAX_PAYLOAD_V1 because we don't
     // yet know how much data the other size is willing to accept.
     if (connection_str.length() > MAX_PAYLOAD_V1) {
-        LOG(FATAL) << "Connection banner is too long (length = "
+       LOG_SAFE(FATAL) << "Connection banner is too long (length = "
                    << connection_str.length() << ")";
     }
 
@@ -423,7 +423,7 @@ static void handle_new_connection(atransport* t, apacket* p) {
         // first packet the client receives to indicate the new protocol.
         send_tls_request(t);
     } else if (!auth_required) {
-        LOG(INFO) << "authentication not required";
+       LOG_SAFE(INFO) << "authentication not required";
         handle_online(t);
         send_connect(t);
     } else {
@@ -504,7 +504,7 @@ void handle_packet(apacket* p, atransport* t) {
 
         uint32_t send_bytes = static_cast<uint32_t>(p->msg.arg1);
         if (t->SupportsDelayedAck() != static_cast<bool>(send_bytes)) {
-            LOG(ERROR) << "unexpected value of A_OPEN arg1: " << send_bytes
+           LOG_SAFE(ERROR) << "unexpected value of A_OPEN arg1: " << send_bytes
                        << " (delayed acks = " << t->SupportsDelayedAck() << ")";
             send_close(0, p->msg.arg0, t);
             break;
@@ -522,7 +522,7 @@ void handle_packet(apacket* p, atransport* t) {
         // such requests - namely, those from (a potentially compromised)
         // adbd (reverse:forward: source) port transport.
         if (!t->IsReverseConfigured(address.data())) {
-            LOG(FATAL) << __func__ << " disallowed connect to " << address << " from "
+           LOG_SAFE(FATAL) << __func__ << " disallowed connect to " << address << " from "
                        << t->serial_name();
         }
 #endif
@@ -564,7 +564,7 @@ void handle_packet(apacket* p, atransport* t) {
                     // of waiting for the writer to hit its limit.
                     acked_bytes = value;
                 } else if (p->payload.size() != 0) {
-                    LOG(ERROR) << "invalid A_OKAY payload size: " << p->payload.size();
+                   LOG_SAFE(ERROR) << "invalid A_OKAY payload size: " << p->payload.size();
                     return;
                 }
 
@@ -1265,7 +1265,7 @@ HostRequestResult handle_host_request(std::string_view service, TransportType ty
                                       asocket* s) {
     if (service == "kill") {
         if (g_reject_kill_server) {
-            LOG(WARNING) << "adb server ignoring kill-server";
+           LOG_SAFE(WARNING) << "adb server ignoring kill-server";
             SendFail(reply_fd, "kill-server rejected by remote server");
         } else {
             fprintf(stderr, "adb server killed by remote request\n");
@@ -1522,7 +1522,7 @@ HostRequestResult handle_host_request(std::string_view service, TransportType ty
     if (android::base::ConsumePrefix(&service, "emulator:")) {
         unsigned int port;
         if (!ParseUint(&port, service)) {
-          LOG(ERROR) << "received invalid port for emulator: " << service;
+         LOG_SAFE(ERROR) << "received invalid port for emulator: " << service;
         } else {
             // We are running on the fdevent thread. Connect is a long operation which
             // can potentially block 10s until connection is established. We must run
diff --git a/vendor/adb/adb_listeners.cpp b/vendor/adb/adb_listeners.cpp
index 32aa535..52d92c1 100644
--- a/vendor/adb/adb_listeners.cpp
+++ b/vendor/adb/adb_listeners.cpp
@@ -236,7 +236,7 @@ InstallStatus install_listener(const std::string& local_name, const char* connec
 #if ADB_HOST
         listener->fde = fdevent_create(listener->fd, ss_listener_event_func, listener.get());
 #else
-        LOG(FATAL) << "attempted to connect to *smartsocket* in daemon";
+       LOG_SAFE(FATAL) << "attempted to connect to *smartsocket* in daemon";
 #endif
     } else {
         listener->fde = fdevent_create(listener->fd, listener_event_func, listener.get());
diff --git a/vendor/adb/adb_trace.cpp b/vendor/adb/adb_trace.cpp
index 0220d7d..69c7a17 100644
--- a/vendor/adb/adb_trace.cpp
+++ b/vendor/adb/adb_trace.cpp
@@ -140,7 +140,7 @@ static void setup_trace_mask() {
     size_t num_flags = trace_flags.size() - 2;
     size_t num_traces = AdbTrace::NUM_TRACES;
     if (num_flags != num_traces) {
-        LOG(FATAL) << "Mismatched #AdbTrace=" << num_traces
+       LOG_SAFE(FATAL) << "Mismatched #AdbTrace=" << num_traces
                    << " and trace_flags.size=" << num_flags;
     }
 
@@ -148,7 +148,7 @@ static void setup_trace_mask() {
     for (const auto& elem : elements) {
         const auto& flag = trace_flags.find(elem);
         if (flag == trace_flags.end()) {
-            LOG(ERROR) << "Unknown trace flag: " << elem;
+           LOG_SAFE(ERROR) << "Unknown trace flag: " << elem;
             continue;
         }
 
diff --git a/vendor/adb/adb_trace.h b/vendor/adb/adb_trace.h
index f2f3896..d524f2a 100644
--- a/vendor/adb/adb_trace.h
+++ b/vendor/adb/adb_trace.h
@@ -53,7 +53,7 @@ enum AdbTrace {
     if (LIKELY(!VLOG_IS_ON(TAG))) \
         ;                         \
     else                          \
-        LOG(DEBUG)
+        LOG_SAFE(DEBUG)
 
 // You must define TRACE_TAG before using this macro.
 #define D(...) \
diff --git a/vendor/adb/adb_utils.cpp b/vendor/adb/adb_utils.cpp
index 6102f4e..6b2fa9b 100644
--- a/vendor/adb/adb_utils.cpp
+++ b/vendor/adb/adb_utils.cpp
@@ -302,7 +302,7 @@ std::string adb_get_homedir_path() {
         return result->pw_dir;
     }
 
-    LOG(FATAL) << "failed to get user home directory";
+   LOG_SAFE(FATAL) << "failed to get user home directory";
     return {};
 #endif
 }
@@ -332,7 +332,7 @@ std::string GetLogFilePath() {
     DWORD nchars = GetTempPathW(arraysize(temp_path), temp_path);
     if (nchars >= arraysize(temp_path) || nchars == 0) {
         // If string truncation or some other error.
-        LOG(FATAL) << "cannot retrieve temporary file path: "
+       LOG_SAFE(FATAL) << "cannot retrieve temporary file path: "
                    << android::base::SystemErrorCodeToString(GetLastError());
     }
 
diff --git a/vendor/adb/client/adb_client.cpp b/vendor/adb/client/adb_client.cpp
index 6b20151..d09c2d1 100644
--- a/vendor/adb/client/adb_client.cpp
+++ b/vendor/adb/client/adb_client.cpp
@@ -69,7 +69,7 @@ void adb_get_transport(TransportType* type, const char** serial, TransportId* tr
 
 void adb_set_socket_spec(const char* socket_spec) {
     if (__adb_server_socket_spec) {
-        LOG(FATAL) << "attempted to reinitialize adb_server_socket_spec " << socket_spec << " (was " << __adb_server_socket_spec << ")";
+       LOG_SAFE(FATAL) << "attempted to reinitialize adb_server_socket_spec " << socket_spec << " (was " << __adb_server_socket_spec << ")";
     }
     __adb_server_socket_spec = socket_spec;
 }
@@ -116,7 +116,7 @@ static std::optional<TransportId> switch_socket_transport(int fd, std::string* e
         return std::nullopt;
     }
 
-    LOG(DEBUG) << "Switch transport in progress: " << service;
+   LOG_SAFE(DEBUG) << "Switch transport in progress: " << service;
 
     if (!adb_status(fd, error)) {
         D("Switch transport failed: %s", error->c_str());
@@ -157,7 +157,7 @@ bool adb_status(borrowed_fd fd, std::string* error) {
 
 static int _adb_connect(std::string_view service, TransportId* transport, std::string* error,
                         bool force_switch = false) {
-    LOG(DEBUG) << "_adb_connect: " << service;
+   LOG_SAFE(DEBUG) << "_adb_connect: " << service;
     if (service.empty() || service.size() > MAX_PAYLOAD) {
         *error = android::base::StringPrintf("bad service name length (%zd)", service.size());
         return -1;
@@ -321,7 +321,7 @@ static bool __adb_check_server_version(std::string* error) {
                         android::base::ReadFileToString(*server_path_path, &server_path)) {
                         if (execve(server_path.c_str(), const_cast<char**>(__adb_argv),
                                    const_cast<char**>(__adb_envp)) == -1) {
-                            LOG(ERROR) << "failed to exec newer version at " << server_path;
+                           LOG_SAFE(ERROR) << "failed to exec newer version at " << server_path;
                         }
 
                         // Fall-through to restarting the server.
@@ -355,7 +355,7 @@ bool adb_check_server_version(std::string* error) {
 
 int adb_connect(TransportId* transport, std::string_view service, std::string* error,
                 bool force_switch_device) {
-    LOG(DEBUG) << "adb_connect: service: " << service;
+   LOG_SAFE(DEBUG) << "adb_connect: service: " << service;
 
     // Query the adb server's version.
     if (!adb_check_server_version(error)) {
diff --git a/vendor/adb/client/adb_wifi.cpp b/vendor/adb/client/adb_wifi.cpp
index 33762f3..b3f62ed 100644
--- a/vendor/adb/client/adb_wifi.cpp
+++ b/vendor/adb/client/adb_wifi.cpp
@@ -138,7 +138,7 @@ bool KnownWifiHostsFile::AddKnownHost(const std::string& host) {
     }
 
     if (!known_hosts.SerializeToFileDescriptor(temp_file->fd)) {
-        LOG(ERROR) << "Unable to write out adb_knowns_hosts";
+       LOG_SAFE(ERROR) << "Unable to write out adb_knowns_hosts";
         return false;
     }
     temp_file->DoNotRemove();
@@ -147,7 +147,7 @@ bool KnownWifiHostsFile::AddKnownHost(const std::string& host) {
 
     // Replace the existing adb_known_hosts with the new one
     if (!SafeReplaceFile(keystore_path_, temp_file_name.c_str())) {
-        LOG(ERROR) << "Failed to replace old adb_known_hosts";
+       LOG_SAFE(ERROR) << "Failed to replace old adb_known_hosts";
         adb_unlink(temp_file_name.c_str());
         return false;
     }
diff --git a/vendor/adb/client/auth.cpp b/vendor/adb/client/auth.cpp
index 6ce3d8b..21cef14 100644
--- a/vendor/adb/client/auth.cpp
+++ b/vendor/adb/client/auth.cpp
@@ -63,7 +63,7 @@ static bool generate_key(const std::string& file) {
 
     auto rsa_2048 = CreateRSA2048Key();
     if (!rsa_2048) {
-        LOG(ERROR) << "Unable to create key";
+       LOG_SAFE(ERROR) << "Unable to create key";
         return false;
     }
     std::string pubkey;
@@ -72,7 +72,7 @@ static bool generate_key(const std::string& file) {
     CHECK(rsa);
 
     if (!CalculatePublicKey(&pubkey, rsa)) {
-        LOG(ERROR) << "failed to calculate public key";
+       LOG_SAFE(ERROR) << "failed to calculate public key";
         return false;
     }
 
@@ -90,7 +90,7 @@ static bool generate_key(const std::string& file) {
 
     if (!PEM_write_PrivateKey(f.get(), rsa_2048->GetEvpPkey(), nullptr, nullptr, 0, nullptr,
                               nullptr)) {
-        LOG(ERROR) << "Failed to write key";
+       LOG_SAFE(ERROR) << "Failed to write key";
         return false;
     }
 
@@ -106,7 +106,7 @@ static std::string hash_key(RSA* key) {
     unsigned char* pubkey = nullptr;
     int len = i2d_RSA_PUBKEY(key, &pubkey);
     if (len < 0) {
-        LOG(ERROR) << "failed to encode RSA public key";
+       LOG_SAFE(ERROR) << "failed to encode RSA public key";
         return std::string();
     }
 
@@ -126,7 +126,7 @@ static std::shared_ptr<RSA> read_key_file(const std::string& file) {
 
     RSA* key = RSA_new();
     if (!PEM_read_RSAPrivateKey(fp.get(), &key, nullptr, nullptr)) {
-        LOG(ERROR) << "Failed to read key from '" << file << "'";
+       LOG_SAFE(ERROR) << "Failed to read key from '" << file << "'";
         ERR_print_errors_fp(stderr);
         RSA_free(key);
         return nullptr;
@@ -169,7 +169,7 @@ static bool load_keys(const std::string& path, bool allow_dir = true) {
         if (!allow_dir) {
             // inotify isn't recursive. It would break expectations to load keys in nested
             // directories but not monitor them for new keys.
-            LOG(WARNING) << "load_keys: refusing to recurse into directory '" << path << "'";
+           LOG_SAFE(WARNING) << "load_keys: refusing to recurse into directory '" << path << "'";
             return false;
         }
 
@@ -198,7 +198,7 @@ static bool load_keys(const std::string& path, bool allow_dir = true) {
         return result;
     }
 
-    LOG(ERROR) << "load_keys: unexpected type for '" << path << "': 0x" << std::hex << st.st_mode;
+   LOG_SAFE(ERROR) << "load_keys: unexpected type for '" << path << "': 0x" << std::hex << st.st_mode;
     return false;
 }
 
@@ -217,7 +217,7 @@ static bool load_userkey() {
     if (stat(path.c_str(), &buf) == -1) {
         VLOG(AUTH) << "User key '" << path << "' does not exist...";
         if (!generate_key(path)) {
-            LOG(ERROR) << "Failed to generate new key";
+           LOG_SAFE(ERROR) << "Failed to generate new key";
             return false;
         }
     }
@@ -261,7 +261,7 @@ std::deque<std::shared_ptr<RSA>> adb_auth_get_private_keys() {
 
 static std::string adb_auth_sign(RSA* key, const char* token, size_t token_size) {
     if (token_size != TOKEN_SIZE) {
-        LOG(WARNING) << "Unexpected token size=" << token_size;
+       LOG_SAFE(WARNING) << "Unexpected token size=" << token_size;
         return std::string();
     }
 
@@ -302,7 +302,7 @@ bssl::UniquePtr<EVP_PKEY> adb_auth_get_user_privkey() {
 
     bssl::UniquePtr<EVP_PKEY> pkey(EVP_PKEY_new());
     if (!pkey) {
-        LOG(ERROR) << "Failed to allocate key";
+       LOG_SAFE(ERROR) << "Failed to allocate key";
         return nullptr;
     }
 
@@ -363,7 +363,7 @@ static void adb_auth_inotify_update(int fd, unsigned fd_event, void*) {
             inotify_event* event = reinterpret_cast<inotify_event*>(start);
             auto root_it = g_monitored_paths.find(event->wd);
             if (root_it == g_monitored_paths.end()) {
-                LOG(FATAL) << "observed inotify event for unmonitored path, wd = " << event->wd;
+               LOG_SAFE(FATAL) << "observed inotify event for unmonitored path, wd = " << event->wd;
             }
 
             std::string path = root_it->second;
@@ -380,7 +380,7 @@ static void adb_auth_inotify_update(int fd, unsigned fd_event, void*) {
                     load_keys(path, false);
                 }
             } else {
-                LOG(WARNING) << "unmonitored event for " << path << ": 0x" << std::hex
+               LOG_SAFE(WARNING) << "unmonitored event for " << path << ": 0x" << std::hex
                              << event->mask;
             }
 
@@ -418,7 +418,7 @@ void adb_auth_init() {
     VLOG(AUTH) << "adb_auth_init...";
 
     if (!load_userkey()) {
-        LOG(ERROR) << "Failed to load (or generate) user key";
+       LOG_SAFE(ERROR) << "Failed to load (or generate) user key";
         return;
     }
 
@@ -438,12 +438,12 @@ static void send_auth_publickey(atransport* t) {
 
     std::string key = adb_auth_get_userkey();
     if (key.empty()) {
-        LOG(WARNING) << "Failed to get user public key";
+       LOG_SAFE(WARNING) << "Failed to get user public key";
         return;
     }
 
     if (key.size() >= MAX_PAYLOAD_V1) {
-        LOG(WARNING) << "User public key too large " << key.size() << " bytes";
+       LOG_SAFE(WARNING) << "User public key too large " << key.size() << " bytes";
         return;
     }
 
@@ -472,7 +472,7 @@ void send_auth_response(const char* token, size_t token_size, atransport* t) {
 
     std::string result = adb_auth_sign(key.get(), token, token_size);
     if (result.empty()) {
-        LOG(WARNING) << "Error signing the token";
+       LOG_SAFE(WARNING) << "Error signing the token";
         put_apacket(p);
         return;
     }
diff --git a/vendor/adb/client/commandline.cpp b/vendor/adb/client/commandline.cpp
index abe38dc..6ae0228 100644
--- a/vendor/adb/client/commandline.cpp
+++ b/vendor/adb/client/commandline.cpp
@@ -301,7 +301,7 @@ int read_and_dump_protocol(borrowed_fd fd, StandardStreamsCallbackInterface* cal
     int exit_code = 255;
     std::unique_ptr<ShellProtocol> protocol = std::make_unique<ShellProtocol>(fd);
     if (!protocol) {
-      LOG(ERROR) << "failed to allocate memory for ShellProtocol object";
+     LOG_SAFE(ERROR) << "failed to allocate memory for ShellProtocol object";
       return 1;
     }
     while (protocol->Read()) {
@@ -648,7 +648,7 @@ static int RemoteShell(bool use_shell_protocol, const std::string& type_arg, cha
 
     StdinReadArgs* args = new StdinReadArgs;
     if (!args) {
-        LOG(ERROR) << "couldn't allocate StdinReadArgs object";
+       LOG_SAFE(ERROR) << "couldn't allocate StdinReadArgs object";
         return 1;
     }
     args->stdin_fd = STDIN_FILENO;
@@ -1676,7 +1676,7 @@ int adb_commandline(int argc, const char** argv) {
             rc = asprintf(&temp, "tcp:%d", server_port);
         }
         if (rc < 0) {
-            LOG(FATAL) << "failed to allocate server socket specification";
+           LOG_SAFE(FATAL) << "failed to allocate server socket specification";
         }
         server_socket_str = temp;
     }
diff --git a/vendor/adb/client/file_sync_client.cpp b/vendor/adb/client/file_sync_client.cpp
index 11dde9c..d97be97 100644
--- a/vendor/adb/client/file_sync_client.cpp
+++ b/vendor/adb/client/file_sync_client.cpp
@@ -387,7 +387,7 @@ class SyncConnection {
                 break;
 
             case CompressionType::Any:
-                LOG(FATAL) << "unexpected CompressionType::Any";
+               LOG_SAFE(FATAL) << "unexpected CompressionType::Any";
         }
 
         if (dry_run) {
@@ -438,7 +438,7 @@ class SyncConnection {
                 break;
 
             case CompressionType::Any:
-                LOG(FATAL) << "unexpected CompressionType::Any";
+               LOG_SAFE(FATAL) << "unexpected CompressionType::Any";
         }
 
         buf.resize(sizeof(SyncRequest) + path.length() + sizeof(msg.recv_v2_setup));
@@ -504,7 +504,7 @@ class SyncConnection {
             }
 
             if (msg.stat_v1.id != ID_LSTAT_V1) {
-                LOG(FATAL) << "protocol fault: stat response has wrong message id: "
+               LOG_SAFE(FATAL) << "protocol fault: stat response has wrong message id: "
                            << msg.stat_v1.id;
             }
 
@@ -678,7 +678,7 @@ class SyncConnection {
                 break;
 
             case CompressionType::Any:
-                LOG(FATAL) << "unexpected CompressionType::Any";
+               LOG_SAFE(FATAL) << "unexpected CompressionType::Any";
         }
 
         bool sending = true;
@@ -1193,7 +1193,7 @@ static bool sync_recv_v2(SyncConnection& sc, const char* rpath, const char* lpat
             break;
 
         case CompressionType::Any:
-            LOG(FATAL) << "unexpected CompressionType::Any";
+           LOG_SAFE(FATAL) << "unexpected CompressionType::Any";
     }
 
     while (true) {
@@ -1257,7 +1257,7 @@ static bool sync_recv_v2(SyncConnection& sc, const char* rpath, const char* lpat
                 sc.RecordFilesTransferred(1);
                 return true;
             } else {
-                LOG(FATAL) << "invalid DecodeResult: " << static_cast<int>(result);
+               LOG_SAFE(FATAL) << "invalid DecodeResult: " << static_cast<int>(result);
             }
         }
     }
diff --git a/vendor/adb/client/main.cpp b/vendor/adb/client/main.cpp
index 50a53b0..9182ef5 100644
--- a/vendor/adb/client/main.cpp
+++ b/vendor/adb/client/main.cpp
@@ -63,7 +63,7 @@ static void setup_daemon_logging() {
     unix_close(fd);
 
     fprintf(stderr, "--- adb starting (pid %d) ---\n", getpid());
-    LOG(INFO) << adb_version();
+   LOG_SAFE(INFO) << adb_version();
 }
 
 void adb_server_cleanup() {
@@ -79,7 +79,7 @@ void adb_server_cleanup() {
 static void intentionally_leak() {
     void* p = ::operator new(1);
     // The analyzer is upset about this leaking. NOLINTNEXTLINE
-    LOG(INFO) << "leaking pointer " << p;
+   LOG_SAFE(INFO) << "leaking pointer " << p;
 }
 
 // one_device: if null, server owns all devices, else server owns only
@@ -158,7 +158,7 @@ int adb_server_main(int is_daemon, const std::string& socket_spec, const char* o
     while (install_listener(socket_spec, kSmartSocketConnectTo, nullptr, INSTALL_LISTENER_DISABLED,
                             nullptr, &error) != INSTALL_STATUS_OK) {
         if (std::chrono::steady_clock::now() - start > 0.5s) {
-            LOG(FATAL) << "could not install *smartsocket* listener: " << error;
+           LOG_SAFE(FATAL) << "could not install *smartsocket* listener: " << error;
         }
 
         std::this_thread::sleep_for(100ms);
@@ -194,11 +194,11 @@ int adb_server_main(int is_daemon, const std::string& socket_spec, const char* o
             const DWORD bytes_to_write = arraysize(ack) - 1;
             DWORD written = 0;
             if (!WriteFile(ack_reply_handle, ack, bytes_to_write, &written, NULL)) {
-                LOG(FATAL) << "cannot write ACK to handle " << ack_reply_handle
+               LOG_SAFE(FATAL) << "cannot write ACK to handle " << ack_reply_handle
                            << android::base::SystemErrorCodeToString(GetLastError());
             }
             if (written != bytes_to_write) {
-                LOG(FATAL) << "cannot write " << bytes_to_write << " bytes of ACK: only wrote "
+               LOG_SAFE(FATAL) << "cannot write " << bytes_to_write << " bytes of ACK: only wrote "
                            << written << " bytes";
             }
             CloseHandle(ack_reply_handle);
diff --git a/vendor/adb/client/mdns_tracker.cpp b/vendor/adb/client/mdns_tracker.cpp
index 597d66c..2d02eac 100644
--- a/vendor/adb/client/mdns_tracker.cpp
+++ b/vendor/adb/client/mdns_tracker.cpp
@@ -52,7 +52,7 @@ static std::string list_mdns_services() {
             tls->set_known_device(known_wifi_hosts_file.IsKnownHost(service.instance));
             s = tls->mutable_service();
         } else {
-            LOG(WARNING) << "Unknown service type: " << service;
+           LOG_SAFE(WARNING) << "Unknown service type: " << service;
             return;
         }
 
diff --git a/vendor/adb/client/openscreen/mdns_service.cpp b/vendor/adb/client/openscreen/mdns_service.cpp
index 934abea..fd13bab 100644
--- a/vendor/adb/client/openscreen/mdns_service.cpp
+++ b/vendor/adb/client/openscreen/mdns_service.cpp
@@ -39,12 +39,12 @@ using ServicesUpdatedState = ServiceWatcher::ServicesUpdatedState;
 class DiscoveryReportingClient : public discovery::ReportingClient {
   public:
     void OnFatalError(Error error) override {
-        LOG(ERROR) << "Encountered fatal discovery error: " << error;
+       LOG_SAFE(ERROR) << "Encountered fatal discovery error: " << error;
         got_fatal_ = true;
     }
 
     void OnRecoverableError(Error error) override {
-        LOG(ERROR) << "Encountered recoverable discovery error: " << error;
+       LOG_SAFE(ERROR) << "Encountered recoverable discovery error: " << error;
     }
 
     bool GotFatalError() const { return got_fatal_; }
diff --git a/vendor/adb/client/openscreen/platform/logging.cpp b/vendor/adb/client/openscreen/platform/logging.cpp
index 3b9be16..b283624 100644
--- a/vendor/adb/client/openscreen/platform/logging.cpp
+++ b/vendor/adb/client/openscreen/platform/logging.cpp
@@ -47,15 +47,15 @@ void LogWithLevel(LogLevel level, const char* file, int line, std::stringstream
 
     // We never ignore a warning or worse (error and fatals).
     if (severity >= android::base::WARNING) {
-        LOG(severity) << msg;
+       LOG_SAFE(severity) << msg;
     } else {
         VLOG(MDNS_STACK) << msg;
     }
 }
 
 [[noreturn]] void Break() {
-    LOG(FATAL) << "openscreen Break() called";
-    abort(); // LOG(FATAL) isn't [[noreturn]].
+   LOG_SAFE(FATAL) << "openscreen Break() called";
+    abort(); //LOG_SAFE(FATAL) isn't [[noreturn]].
 }
 
 }  // namespace openscreen
diff --git a/vendor/adb/client/openscreen/platform/udp_socket.cpp b/vendor/adb/client/openscreen/platform/udp_socket.cpp
index 4ea9fab..7a9828b 100644
--- a/vendor/adb/client/openscreen/platform/udp_socket.cpp
+++ b/vendor/adb/client/openscreen/platform/udp_socket.cpp
@@ -163,7 +163,7 @@ class AdbUdpSocket : public UdpSocket {
         CHECK(local_endpoint_.address.IsV4() || local_endpoint_.address.IsV6());
         fde_ = fdevent_create(fd_.get(), OnFdeventResult, this);
         if (!fde_) {
-            LOG(FATAL) << "Unable to create fdevent";
+           LOG_SAFE(FATAL) << "Unable to create fdevent";
             return;
         }
         fdevent_set(fde_, FDE_READ);
@@ -236,7 +236,7 @@ class AdbUdpSocket : public UdpSocket {
         if (mdns_ifindex_) {
             // TODO: move MdnsBind() code back into here once osp-discovery calls Bind() after
             // SetMulticastOutboundInterface().
-            LOG(FATAL) << "osp-discovery called Bind() after SetMulticastOutboundInterface()!";
+           LOG_SAFE(FATAL) << "osp-discovery called Bind() after SetMulticastOutboundInterface()!";
         } else {
             // mdns impl will only call SetMulticastOutboundInterface and JoinMulticastGroup after
             // bind is successful.
@@ -302,7 +302,7 @@ class AdbUdpSocket : public UdpSocket {
                     const auto default_ip =
                             IPAddress(IPAddress::Version::kV4,
                                       reinterpret_cast<const uint8_t*>(&default_addr.s_addr));
-                    LOG(INFO) << "AFTER IP_MULTICAST_IF: default multicast addr=" << default_ip;
+                   LOG_SAFE(INFO) << "AFTER IP_MULTICAST_IF: default multicast addr=" << default_ip;
                 }
 #endif  // !_WIN32
 #endif  // DEBUG_UDP
@@ -340,7 +340,7 @@ class AdbUdpSocket : public UdpSocket {
                 if (adb_setsockopt(fd_, IPPROTO_IP, IP_PKTINFO, &enable_pktinfo,
                                    sizeof(enable_pktinfo)) == -1) {
                     OnError(Error::Code::kSocketOptionSettingFailure);
-                    LOG(ERROR) << "adb_setsockopt failed";
+                   LOG_SAFE(ERROR) << "adb_setsockopt failed";
                     return;
                 }
                 struct ip_mreq multicast_properties;
@@ -348,7 +348,7 @@ class AdbUdpSocket : public UdpSocket {
                 if (adb_setsockopt(fd_, IPPROTO_IP, IP_ADD_MEMBERSHIP, &multicast_properties,
                                    sizeof(multicast_properties)) == -1) {
                     OnError(Error::Code::kSocketOptionSettingFailure);
-                    LOG(ERROR) << "adb_setsockopt failed";
+                   LOG_SAFE(ERROR) << "adb_setsockopt failed";
                     return;
                 }
                 return;
@@ -366,7 +366,7 @@ class AdbUdpSocket : public UdpSocket {
                                    sizeof(enable_pktinfo)) == -1) {
 #endif
                     OnError(Error::Code::kSocketOptionSettingFailure);
-                    LOG(ERROR) << "adb_setsockopt failed";
+                   LOG_SAFE(ERROR) << "adb_setsockopt failed";
                     return;
                 }
                 struct ipv6_mreq multicast_properties = {
@@ -382,7 +382,7 @@ class AdbUdpSocket : public UdpSocket {
                 if (adb_setsockopt(fd_, IPPROTO_IPV6, IPV6_JOIN_GROUP, &multicast_properties,
                                    sizeof(multicast_properties)) == -1) {
                     OnError(Error::Code::kSocketOptionSettingFailure);
-                    LOG(ERROR) << "adb_setsockopt failed";
+                   LOG_SAFE(ERROR) << "adb_setsockopt failed";
                     return;
                 }
                 return;
@@ -441,7 +441,7 @@ class AdbUdpSocket : public UdpSocket {
         // shield ourselves from these and hypothetical "long send" and plain errors by reporting
         // any unexpected return value.
         if (num_bytes_sent != (ssize_t)length) {
-            LOG(WARNING) << "Error: sendmsg datagram size=" << length << " sent=" << num_bytes_sent;
+           LOG_SAFE(WARNING) << "Error: sendmsg datagram size=" << length << " sent=" << num_bytes_sent;
             client_->OnSendError(this, ChooseError(errno, Error::Code::kSocketSendFailure));
             return;
         }
@@ -453,7 +453,7 @@ class AdbUdpSocket : public UdpSocket {
         // TODO: this method doesn't seem to be used anywhere in the openscreen code, so
         // ignoring implementation for now.
         // Windows 10 seems to ignore setsockopt IP_TOS, so need to use Win APIs instead.
-        LOG(FATAL) << "NOT IMPLEMENTED";
+       LOG_SAFE(FATAL) << "NOT IMPLEMENTED";
 #else   // !_WIN32
         if (!fd_.ok()) {
             OnError(Error::Code::kSocketClosedFailure);
@@ -466,15 +466,15 @@ class AdbUdpSocket : public UdpSocket {
 
         if (code == EBADF || code == ENOTSOCK || code == EFAULT) {
             OnError(Error::Code::kSocketOptionSettingFailure);
-            LOG(WARNING) << "BAD SOCKET PROVIDED. CODE: " << code;
+           LOG_SAFE(WARNING) << "BAD SOCKET PROVIDED. CODE: " << code;
             return;
         } else if (code == EINVAL) {
             OnError(Error::Code::kSocketOptionSettingFailure);
-            LOG(WARNING) << "INVALID DSCP INFO PROVIDED";
+           LOG_SAFE(WARNING) << "INVALID DSCP INFO PROVIDED";
             return;
         } else if (code == ENOPROTOOPT) {
             OnError(Error::Code::kSocketOptionSettingFailure);
-            LOG(WARNING) << "INVALID DSCP SETTING LEVEL PROVIDED: " << kSettingLevel;
+           LOG_SAFE(WARNING) << "INVALID DSCP SETTING LEVEL PROVIDED: " << kSettingLevel;
             return;
         }
 #endif  // _WIN32
@@ -486,7 +486,7 @@ class AdbUdpSocket : public UdpSocket {
     void MdnsBind(NetworkInterfaceIndex ifindex) {
         if (!fd_.ok()) {
             OnError(Error::Code::kSocketClosedFailure);
-            LOG(ERROR) << "Bind() failed. Socket is closed.";
+           LOG_SAFE(ERROR) << "Bind() failed. Socket is closed.";
             return;
         }
 
@@ -496,7 +496,7 @@ class AdbUdpSocket : public UdpSocket {
         const int reuse = 1;
         if (adb_setsockopt(fd_, SOL_SOCKET, SO_REUSEADDR, &reuse, sizeof(reuse)) == -1) {
             OnError(Error::Code::kSocketOptionSettingFailure);
-            LOG(WARNING) << "Failed to set SO_REUSEADDR";
+           LOG_SAFE(WARNING) << "Failed to set SO_REUSEADDR";
             return;
         }
 
@@ -505,7 +505,7 @@ class AdbUdpSocket : public UdpSocket {
         // We need to also set the option SO_REUSEPORT.
         if (adb_setsockopt(fd_, SOL_SOCKET, SO_REUSEPORT, &reuse, sizeof(reuse)) == -1) {
             OnError(Error::Code::kSocketOptionSettingFailure);
-            LOG(WARNING) << "Failed to set SO_REUSEPORT";
+           LOG_SAFE(WARNING) << "Failed to set SO_REUSEPORT";
             return;
         }
 #endif
@@ -561,7 +561,7 @@ class AdbUdpSocket : public UdpSocket {
                 return;
             }
             default:
-                LOG(FATAL) << "Invalid domain";
+               LOG_SAFE(FATAL) << "Invalid domain";
                 break;
         }
     }
@@ -592,7 +592,7 @@ class AdbUdpSocket : public UdpSocket {
                 break;
             }
             default: {
-                LOG(FATAL) << "Invalid domain";
+               LOG_SAFE(FATAL) << "Invalid domain";
                 break;
             }
         }
@@ -651,7 +651,7 @@ ErrorOr<std::unique_ptr<UdpSocket>> UdpSocket::Create(TaskRunner* task_runner,
             domain = AF_INET6;
             break;
         default:
-            LOG(FATAL) << "Invalid domain";
+           LOG_SAFE(FATAL) << "Invalid domain";
             return Error::Code::kInitializationFailure;
     }
 
diff --git a/vendor/adb/client/pairing/pairing_client.cpp b/vendor/adb/client/pairing/pairing_client.cpp
index 937a5bd..21a9055 100644
--- a/vendor/adb/client/pairing/pairing_client.cpp
+++ b/vendor/adb/client/pairing/pairing_client.cpp
@@ -106,7 +106,7 @@ bool PairingClientImpl::Start(std::string_view ip_addr, pairing_client_result_cb
     opaque_ = opaque;
 
     if (state_ != State::Ready) {
-        LOG(ERROR) << "PairingClient already running or finished";
+       LOG_SAFE(ERROR) << "PairingClient already running or finished";
         return false;
     }
 
@@ -116,7 +116,7 @@ bool PairingClientImpl::Start(std::string_view ip_addr, pairing_client_result_cb
     CHECK(port_ > 0 && port_ <= 65535);
 
     if (!StartConnection()) {
-        LOG(ERROR) << "Unable to start PairingClient connection";
+       LOG_SAFE(ERROR) << "Unable to start PairingClient connection";
         state_ = State::Stopped;
         return false;
     }
@@ -130,7 +130,7 @@ bool PairingClientImpl::StartConnection() {
     const int timeout = 10;  // seconds
     unique_fd fd(network_connect(host_, port_, SOCK_STREAM, timeout, &err));
     if (fd.get() == -1) {
-        LOG(ERROR) << "Failed to start pairing connection client [" << err << "]";
+       LOG_SAFE(ERROR) << "Failed to start pairing connection client [" << err << "]";
         return false;
     }
     int off = 1;
@@ -143,7 +143,7 @@ bool PairingClientImpl::StartConnection() {
 
     int osh = cast_handle_to_int(adb_get_os_handle(fd.release()));
     if (!pairing_connection_start(connection_.get(), osh, OnPairingResult, this)) {
-        LOG(ERROR) << "PairingClient failed to start the PairingConnection";
+       LOG_SAFE(ERROR) << "PairingClient failed to start the PairingConnection";
         state_ = State::Stopped;
         return false;
     }
diff --git a/vendor/adb/client/transport_emulator.cpp b/vendor/adb/client/transport_emulator.cpp
index 83fb1e4..71358b0 100644
--- a/vendor/adb/client/transport_emulator.cpp
+++ b/vendor/adb/client/transport_emulator.cpp
@@ -118,7 +118,7 @@ void connect_device(const std::string& address, std::string* response) {
         std::string serial;
         socket_spec_connect(&fd, prefix_addr, &port, &serial, &response);
         if (fd == -1) {
-            LOG(INFO) << "reconnect " << serial << " failed: " << response;
+           LOG_SAFE(INFO) << "reconnect " << serial << " failed: " << response;
             return ReconnectResult::Retry;
         }
         // This invokes the part of register_socket_transport() that needs to be
diff --git a/vendor/adb/client/transport_usb.cpp b/vendor/adb/client/transport_usb.cpp
index d3b4929..5d882f6 100644
--- a/vendor/adb/client/transport_usb.cpp
+++ b/vendor/adb/client/transport_usb.cpp
@@ -147,7 +147,7 @@ bool UsbConnection::Write(apacket* packet) {
 
 bool UsbConnection::DoTlsHandshake(RSA* key, std::string* auth_key) {
     // TODO: support TLS for usb connections
-    LOG(FATAL) << "Not supported yet.";
+   LOG_SAFE(FATAL) << "Not supported yet.";
     return false;
 }
 
diff --git a/vendor/adb/client/usb_libusb.cpp b/vendor/adb/client/usb_libusb.cpp
index 34dc5c4..1b70e5e 100644
--- a/vendor/adb/client/usb_libusb.cpp
+++ b/vendor/adb/client/usb_libusb.cpp
@@ -133,7 +133,7 @@ void LibUsbConnection::StartWriteThread() {
 }
 
 bool LibUsbConnection::DoTlsHandshake(RSA* key, std::string* auth_key) {
-    LOG(WARNING) << "TlsHandshake is not supported by libusb backend";
+   LOG_SAFE(WARNING) << "TlsHandshake is not supported by libusb backend";
     return false;
 }
 
@@ -141,12 +141,12 @@ void LibUsbConnection::Reset() {
     {
         std::lock_guard<std::mutex> lock(mutex_);
         if (!running_) {
-            LOG(INFO) << "LibUsbConnection(" << Serial() << "): not running";
+           LOG_SAFE(INFO) << "LibUsbConnection(" << Serial() << "): not running";
             return;
         }
     }
 
-    LOG(INFO) << "LibUsbConnection(" << Serial() << "): RESET";
+   LOG_SAFE(INFO) << "LibUsbConnection(" << Serial() << "): RESET";
     this->device_->Reset();
     Stop();
 }
@@ -156,14 +156,14 @@ void LibUsbConnection::Stop() {
         std::lock_guard<std::mutex> lock(mutex_);
 
         if (!running_) {
-            LOG(INFO) << "LibUsbConnection(" << Serial() << ") Stop: not running";
+           LOG_SAFE(INFO) << "LibUsbConnection(" << Serial() << ") Stop: not running";
             return;
         }
 
         running_ = false;
     }
 
-    LOG(INFO) << "LibUsbConnection(" << Serial() << "): stopping";
+   LOG_SAFE(INFO) << "LibUsbConnection(" << Serial() << "): stopping";
 
     this->device_->Close();
     this->cv_write_.notify_one();
diff --git a/vendor/adb/client/usb_libusb_device.cpp b/vendor/adb/client/usb_libusb_device.cpp
index f75178e..142b4fb 100644
--- a/vendor/adb/client/usb_libusb_device.cpp
+++ b/vendor/adb/client/usb_libusb_device.cpp
@@ -169,7 +169,7 @@ void LibUsbDevice::Reset() {
     }
     int rc = libusb_reset_device(device_handle_);
     if (rc != 0) {
-        LOG(ERROR) << "libusb_reset_device failed: " << libusb_error_name(rc);
+       LOG_SAFE(ERROR) << "libusb_reset_device failed: " << libusb_error_name(rc);
     }
 }
 
@@ -191,7 +191,7 @@ std::optional<libusb_device_descriptor> LibUsbDevice::GetDeviceDescriptor() {
     libusb_device_descriptor device_desc;
     int rc = libusb_get_device_descriptor(device_, &device_desc);
     if (rc != 0) {
-        LOG(WARNING) << "failed to get device descriptor for device :" << libusb_error_name(rc);
+       LOG_SAFE(WARNING) << "failed to get device descriptor for device :" << libusb_error_name(rc);
         return {};
     }
     return device_desc;
@@ -219,7 +219,7 @@ bool LibUsbDevice::FindAdbInterface() {
     libusb_config_descriptor* config;
     int rc = libusb_get_active_config_descriptor(device_, &config);
     if (rc != 0) {
-        LOG(WARNING) << "failed to get active config descriptor for device at " << device_address_
+       LOG_SAFE(WARNING) << "failed to get active config descriptor for device at " << device_address_
                      << ": " << libusb_error_name(rc);
         return false;
     }
@@ -325,7 +325,7 @@ bool LibUsbDevice::RetrieveSerial() {
                                                 reinterpret_cast<unsigned char*>(&serial_[0]),
                                                 serial_.length());
     if (rc == 0) {
-        LOG(WARNING) << "received empty serial from device at " << device_address_;
+       LOG_SAFE(WARNING) << "received empty serial from device at " << device_address_;
         return false;
     } else if (rc < 0) {
         VLOG(USB) << "failed to get serial from device " << device_address_ << " :"
@@ -533,7 +533,7 @@ USBSessionID LibUsbDevice::GenerateSessionId(libusb_device* dev) {
     libusb_device_descriptor desc{};
     auto result = libusb_get_device_descriptor(dev, &desc);
     if (result != LIBUSB_SUCCESS) {
-        LOG(WARNING) << "Unable to retrieve device descriptor: " << libusb_error_name(result);
+       LOG_SAFE(WARNING) << "Unable to retrieve device descriptor: " << libusb_error_name(result);
         return USBSessionID{};
     }
 
diff --git a/vendor/adb/client/usb_libusb_hotplug.cpp b/vendor/adb/client/usb_libusb_hotplug.cpp
index 9570570..d6bdd4a 100644
--- a/vendor/adb/client/usb_libusb_hotplug.cpp
+++ b/vendor/adb/client/usb_libusb_hotplug.cpp
@@ -58,7 +58,7 @@ static std::unordered_map<libusb_device*, std::weak_ptr<LibUsbConnection>> GUARD
 static void process_device(libusb_device* raw_device) {
     auto device = std::make_unique<LibUsbDevice>(raw_device);
     if (!device) {
-        LOG(FATAL) << "Failed to construct LibusbConnection";
+       LOG_SAFE(FATAL) << "Failed to construct LibusbConnection";
     }
 
     if (!device->IsInitialized()) {
@@ -152,7 +152,7 @@ static void device_connected(libusb_device* device) {
                     if (errno == EINTR) {
                         continue;
                     } else {
-                        LOG(WARNING) << "timeout expired while waiting for device accessibility";
+                       LOG_SAFE(WARNING) << "timeout expired while waiting for device accessibility";
                         break;
                     }
                 }
@@ -200,7 +200,7 @@ static void hotplug_thread() {
                 VLOG(USB) << "libusb hotplug: device left";
                 device_disconnected(device);
             } else {
-                LOG(WARNING) << "unknown libusb hotplug event: " << event;
+               LOG_SAFE(WARNING) << "unknown libusb hotplug event: " << event;
             }
         });
     }
@@ -231,7 +231,7 @@ static void usb_init_libusb_hotplug() {
             LIBUSB_HOTPLUG_MATCH_ANY, hotplug_callback, nullptr, nullptr);
 
     if (rc != LIBUSB_SUCCESS) {
-        LOG(FATAL) << "failed to register libusb hotplug callback";
+       LOG_SAFE(FATAL) << "failed to register libusb hotplug callback";
     }
 
     // Spawn a thread for libusb_handle_events.
@@ -260,7 +260,7 @@ void usb_init() {
     VLOG(USB) << "initializing libusb...";
     int rc = libusb_init(nullptr);
     if (rc != 0) {
-        LOG(WARNING) << "failed to initialize libusb: " << libusb_error_name(rc);
+       LOG_SAFE(WARNING) << "failed to initialize libusb: " << libusb_error_name(rc);
         return;
     }
 
diff --git a/vendor/adb/client/usb_linux.cpp b/vendor/adb/client/usb_linux.cpp
index de33a1b..9b1585e 100644
--- a/vendor/adb/client/usb_linux.cpp
+++ b/vendor/adb/client/usb_linux.cpp
@@ -630,7 +630,7 @@ static void scan_usb_devices(void (*register_device_callback)(const char*, const
     std::string base = "/dev/bus/usb";
     std::unique_ptr<DIR, int (*)(DIR*)> bus_dir(opendir(base.c_str()), closedir);
     if (!bus_dir) {
-        LOG(WARNING) << "Failed to open bus directory '" << base << "': " << strerror(errno);
+       LOG_SAFE(WARNING) << "Failed to open bus directory '" << base << "': " << strerror(errno);
         return;
     }
 
@@ -665,13 +665,13 @@ static void device_poll_thread() {
     // Set up netlink to get add/remove events
     unique_fd fd{socket(AF_NETLINK, SOCK_RAW, NETLINK_KOBJECT_UEVENT)};
     if (fd == -1) {
-        LOG(WARNING) << "Failed to open netlink socket: " << strerror(errno);
+       LOG_SAFE(WARNING) << "Failed to open netlink socket: " << strerror(errno);
         return;
     }
 
     sockaddr_nl sa = {.nl_family = AF_NETLINK, .nl_groups = 1};
     if (bind(fd.get(), (sockaddr*)&sa, sizeof(sa)) < 0) {
-        LOG(WARNING) << "Failed to bind netlink socket: " << strerror(errno);
+       LOG_SAFE(WARNING) << "Failed to bind netlink socket: " << strerror(errno);
         return;
     }
 
@@ -679,25 +679,25 @@ static void device_poll_thread() {
     while (true) {
         ssize_t len = TEMP_FAILURE_RETRY(recv(fd.get(), buffer, sizeof(buffer) - 1, 0));
         if (len < 0) {
-            LOG(ERROR) << "Netlink socket recv has failed: " << strerror(errno);
+           LOG_SAFE(ERROR) << "Netlink socket recv has failed: " << strerror(errno);
             break;
         }
         NetlinkMessage msg(buffer, len);
 
         if (!msg.has_attr("SUBSYSTEM", "usb")) {
-            LOG(WARNING) << "Netlink: SUBSYSTEM not found";
+           LOG_SAFE(WARNING) << "Netlink: SUBSYSTEM not found";
             continue;
         }
 
         // We also receive "add" for "usb_interface" but we only look for whole device.
         if (!msg.has_attr("DEVTYPE", "usb_device")) {
-            LOG(WARNING) << "Netlink: DEVTYPE not found";
+           LOG_SAFE(WARNING) << "Netlink: DEVTYPE not found";
             continue;
         }
 
         std::string dev_name = "/dev/" + msg.attr("DEVNAME");
         if (dev_name.empty()) {
-            LOG(WARNING) << "Netlink: usb_device DEVNAME not found";
+           LOG_SAFE(WARNING) << "Netlink: usb_device DEVNAME not found";
             continue;
         }
 
diff --git a/vendor/adb/client/usb_osx.cpp b/vendor/adb/client/usb_osx.cpp
index 730150b..7ea2444 100644
--- a/vendor/adb/client/usb_osx.cpp
+++ b/vendor/adb/client/usb_osx.cpp
@@ -125,14 +125,14 @@ static bool FindUSBDevices() {
     // Create the matching dictionary to find the Android device's adb interface.
     CFMutableDictionaryRef matchingDict = IOServiceMatching(kIOUSBInterfaceClassName);
     if (!matchingDict) {
-        LOG(ERROR) << "couldn't create USB matching dictionary";
+       LOG_SAFE(ERROR) << "couldn't create USB matching dictionary";
         return false;
     }
     // Create an iterator for all I/O Registry objects that match the dictionary.
     io_iterator_t iter = 0;
     kern_return_t kr = IOServiceGetMatchingServices(kIOMasterPortDefault, matchingDict, &iter);
     if (kr != KERN_SUCCESS) {
-        LOG(ERROR) << "failed to get matching services";
+       LOG_SAFE(ERROR) << "failed to get matching services";
         return false;
     }
     // Iterate over all matching objects.
@@ -168,7 +168,7 @@ AndroidInterfaceAdded(io_iterator_t iterator)
                                                &plugInInterface, &score);
         IOObjectRelease(usbInterface);
         if ((kIOReturnSuccess != kr) || (!plugInInterface)) {
-            LOG(ERROR) << "Unable to create an interface plug-in (" << std::hex << kr << ")";
+           LOG_SAFE(ERROR) << "Unable to create an interface plug-in (" << std::hex << kr << ")";
             continue;
         }
 
@@ -179,7 +179,7 @@ AndroidInterfaceAdded(io_iterator_t iterator)
         //* We only needed the plugin to get the interface, so discard it
         (*plugInInterface)->Release(plugInInterface);
         if (result || !iface) {
-            LOG(ERROR) << "Couldn't query the interface (" << std::hex << result << ")";
+           LOG_SAFE(ERROR) << "Couldn't query the interface (" << std::hex << result << ")";
             continue;
         }
 
@@ -199,7 +199,7 @@ AndroidInterfaceAdded(io_iterator_t iterator)
         //* Gotta love OS X
         kr = (*iface)->GetDevice(iface, &usbDevice);
         if (kIOReturnSuccess != kr || !usbDevice) {
-            LOG(ERROR) << "Couldn't grab device from interface (" << std::hex << kr << ")";
+           LOG_SAFE(ERROR) << "Couldn't grab device from interface (" << std::hex << kr << ")";
             (*iface)->Release(iface);
             continue;
         }
@@ -214,7 +214,7 @@ AndroidInterfaceAdded(io_iterator_t iterator)
         //* only needed this to find the plugin
         (void)IOObjectRelease(usbDevice);
         if ((kIOReturnSuccess != kr) || (!plugInInterface)) {
-            LOG(ERROR) << "Unable to create a device plug-in (" << std::hex << kr << ")";
+           LOG_SAFE(ERROR) << "Unable to create a device plug-in (" << std::hex << kr << ")";
             (*iface)->Release(iface);
             continue;
         }
@@ -224,7 +224,7 @@ AndroidInterfaceAdded(io_iterator_t iterator)
         //* only needed this to query the plugin
         (*plugInInterface)->Release(plugInInterface);
         if (result || !dev) {
-            LOG(ERROR) << "Couldn't create a device interface (" << std::hex << result << ")";
+           LOG_SAFE(ERROR) << "Couldn't create a device interface (" << std::hex << result << ")";
             (*iface)->Release(iface);
             continue;
         }
@@ -315,14 +315,14 @@ AndroidInterfaceAdded(io_iterator_t iterator)
         std::unique_ptr<usb_handle> handle =
             CheckInterface((IOUSBInterfaceInterface550**)iface, vendor, product);
         if (handle == nullptr) {
-            LOG(ERROR) << "Could not find device interface";
+           LOG_SAFE(ERROR) << "Could not find device interface";
             (*iface)->Release(iface);
             continue;
         }
         handle->devpath = devpath;
         usb_handle* handle_p = handle.get();
         VLOG(USB) << "Add usb device " << serial;
-        LOG(INFO) << "reported max packet size for " << serial << " is " << handle->max_packet_size;
+       LOG_SAFE(INFO) << "reported max packet size for " << serial << " is " << handle->max_packet_size;
         AddDevice(std::move(handle));
         register_usb_transport(reinterpret_cast<::usb_handle*>(handle_p), serial, devpath.c_str(),
                                1);
@@ -345,7 +345,7 @@ static bool ClearPipeStallBothEnds(IOUSBInterfaceInterface550** interface, UInt8
 
     IOReturn rc = (*interface)->ClearPipeStallBothEnds(interface, bulkEp);
     if (rc != kIOReturnSuccess) {
-        LOG(ERROR) << "Could not clear pipe stall both ends: " << std::hex << rc;
+       LOG_SAFE(ERROR) << "Could not clear pipe stall both ends: " << std::hex << rc;
         return false;
     }
     return true;
@@ -424,14 +424,14 @@ static std::unique_ptr<usb_handle> CheckInterface(IOUSBInterfaceInterface550** i
     //* the endpoints in the interface descriptor to be instantiated
     kr = (*interface)->USBInterfaceOpen(interface);
     if (kr != kIOReturnSuccess) {
-        LOG(ERROR) << "Could not open interface: " << std::hex << kr;
+       LOG_SAFE(ERROR) << "Could not open interface: " << std::hex << kr;
         return NULL;
     }
 
     //* Get the number of endpoints associated with this interface
     kr = (*interface)->GetNumEndpoints(interface, &interfaceNumEndpoints);
     if (kr != kIOReturnSuccess) {
-        LOG(ERROR) << "Unable to get number of endpoints: " << std::hex << kr;
+       LOG_SAFE(ERROR) << "Unable to get number of endpoints: " << std::hex << kr;
         goto err_get_num_ep;
     }
 
@@ -439,7 +439,7 @@ static std::unique_ptr<usb_handle> CheckInterface(IOUSBInterfaceInterface550** i
     if ((*interface)->GetInterfaceClass(interface, &interfaceClass) != kIOReturnSuccess ||
             (*interface)->GetInterfaceSubClass(interface, &interfaceSubClass) != kIOReturnSuccess ||
             (*interface)->GetInterfaceProtocol(interface, &interfaceProtocol) != kIOReturnSuccess) {
-            LOG(ERROR) << "Unable to get interface class, subclass and protocol";
+           LOG_SAFE(ERROR) << "Unable to get interface class, subclass and protocol";
             goto err_get_interface_class;
     }
 
@@ -465,7 +465,7 @@ static std::unique_ptr<usb_handle> CheckInterface(IOUSBInterfaceInterface550** i
         // because it is the FULL maxPacketSize which includes burst and mul.
         kr = (*interface)->GetPipePropertiesV3(interface, endpoint, &properties);
         if (kr != kIOReturnSuccess) {
-            LOG(ERROR) << "GetPipePropertiesV3 error : " << darwinErrorToString(kr);
+           LOG_SAFE(ERROR) << "GetPipePropertiesV3 error : " << darwinErrorToString(kr);
             goto err_get_pipe_props;
         }
         dumpEndpointProperties("GetPipePropertiesV3 values", properties);
@@ -475,7 +475,7 @@ static std::unique_ptr<usb_handle> CheckInterface(IOUSBInterfaceInterface550** i
         // GetPipePropertiesV3.
         kr = (*interface)->GetEndpointPropertiesV3(interface, &properties);
         if (kr != kIOReturnSuccess) {
-            LOG(ERROR) << "GetEndpointPropertiesV3 error : " << darwinErrorToString(kr);
+           LOG_SAFE(ERROR) << "GetEndpointPropertiesV3 error : " << darwinErrorToString(kr);
             goto err_get_pipe_props;
         }
         dumpEndpointProperties("GetEndpointPropertiesV3 values", properties);
@@ -568,12 +568,12 @@ int usb_write(usb_handle *handle, const void *buf, int len)
         return -1;
 
     if (NULL == handle->interface) {
-        LOG(ERROR) << "usb_write interface was null";
+       LOG_SAFE(ERROR) << "usb_write interface was null";
         return -1;
     }
 
     if (0 == handle->bulkOut) {
-        LOG(ERROR) << "bulkOut endpoint not assigned";
+       LOG_SAFE(ERROR) << "bulkOut endpoint not assigned";
         return -1;
     }
 
@@ -592,7 +592,7 @@ int usb_write(usb_handle *handle, const void *buf, int len)
     if (!result)
         return len;
 
-    LOG(ERROR) << "usb_write failed with status: " << std::hex << result;
+   LOG_SAFE(ERROR) << "usb_write failed with status: " << std::hex << result;
     return -1;
 }
 
@@ -610,19 +610,19 @@ int usb_read(usb_handle *handle, void *buf, int len)
     }
 
     if (NULL == handle->interface) {
-        LOG(ERROR) << "usb_read interface was null";
+       LOG_SAFE(ERROR) << "usb_read interface was null";
         return -1;
     }
 
     if (0 == handle->bulkIn) {
-        LOG(ERROR) << "bulkIn endpoint not assigned";
+       LOG_SAFE(ERROR) << "bulkIn endpoint not assigned";
         return -1;
     }
 
     result = (*handle->interface)->ReadPipe(handle->interface, handle->bulkIn, buf, &numBytes);
 
     if (kIOUSBPipeStalled == result) {
-        LOG(ERROR) << "Pipe stalled, clearing stall.\n";
+       LOG_SAFE(ERROR) << "Pipe stalled, clearing stall.\n";
         (*handle->interface)->ClearPipeStall(handle->interface, handle->bulkIn);
         result = (*handle->interface)->ReadPipe(handle->interface, handle->bulkIn, buf, &numBytes);
     }
@@ -630,7 +630,7 @@ int usb_read(usb_handle *handle, void *buf, int len)
     if (kIOReturnSuccess == result)
         return numBytes;
     else {
-        LOG(ERROR) << "usb_read failed with status: " << std::hex << result;
+       LOG_SAFE(ERROR) << "usb_read failed with status: " << std::hex << result;
     }
 
     return -1;
@@ -655,7 +655,7 @@ void usb_reset(usb_handle* handle) {
 
 static void usb_kick_locked(usb_handle *handle)
 {
-    LOG(INFO) << "Kicking handle";
+   LOG_SAFE(INFO) << "Kicking handle";
     /* release the interface */
     if (!handle)
         return;
diff --git a/vendor/adb/client/usb_windows.cpp b/vendor/adb/client/usb_windows.cpp
index b17c480..aa36f00 100644
--- a/vendor/adb/client/usb_windows.cpp
+++ b/vendor/adb/client/usb_windows.cpp
@@ -213,7 +213,7 @@ static void _power_notification_thread() {
     const HINSTANCE instance = GetModuleHandleW(nullptr);
     if (!instance) {
         // This is such a common API call that this should never fail.
-        LOG(FATAL) << "GetModuleHandleW failed: "
+       LOG_SAFE(FATAL) << "GetModuleHandleW failed: "
                    << android::base::SystemErrorCodeToString(GetLastError());
     }
 
@@ -224,14 +224,14 @@ static void _power_notification_thread() {
     wndclass.hInstance = instance;
     wndclass.lpszClassName = kPowerNotificationWindowClassName;
     if (!RegisterClassExW(&wndclass)) {
-        LOG(FATAL) << "RegisterClassExW failed: "
+       LOG_SAFE(FATAL) << "RegisterClassExW failed: "
                    << android::base::SystemErrorCodeToString(GetLastError());
     }
 
     if (!CreateWindowExW(WS_EX_NOACTIVATE, kPowerNotificationWindowClassName,
                          L"ADB Power Notification Window", WS_POPUP, 0, 0, 0, 0, nullptr, nullptr,
                          instance, nullptr)) {
-        LOG(FATAL) << "CreateWindowExW failed: "
+       LOG_SAFE(FATAL) << "CreateWindowExW failed: "
                    << android::base::SystemErrorCodeToString(GetLastError());
     }
 
diff --git a/vendor/adb/compression_utils.h b/vendor/adb/compression_utils.h
index a747108..cf7567e 100644
--- a/vendor/adb/compression_utils.h
+++ b/vendor/adb/compression_utils.h
@@ -48,7 +48,7 @@ struct Decoder {
     bool Finish() {
         bool old = std::exchange(finished_, true);
         if (old) {
-            LOG(FATAL) << "Decoder::Finish called while already finished?";
+           LOG_SAFE(FATAL) << "Decoder::Finish called while already finished?";
             return false;
         }
         return true;
@@ -70,7 +70,7 @@ struct Encoder {
     bool Finish() {
         bool old = std::exchange(finished_, true);
         if (old) {
-            LOG(FATAL) << "Decoder::Finish called while already finished?";
+           LOG_SAFE(FATAL) << "Decoder::Finish called while already finished?";
             return false;
         }
         return true;
@@ -237,7 +237,7 @@ struct LZ4Decoder final : public Decoder {
         : Decoder(output_buffer), decoder_(nullptr, nullptr) {
         LZ4F_dctx* dctx;
         if (LZ4F_createDecompressionContext(&dctx, LZ4F_VERSION) != 0) {
-            LOG(FATAL) << "failed to initialize LZ4 decompression context";
+           LOG_SAFE(FATAL) << "failed to initialize LZ4 decompression context";
         }
         decoder_ = std::unique_ptr<LZ4F_dctx, decltype(&LZ4F_freeDecompressionContext)>(
                 dctx, LZ4F_freeDecompressionContext);
@@ -253,7 +253,7 @@ struct LZ4Decoder final : public Decoder {
         size_t rc = LZ4F_decompress(decoder_.get(), next_out, &available_out, next_in,
                                     &available_in, nullptr);
         if (LZ4F_isError(rc)) {
-            LOG(ERROR) << "LZ4F_decompress failed: " << LZ4F_getErrorName(rc);
+           LOG_SAFE(ERROR) << "LZ4F_decompress failed: " << LZ4F_getErrorName(rc);
             return DecodeResult::Error;
         }
 
@@ -261,7 +261,7 @@ struct LZ4Decoder final : public Decoder {
 
         if (rc == 0) {
             if (!input_buffer_.empty()) {
-                LOG(ERROR) << "LZ4 stream hit end before reading all data";
+               LOG_SAFE(ERROR) << "LZ4 stream hit end before reading all data";
                 return DecodeResult::Error;
             }
             lz4_done_ = true;
@@ -287,14 +287,14 @@ struct LZ4Encoder final : public Encoder {
         : Encoder(output_block_size), encoder_(nullptr, nullptr) {
         LZ4F_cctx* cctx;
         if (LZ4F_createCompressionContext(&cctx, LZ4F_VERSION) != 0) {
-            LOG(FATAL) << "failed to initialize LZ4 compression context";
+           LOG_SAFE(FATAL) << "failed to initialize LZ4 compression context";
         }
         encoder_ = std::unique_ptr<LZ4F_cctx, decltype(&LZ4F_freeCompressionContext)>(
                 cctx, LZ4F_freeCompressionContext);
         Block header(LZ4F_HEADER_SIZE_MAX);
         size_t rc = LZ4F_compressBegin(encoder_.get(), header.data(), header.size(), nullptr);
         if (LZ4F_isError(rc)) {
-            LOG(FATAL) << "LZ4F_compressBegin failed: %s", LZ4F_getErrorName(rc);
+           LOG_SAFE(FATAL) << "LZ4F_compressBegin failed: %s", LZ4F_getErrorName(rc);
         }
         header.resize(rc);
         output_buffer_.append(std::move(header));
@@ -321,7 +321,7 @@ struct LZ4Encoder final : public Encoder {
 
         if (available_in != 0) {
             if (lz4_finalized_) {
-                LOG(ERROR) << "LZ4Encoder received data after Finish?";
+               LOG_SAFE(ERROR) << "LZ4Encoder received data after Finish?";
                 return EncodeResult::Error;
             }
 
@@ -334,7 +334,7 @@ struct LZ4Encoder final : public Encoder {
             size_t rc = LZ4F_compressUpdate(encoder_.get(), next_out, available_out, next_in,
                                             available_in, nullptr);
             if (LZ4F_isError(rc)) {
-                LOG(ERROR) << "LZ4F_compressUpdate failed: " << LZ4F_getErrorName(rc);
+               LOG_SAFE(ERROR) << "LZ4F_compressUpdate failed: " << LZ4F_getErrorName(rc);
                 return EncodeResult::Error;
             }
 
@@ -354,7 +354,7 @@ struct LZ4Encoder final : public Encoder {
             size_t rc = LZ4F_compressEnd(encoder_.get(), final_block.data(), final_block.size(),
                                          nullptr);
             if (LZ4F_isError(rc)) {
-                LOG(ERROR) << "LZ4F_compressEnd failed: " << LZ4F_getErrorName(rc);
+               LOG_SAFE(ERROR) << "LZ4F_compressEnd failed: " << LZ4F_getErrorName(rc);
                 return EncodeResult::Error;
             }
 
@@ -387,7 +387,7 @@ struct ZstdDecoder final : public Decoder {
     explicit ZstdDecoder(std::span<char> output_buffer)
         : Decoder(output_buffer), decoder_(ZSTD_createDStream(), ZSTD_freeDStream) {
         if (!decoder_) {
-            LOG(FATAL) << "failed to initialize Zstd decompression context";
+           LOG_SAFE(FATAL) << "failed to initialize Zstd decompression context";
         }
     }
 
@@ -406,14 +406,14 @@ struct ZstdDecoder final : public Decoder {
 
         size_t rc = ZSTD_decompressStream(decoder_.get(), &out, &in);
         if (ZSTD_isError(rc)) {
-            LOG(ERROR) << "ZSTD_decompressStream failed: " << ZSTD_getErrorName(rc);
+           LOG_SAFE(ERROR) << "ZSTD_decompressStream failed: " << ZSTD_getErrorName(rc);
             return DecodeResult::Error;
         }
 
         input_buffer_.drop_front(in.pos);
         if (rc == 0) {
             if (!input_buffer_.empty()) {
-                LOG(ERROR) << "Zstd stream hit end before reading all data";
+               LOG_SAFE(ERROR) << "Zstd stream hit end before reading all data";
                 return DecodeResult::Error;
             }
             zstd_done_ = true;
@@ -437,7 +437,7 @@ struct ZstdEncoder final : public Encoder {
     explicit ZstdEncoder(size_t output_block_size)
         : Encoder(output_block_size), encoder_(ZSTD_createCStream(), ZSTD_freeCStream) {
         if (!encoder_) {
-            LOG(FATAL) << "failed to initialize Zstd compression context";
+           LOG_SAFE(FATAL) << "failed to initialize Zstd compression context";
         }
         ZSTD_CCtx_setParameter(encoder_.get(), ZSTD_c_compressionLevel, 1);
     }
@@ -458,7 +458,7 @@ struct ZstdEncoder final : public Encoder {
         ZSTD_EndDirective end_directive = finished_ ? ZSTD_e_end : ZSTD_e_continue;
         size_t rc = ZSTD_compressStream2(encoder_.get(), &out, &in, end_directive);
         if (ZSTD_isError(rc)) {
-            LOG(ERROR) << "ZSTD_compressStream2 failed: " << ZSTD_getErrorName(rc);
+           LOG_SAFE(ERROR) << "ZSTD_compressStream2 failed: " << ZSTD_getErrorName(rc);
             return EncodeResult::Error;
         }
 
@@ -469,7 +469,7 @@ struct ZstdEncoder final : public Encoder {
             // Zstd finished flushing its data.
             if (finished_) {
                 if (!input_buffer_.empty()) {
-                    LOG(ERROR) << "ZSTD_compressStream2 finished early";
+                   LOG_SAFE(ERROR) << "ZSTD_compressStream2 finished early";
                     return EncodeResult::Error;
                 }
                 return EncodeResult::Done;
diff --git a/vendor/adb/crypto/key.cpp b/vendor/adb/crypto/key.cpp
index 4d87006..1310688 100644
--- a/vendor/adb/crypto/key.cpp
+++ b/vendor/adb/crypto/key.cpp
@@ -29,14 +29,14 @@ std::string Key::ToPEMString(EVP_PKEY* pkey) {
     bssl::UniquePtr<BIO> bio(BIO_new(BIO_s_mem()));
     int rc = PEM_write_bio_PKCS8PrivateKey(bio.get(), pkey, nullptr, nullptr, 0, nullptr, nullptr);
     if (rc != 1) {
-        LOG(ERROR) << "PEM_write_bio_PKCS8PrivateKey failed";
+       LOG_SAFE(ERROR) << "PEM_write_bio_PKCS8PrivateKey failed";
         return "";
     }
 
     BUF_MEM* mem = nullptr;
     BIO_get_mem_ptr(bio.get(), &mem);
     if (!mem || !mem->data || !mem->length) {
-        LOG(ERROR) << "BIO_get_mem_ptr failed";
+       LOG_SAFE(ERROR) << "BIO_get_mem_ptr failed";
         return "";
     }
 
diff --git a/vendor/adb/crypto/rsa_2048_key.cpp b/vendor/adb/crypto/rsa_2048_key.cpp
index 6d9ee30..1c5d512 100644
--- a/vendor/adb/crypto/rsa_2048_key.cpp
+++ b/vendor/adb/crypto/rsa_2048_key.cpp
@@ -28,13 +28,13 @@ namespace crypto {
 bool CalculatePublicKey(std::string* out, RSA* private_key) {
     uint8_t binary_key_data[ANDROID_PUBKEY_ENCODED_SIZE];
     if (!android_pubkey_encode(private_key, binary_key_data, sizeof(binary_key_data))) {
-        LOG(ERROR) << "Failed to convert to public key";
+       LOG_SAFE(ERROR) << "Failed to convert to public key";
         return false;
     }
 
     size_t expected_length;
     if (!EVP_EncodedLength(&expected_length, sizeof(binary_key_data))) {
-        LOG(ERROR) << "Public key too large to base64 encode";
+       LOG_SAFE(ERROR) << "Public key too large to base64 encode";
         return false;
     }
 
@@ -54,7 +54,7 @@ std::optional<Key> CreateRSA2048Key() {
     bssl::UniquePtr<BIGNUM> exponent(BN_new());
     bssl::UniquePtr<RSA> rsa(RSA_new());
     if (!pkey || !exponent || !rsa) {
-        LOG(ERROR) << "Failed to allocate key";
+       LOG_SAFE(ERROR) << "Failed to allocate key";
         return std::nullopt;
     }
 
diff --git a/vendor/adb/crypto/x509_generator.cpp b/vendor/adb/crypto/x509_generator.cpp
index a64e49a..8cda408 100644
--- a/vendor/adb/crypto/x509_generator.cpp
+++ b/vendor/adb/crypto/x509_generator.cpp
@@ -60,7 +60,7 @@ bssl::UniquePtr<X509> GenerateX509Certificate(EVP_PKEY* pkey) {
     CHECK(pkey);
     bssl::UniquePtr<X509> x509(X509_new());
     if (!x509) {
-        LOG(ERROR) << "Unable to allocate x509 container";
+       LOG_SAFE(ERROR) << "Unable to allocate x509 container";
         return nullptr;
     }
     X509_set_version(x509.get(), 2);
@@ -70,13 +70,13 @@ bssl::UniquePtr<X509> GenerateX509Certificate(EVP_PKEY* pkey) {
     X509_gmtime_adj(X509_get_notAfter(x509.get()), kCertLifetimeSeconds);
 
     if (!X509_set_pubkey(x509.get(), pkey)) {
-        LOG(ERROR) << "Unable to set x509 public key";
+       LOG_SAFE(ERROR) << "Unable to set x509 public key";
         return nullptr;
     }
 
     X509_NAME* name = X509_get_subject_name(x509.get());
     if (!name) {
-        LOG(ERROR) << "Unable to get x509 subject name";
+       LOG_SAFE(ERROR) << "Unable to get x509 subject name";
         return nullptr;
     }
     X509_NAME_add_entry_by_txt(name, "C", MBSTRING_ASC,
@@ -86,7 +86,7 @@ bssl::UniquePtr<X509> GenerateX509Certificate(EVP_PKEY* pkey) {
     X509_NAME_add_entry_by_txt(name, "CN", MBSTRING_ASC,
                                reinterpret_cast<const unsigned char*>("Adb"), -1, -1, 0);
     if (!X509_set_issuer_name(x509.get(), name)) {
-        LOG(ERROR) << "Unable to set x509 issuer name";
+       LOG_SAFE(ERROR) << "Unable to set x509 issuer name";
         return nullptr;
     }
 
@@ -96,7 +96,7 @@ bssl::UniquePtr<X509> GenerateX509Certificate(EVP_PKEY* pkey) {
 
     int bytes = X509_sign(x509.get(), pkey, EVP_sha256());
     if (bytes <= 0) {
-        LOG(ERROR) << "Unable to sign x509 certificate";
+       LOG_SAFE(ERROR) << "Unable to sign x509 certificate";
         return nullptr;
     }
 
@@ -107,14 +107,14 @@ std::string X509ToPEMString(X509* x509) {
     bssl::UniquePtr<BIO> bio(BIO_new(BIO_s_mem()));
     int rc = PEM_write_bio_X509(bio.get(), x509);
     if (rc != 1) {
-        LOG(ERROR) << "PEM_write_bio_X509 failed";
+       LOG_SAFE(ERROR) << "PEM_write_bio_X509 failed";
         return "";
     }
 
     BUF_MEM* mem = nullptr;
     BIO_get_mem_ptr(bio.get(), &mem);
     if (!mem || !mem->data || !mem->length) {
-        LOG(ERROR) << "BIO_get_mem_ptr failed";
+       LOG_SAFE(ERROR) << "BIO_get_mem_ptr failed";
         return "";
     }
 
diff --git a/vendor/adb/daemon/abb.cpp b/vendor/adb/daemon/abb.cpp
index 57a3e31..98344c0 100644
--- a/vendor/adb/daemon/abb.cpp
+++ b/vendor/adb/daemon/abb.cpp
@@ -100,7 +100,7 @@ int main(int argc, char* const argv[]) {
         } else if (android::base::ConsumePrefix(&name, "abb_exec:")) {
             protocol = SubprocessProtocol::kNone;
         } else {
-            LOG(FATAL) << "Unknown command prefix for abb: " << data;
+           LOG_SAFE(FATAL) << "Unknown command prefix for abb: " << data;
         }
 
         unique_fd result = StartCommandInProcess(std::string(name), &execCmd, protocol);
diff --git a/vendor/adb/daemon/abb_service.cpp b/vendor/adb/daemon/abb_service.cpp
index 9725f5c..cf54a1d 100644
--- a/vendor/adb/daemon/abb_service.cpp
+++ b/vendor/adb/daemon/abb_service.cpp
@@ -46,7 +46,7 @@ unique_fd AbbProcess::sendCommand(std::string_view command) {
             socket_fd_ = startAbbProcess(&error_fd);
         }
         if (socket_fd_ == -1) {
-            LOG(ERROR) << "failed to start abb process";
+           LOG_SAFE(ERROR) << "failed to start abb process";
             return error_fd;
         }
 
@@ -67,7 +67,7 @@ unique_fd AbbProcess::sendCommand(std::string_view command) {
         return fd;
     }
 
-    LOG(ERROR) << "abb is unavailable";
+   LOG_SAFE(ERROR) << "abb is unavailable";
     socket_fd_.reset();
     return ReportError(kErrorProtocol, "abb is unavailable");
 }
diff --git a/vendor/adb/daemon/adb_wifi.cpp b/vendor/adb/daemon/adb_wifi.cpp
index 8e9838b..b2dfa42 100644
--- a/vendor/adb/daemon/adb_wifi.cpp
+++ b/vendor/adb/daemon/adb_wifi.cpp
@@ -42,7 +42,7 @@ static void adb_disconnected(void* unused, atransport* t);
 static struct adisconnect adb_disconnect = {adb_disconnected, nullptr};
 
 static void adb_disconnected(void* unused, atransport* t) {
-    LOG(INFO) << "ADB wifi device disconnected";
+   LOG_SAFE(INFO) << "ADB wifi device disconnected";
     CHECK(t->auth_id.has_value());
     adbd_auth_tls_device_disconnected(auth_ctx, kAdbTransportTypeWifi, t->auth_id.value());
 }
@@ -88,23 +88,23 @@ bool TlsServer::Start() {
     std::string err;
     unique_fd fd(network_inaddr_any_server(port_, SOCK_STREAM, &err));
     if (fd.get() == -1) {
-        LOG(ERROR) << "Failed to start TLS server [" << err << "]";
+       LOG_SAFE(ERROR) << "Failed to start TLS server [" << err << "]";
         return false;
     }
     close_on_exec(fd.get());
     int port = socket_get_local_port(fd.get());
     if (port <= 0 || port > 65535) {
-        LOG(ERROR) << "Invalid port for tls server";
+       LOG_SAFE(ERROR) << "Invalid port for tls server";
         return false;
     }
     port_ = static_cast<uint16_t>(port);
-    LOG(INFO) << "adbwifi started on port " << port_;
+   LOG_SAFE(INFO) << "adbwifi started on port " << port_;
 
     std::unique_lock<std::mutex> lock(mutex);
     fdevent_run_on_looper([&]() {
         fd_event_ = fdevent_create(fd.release(), &TlsServer::StaticOnFdEvent, this);
         if (fd_event_ == nullptr) {
-            LOG(ERROR) << "Failed to create fd event for TlsServer.";
+           LOG_SAFE(ERROR) << "Failed to create fd event for TlsServer.";
             callback(false);
             return;
         }
@@ -113,11 +113,11 @@ bool TlsServer::Start() {
 
     cv.wait(lock, [&]() { return success.has_value(); });
     if (!*success) {
-        LOG(INFO) << "TlsServer fdevent_create failed";
+       LOG_SAFE(INFO) << "TlsServer fdevent_create failed";
         return false;
     }
     fdevent_set(fd_event_, FDE_READ);
-    LOG(INFO) << "TlsServer running on port " << port_;
+   LOG_SAFE(INFO) << "TlsServer running on port " << port_;
 
     return *success;
 }
@@ -130,13 +130,13 @@ void TlsServer::StaticOnFdEvent(int fd, unsigned ev, void* opaque) {
 
 void TlsServer::OnFdEvent(int fd, unsigned ev) {
     if ((ev & FDE_READ) == 0 || fd != fd_event_->fd.get()) {
-        LOG(INFO) << __func__ << ": No read [ev=" << ev << " fd=" << fd << "]";
+       LOG_SAFE(INFO) << __func__ << ": No read [ev=" << ev << " fd=" << fd << "]";
         return;
     }
 
     unique_fd new_fd(adb_socket_accept(fd, nullptr, nullptr));
     if (new_fd >= 0) {
-        LOG(INFO) << "New TLS connection [fd=" << new_fd.get() << "]";
+       LOG_SAFE(INFO) << "New TLS connection [fd=" << new_fd.get() << "]";
         close_on_exec(new_fd.get());
         disable_tcp_nagle(new_fd.get());
         std::string serial = android::base::StringPrintf("host-%d", new_fd.get());
@@ -152,16 +152,16 @@ const char kWifiEnabledProp[] = "persist.adb.tls_server.enable";
 
 // Pre API 37 control ADB Wifi TLSServer by toggling kWifiEnabledProp property
 static void start_wifi_enabled_observer() {
-    LOG(INFO) << "wifi_init: Starting property observer thread";
+   LOG_SAFE(INFO) << "wifi_init: Starting property observer thread";
     std::thread([]() {
         bool wifi_enabled = false;
         while (true) {
             std::string toggled_val = wifi_enabled ? "0" : "1";
-            LOG(INFO) << "Waiting for " << kWifiEnabledProp << "=" << toggled_val;
+           LOG_SAFE(INFO) << "Waiting for " << kWifiEnabledProp << "=" << toggled_val;
 
             if (WaitForProperty(kWifiEnabledProp, toggled_val)) {
                 wifi_enabled = !wifi_enabled;
-                LOG(INFO) << kWifiEnabledProp << " changed to " << toggled_val;
+               LOG_SAFE(INFO) << kWifiEnabledProp << " changed to " << toggled_val;
                 if (wifi_enabled) {
                     enable_wifi_debugging();
                 } else {
@@ -176,14 +176,14 @@ static void start_wifi_enabled_observer() {
 static void adbd_send_tls_server_port(uint16_t port) {
     if (com_android_adbdauth_flags_use_tls_lifecycle()) {
         if (__builtin_available(android 37, *)) {
-            LOG(INFO) << "Sending TLS port to framework via adbdauth'" << port << "'";
+           LOG_SAFE(INFO) << "Sending TLS port to framework via adbdauth'" << port << "'";
             adbd_auth_send_tls_server_port(auth_ctx, port);
             return;
         }
     }
 
     // Fallback to legacy system property
-    LOG(INFO) << "Sending TLS port to framework via system property '" << port << "'";
+   LOG_SAFE(INFO) << "Sending TLS port to framework via system property '" << port << "'";
     SetProperty("service.adb.tls.port", std::to_string(port));
 }
 
@@ -191,13 +191,13 @@ void enable_wifi_debugging() {
     if (sTlsServer != nullptr) {
         // The TLS server is already up. This is a noop. But the Framework may still expect to get
         // sent the tls port.
-        LOG(INFO) << "enable_wifi_debugging noop, still sending port=" << sTlsServer->port();
+       LOG_SAFE(INFO) << "enable_wifi_debugging noop, still sending port=" << sTlsServer->port();
         adbd_send_tls_server_port(sTlsServer->port());
         return;
     }
     sTlsServer = new TlsServer(0);
     if (!sTlsServer->Start()) {
-        LOG(ERROR) << "Failed to start TlsServer";
+       LOG_SAFE(ERROR) << "Failed to start TlsServer";
         delete sTlsServer;
         sTlsServer = nullptr;
         return;
@@ -205,7 +205,7 @@ void enable_wifi_debugging() {
 
     // Start mdns connect service for discovery
     register_adb_tls_service(sTlsServer->port());
-    LOG(INFO) << "adb wifi started on port " << sTlsServer->port();
+   LOG_SAFE(INFO) << "adb wifi started on port " << sTlsServer->port();
     adbd_send_tls_server_port(sTlsServer->port());
 }
 
@@ -218,7 +218,7 @@ void disable_wifi_debugging() {
     unregister_adb_tls_service();
 
     kick_all_tcp_tls_transports();
-    LOG(INFO) << "adb wifi stopped";
+   LOG_SAFE(INFO) << "adb wifi stopped";
     adbd_send_tls_server_port(0);
 }
 
@@ -226,7 +226,7 @@ void adbd_wifi_init(AdbdAuthContext* ctx) {
     auth_ctx = ctx;
     if (com_android_adbdauth_flags_use_tls_lifecycle()) {
         if (__builtin_available(android 37, *)) {
-            LOG(INFO) << "wifi_init: Expecting lifecycle message over adbdauth";
+           LOG_SAFE(INFO) << "wifi_init: Expecting lifecycle message over adbdauth";
             return;
         }
     }
@@ -239,7 +239,7 @@ void adbd_wifi_secure_connect(atransport* t) {
     t->AddDisconnect(&adb_disconnect);
     handle_online(t);
     send_connect(t);
-    LOG(INFO) << __func__ << ": connected " << t->name;
+   LOG_SAFE(INFO) << __func__ << ": connected " << t->name;
     t->auth_id = adbd_auth_tls_device_connected(auth_ctx, kAdbTransportTypeWifi, t->auth_key.data(),
                                                 t->auth_key.size());
 }
diff --git a/vendor/adb/daemon/auth.cpp b/vendor/adb/daemon/auth.cpp
index a80438e..d5f9fb5 100644
--- a/vendor/adb/daemon/auth.cpp
+++ b/vendor/adb/daemon/auth.cpp
@@ -76,19 +76,19 @@ static void* transport_to_callback_arg(atransport* transport) {
 static atransport* transport_from_callback_arg(void* id) {
     uint64_t id_u64 = reinterpret_cast<uint64_t>(id);
     if (id_u64 > std::numeric_limits<uint32_t>::max()) {
-        LOG(FATAL) << "transport_from_callback_arg called on out of range value: " << id_u64;
+       LOG_SAFE(FATAL) << "transport_from_callback_arg called on out of range value: " << id_u64;
     }
 
     uint32_t id_u32 = static_cast<uint32_t>(id_u64);
     auto it = transports->find(id_u32);
     if (it == transports->end()) {
-        LOG(ERROR) << "transport_from_callback_arg failed to find transport for id " << id_u32;
+       LOG_SAFE(ERROR) << "transport_from_callback_arg failed to find transport for id " << id_u32;
         return nullptr;
     }
 
     atransport* t = it->second.get();
     if (!t) {
-        LOG(WARNING) << "transport_from_callback_arg found already destructed transport";
+       LOG_SAFE(WARNING) << "transport_from_callback_arg found already destructed transport";
         return nullptr;
     }
 
@@ -118,13 +118,13 @@ bssl::UniquePtr<STACK_OF(X509_NAME)> adbd_tls_client_ca_list() {
         uint8_t keybuf[ANDROID_PUBKEY_ENCODED_SIZE + 1];
         const std::string& pubkey = split[0];
         if (b64_pton(pubkey.c_str(), keybuf, sizeof(keybuf)) != ANDROID_PUBKEY_ENCODED_SIZE) {
-            LOG(ERROR) << "Invalid base64 key " << pubkey;
+           LOG_SAFE(ERROR) << "Invalid base64 key " << pubkey;
             return true;
         }
 
         RSA* key = nullptr;
         if (!android_pubkey_decode(keybuf, ANDROID_PUBKEY_ENCODED_SIZE, &key)) {
-            LOG(ERROR) << "Failed to parse key " << pubkey;
+           LOG_SAFE(ERROR) << "Failed to parse key " << pubkey;
             return true;
         }
         bssl::UniquePtr<RSA> rsa_key(key);
@@ -132,7 +132,7 @@ bssl::UniquePtr<STACK_OF(X509_NAME)> adbd_tls_client_ca_list() {
         unsigned char* dkey = nullptr;
         int len = i2d_RSA_PUBKEY(rsa_key.get(), &dkey);
         if (len <= 0 || dkey == nullptr) {
-            LOG(ERROR) << "Failed to encode RSA public key";
+           LOG_SAFE(ERROR) << "Failed to encode RSA public key";
             return true;
         }
 
@@ -145,7 +145,7 @@ bssl::UniquePtr<STACK_OF(X509_NAME)> adbd_tls_client_ca_list() {
 
         auto digest_str = SHA256BitsToHexString(
                 std::string_view(reinterpret_cast<const char*>(&digest[0]), sizeof(digest)));
-        LOG(INFO) << "fingerprint=[" << digest_str << "]";
+       LOG_SAFE(INFO) << "fingerprint=[" << digest_str << "]";
         auto issuer = CreateCAIssuerFromEncodedKey(digest_str);
         CHECK(bssl::PushToStack(ca_list.get(), std::move(issuer)));
         return true;
@@ -165,13 +165,13 @@ bool adbd_auth_verify(const char* token, size_t token_size, const std::string& s
         uint8_t keybuf[ANDROID_PUBKEY_ENCODED_SIZE + 1];
         const std::string& pubkey = split[0];
         if (b64_pton(pubkey.c_str(), keybuf, sizeof(keybuf)) != ANDROID_PUBKEY_ENCODED_SIZE) {
-            LOG(ERROR) << "Invalid base64 key " << pubkey;
+           LOG_SAFE(ERROR) << "Invalid base64 key " << pubkey;
             return true;
         }
 
         RSA* key = nullptr;
         if (!android_pubkey_decode(keybuf, ANDROID_PUBKEY_ENCODED_SIZE, &key)) {
-            LOG(ERROR) << "Failed to parse key " << pubkey;
+           LOG_SAFE(ERROR) << "Failed to parse key " << pubkey;
             return true;
         }
 
@@ -209,17 +209,17 @@ void adbd_cloexec_auth_socket() {
 }
 
 static void adbd_auth_key_authorized(void* arg, uint64_t id) {
-    LOG(INFO) << "adb client " << id << " authorized";
+   LOG_SAFE(INFO) << "adb client " << id << " authorized";
     fdevent_run_on_looper([=]() {
         auto* transport = transport_from_callback_arg(arg);
         if (!transport) {
-            LOG(ERROR) << "authorization received for deleted transport (" << id << "), ignoring";
+           LOG_SAFE(ERROR) << "authorization received for deleted transport (" << id << "), ignoring";
             return;
         }
 
         if (transport->auth_id.has_value()) {
             if (transport->auth_id.value() != id) {
-                LOG(ERROR)
+               LOG_SAFE(ERROR)
                         << "authorization received, but auth id doesn't match, ignoring (expected "
                         << transport->auth_id.value() << ", got " << id << ")";
                 return;
@@ -244,7 +244,7 @@ static void adbd_key_removed(const char* public_key, size_t len) {
 void adbd_auth_init() {
     auto adbd_auth_version = adbd_auth_get_max_version();
     auto max_handled_version = 3u;
-    LOG(INFO) << std::format("adbd_auth detected v={} (max_handled={})", adbd_auth_version,
+   LOG_SAFE(INFO) << std::format("adbd_auth detected v={} (max_handled={})", adbd_auth_version,
                              max_handled_version);
     if (adbd_auth_version >= max_handled_version) {
         AdbdAuthCallbacksV3 cb{};
@@ -274,7 +274,7 @@ void adbd_auth_init() {
     std::thread([]() {
         adb_thread_setname("adbd auth");
         adbd_auth_run(auth_ctx);
-        LOG(FATAL) << "auth thread terminated";
+       LOG_SAFE(FATAL) << "auth thread terminated";
     }).detach();
 
     adbd_wifi_init(auth_ctx);
@@ -350,13 +350,13 @@ int adbd_tls_verify_cert(X509_STORE_CTX* ctx, std::string* auth_key) {
         uint8_t keybuf[ANDROID_PUBKEY_ENCODED_SIZE + 1];
         const std::string& pubkey = split[0];
         if (b64_pton(pubkey.c_str(), keybuf, sizeof(keybuf)) != ANDROID_PUBKEY_ENCODED_SIZE) {
-            LOG(ERROR) << "Invalid base64 key " << pubkey;
+           LOG_SAFE(ERROR) << "Invalid base64 key " << pubkey;
             return true;
         }
 
         RSA* key = nullptr;
         if (!android_pubkey_decode(keybuf, ANDROID_PUBKEY_ENCODED_SIZE, &key)) {
-            LOG(ERROR) << "Failed to parse key " << pubkey;
+           LOG_SAFE(ERROR) << "Failed to parse key " << pubkey;
             return true;
         }
 
diff --git a/vendor/adb/daemon/file_sync_service.cpp b/vendor/adb/daemon/file_sync_service.cpp
index 8dc1407..9214578 100644
--- a/vendor/adb/daemon/file_sync_service.cpp
+++ b/vendor/adb/daemon/file_sync_service.cpp
@@ -302,7 +302,7 @@ static bool handle_send_file_data(borrowed_fd s, unique_fd fd, uint32_t* timesta
             break;
 
         case CompressionType::Any:
-            LOG(FATAL) << "unexpected CompressionType::Any";
+           LOG_SAFE(FATAL) << "unexpected CompressionType::Any";
     }
 
     while (true) {
@@ -343,7 +343,7 @@ static bool handle_send_file_data(borrowed_fd s, unique_fd fd, uint32_t* timesta
             } else if (result == DecodeResult::Done) {
                 return true;
             } else {
-                LOG(FATAL) << "invalid DecodeResult: " << static_cast<int>(result);
+               LOG_SAFE(FATAL) << "invalid DecodeResult: " << static_cast<int>(result);
             }
         }
     }
@@ -583,7 +583,7 @@ static bool do_send_v2(int s, const std::string& path, std::vector<char>& buffer
     syncmsg msg;
     int rc = ReadFdExactly(s, &msg.send_v2_setup, sizeof(msg.send_v2_setup));
     if (rc == 0) {
-        LOG(ERROR) << "failed to read send_v2 setup packet: EOF";
+       LOG_SAFE(ERROR) << "failed to read send_v2 setup packet: EOF";
         return false;
     } else if (rc < 0) {
         PLOG(ERROR) << "failed to read send_v2 setup packet";
@@ -672,7 +672,7 @@ static bool recv_impl(borrowed_fd s, const char* path, CompressionType compressi
             break;
 
         case CompressionType::Any:
-            LOG(FATAL) << "unexpected CompressionType::Any";
+           LOG_SAFE(FATAL) << "unexpected CompressionType::Any";
     }
 
     bool sending = true;
@@ -732,7 +732,7 @@ static bool do_recv_v2(borrowed_fd s, const char* path, std::vector<char>& buffe
     // Read the setup packet.
     int rc = ReadFdExactly(s, &msg.recv_v2_setup, sizeof(msg.recv_v2_setup));
     if (rc == 0) {
-        LOG(ERROR) << "failed to read recv_v2 setup packet: EOF";
+       LOG_SAFE(ERROR) << "failed to read recv_v2 setup packet: EOF";
         return false;
     } else if (rc < 0) {
         PLOG(ERROR) << "failed to read recv_v2 setup packet";
diff --git a/vendor/adb/daemon/jdwp_service.cpp b/vendor/adb/daemon/jdwp_service.cpp
index 9070c39..9ca8e28 100644
--- a/vendor/adb/daemon/jdwp_service.cpp
+++ b/vendor/adb/daemon/jdwp_service.cpp
@@ -157,7 +157,7 @@ struct JdwpProcess {
         fdevent_set(this->fde, FDE_READ);
 
         if (!this->fde) {
-            LOG(FATAL) << "could not create fdevent for new JDWP process";
+           LOG_SAFE(FATAL) << "could not create fdevent for new JDWP process";
         }
     }
 
@@ -248,7 +248,7 @@ static size_t process_list_msg(TrackerKind kind, char* buffer, size_t bufferlen)
     // Message is length-prefixed with 4 hex digits in ASCII.
     static constexpr size_t header_len = 4;
     if (bufferlen < header_len) {
-        LOG(FATAL) << "invalid JDWP process list buffer size: " << bufferlen;
+       LOG_SAFE(FATAL) << "invalid JDWP process list buffer size: " << bufferlen;
     }
 
     char head[header_len + 1];
@@ -331,14 +331,14 @@ static unique_fd send_socket_to_process(JdwpProcess& proc) {
     // out of the freezer. Fail instead.
     if (is_process_in_freezer(proc.process)) {
         ProcessInfo& info = proc.process;
-        LOG(WARNING) << std::format("Process {} ({}) is frozen. Denying JDWP connection", info.pid,
+       LOG_SAFE(WARNING) << std::format("Process {} ({}) is frozen. Denying JDWP connection", info.pid,
                                     info.process_name);
         return unique_fd{};
     }
 
     int fds[2];
     if (adb_socketpair(fds) < 0) {
-        LOG(WARNING) << std::format("{}: socket pair creation failed: {}", __FUNCTION__,
+       LOG_SAFE(WARNING) << std::format("{}: socket pair creation failed: {}", __FUNCTION__,
                                     strerror(errno));
         return unique_fd{};
     }
@@ -367,7 +367,7 @@ unique_fd create_jdwp_connection_fd(pid_t pid) {
 
         return send_socket_to_process(*proc);
     }
-    LOG(WARNING) << std::format("search for pid {} failed !!", pid);
+   LOG_SAFE(WARNING) << std::format("search for pid {} failed !!", pid);
     return unique_fd{};
 }
 
@@ -423,7 +423,7 @@ asocket* create_jdwp_service_socket() {
     JdwpSocket* s = new JdwpSocket();
 
     if (!s) {
-        LOG(FATAL) << "failed to allocate JdwpSocket";
+       LOG_SAFE(FATAL) << "failed to allocate JdwpSocket";
     }
 
     install_local_socket(s);
@@ -518,7 +518,7 @@ static int jdwp_tracker_enqueue(asocket* s, apacket::payload_type) {
 static asocket* create_process_tracker_service_socket(TrackerKind kind) {
     std::unique_ptr<JdwpTracker> t = std::make_unique<JdwpTracker>(kind, true);
     if (!t) {
-        LOG(FATAL) << "failed to allocate JdwpTracker";
+       LOG_SAFE(FATAL) << "failed to allocate JdwpTracker";
     }
 
     /* Object layout (with an inheritance hierarchy) varies across arch (e.g
@@ -553,12 +553,12 @@ int init_jdwp() {
     std::thread([]() {
         adb_thread_setname("jdwp control");
         adbconnection_listen([](int fd, ProcessInfo process) {
-            LOG(INFO) << "jdwp connection from " << process.pid;
+           LOG_SAFE(INFO) << "jdwp connection from " << process.pid;
             fdevent_run_on_looper([fd, process] {
                 unique_fd ufd(fd);
                 auto proc = std::make_unique<JdwpProcess>(std::move(ufd), process);
                 if (!proc) {
-                    LOG(FATAL) << "failed to allocate JdwpProcess";
+                   LOG_SAFE(FATAL) << "failed to allocate JdwpProcess";
                 }
                 _jdwp_list.emplace_back(std::move(proc));
                 if (process.debuggable) jdwp_process_list_updated();
diff --git a/vendor/adb/daemon/logging.h b/vendor/adb/daemon/logging.h
index 3e28bef..cd2dfff 100644
--- a/vendor/adb/daemon/logging.h
+++ b/vendor/adb/daemon/logging.h
@@ -28,6 +28,6 @@ enum class LogType {
 
 bool is_logging_enabled(LogType type);
 
-#define ADB_LOG(type) ::adb::is_logging_enabled(::adb::LogType::type) && LOG(INFO)
+#define ADB_LOG(type) ::adb::is_logging_enabled(::adb::LogType::type) &&LOG_SAFE(INFO)
 
 }  // namespace adb
diff --git a/vendor/adb/daemon/main.cpp b/vendor/adb/daemon/main.cpp
index 3d3f302..eaaa877 100644
--- a/vendor/adb/daemon/main.cpp
+++ b/vendor/adb/daemon/main.cpp
@@ -178,7 +178,7 @@ static void drop_privileges() {
                 // If we failed to become root, don't try again to avoid a
                 // restart loop.
                 android::base::SetProperty("service.adb.root", "0");
-                LOG(FATAL) << "Could not set SELinux context";
+               LOG_SAFE(FATAL) << "Could not set SELinux context";
             }
         }
     }
@@ -205,7 +205,7 @@ static void setup_adb(const std::vector<std::string>& addrs) {
     register_adb_tcp_service(port);
 #endif
     for (const auto& addr : addrs) {
-        LOG(INFO) << "adbd listening on " << addr;
+       LOG_SAFE(INFO) << "adbd listening on " << addr;
         init_transport_socket_server(addr);
     }
 }
@@ -305,7 +305,7 @@ int adbd_main() {
         setup_adb(addrs);
     }
 
-    LOG(INFO) << "adbd started";
+   LOG_SAFE(INFO) << "adbd started";
 
     D("adbd_main(): pre init_jdwp()");
     init_jdwp();
@@ -356,7 +356,7 @@ int main(int argc, char** argv) {
                        ADB_VERSION_MINOR, ADB_SERVER_VERSION);
                 return 0;
             case 'l':
-                LOG(ERROR) << "post-fs-data triggered";
+               LOG_SAFE(ERROR) << "post-fs-data triggered";
                 return 0;
             default:
                 // getopt already prints "adbd: invalid option -- %c" for us.
diff --git a/vendor/adb/daemon/mdns.cpp b/vendor/adb/daemon/mdns.cpp
index de0ba2f..499fd43 100644
--- a/vendor/adb/daemon/mdns.cpp
+++ b/vendor/adb/daemon/mdns.cpp
@@ -115,7 +115,7 @@ static void mdns_callback(DNSServiceRef /*ref*/,
                           const char* /*domain*/,
                           void* /*context*/) {
     if (errorCode != kDNSServiceErr_NoError) {
-        LOG(ERROR) << "Encountered mDNS registration error (" << errorCode << ").";
+       LOG_SAFE(ERROR) << "Encountered mDNS registration error (" << errorCode << ").";
     }
 }
 
@@ -130,7 +130,7 @@ static std::vector<char> buildTxtRecord() {
     for (auto const& [key, val] : attributes) {
         size_t length = key.size() + val.size() + 1;
         if (length > 255) {
-            LOG(INFO) << "DNS TXT Record property " << key << "='" << val << "' is too large.";
+           LOG_SAFE(INFO) << "DNS TXT Record property " << key << "='" << val << "' is too large.";
             continue;
         }
         record.emplace_back(length);
@@ -144,7 +144,7 @@ static std::vector<char> buildTxtRecord() {
 
 static void register_mdns_service(int index, int port, const std::string& service_name) {
     if (mdns_refs[index] != nullptr) {
-        LOG(ERROR) << "Unable to register mDNS service " << service_name << " (slot occupied)";
+       LOG_SAFE(ERROR) << "Unable to register mDNS service " << service_name << " (slot occupied)";
         return;
     }
 
@@ -155,7 +155,7 @@ static void register_mdns_service(int index, int port, const std::string& servic
             txtRecord.empty() ? nullptr : txtRecord.data(), mdns_callback, nullptr);
 
     if (error != kDNSServiceErr_NoError) {
-        LOG(ERROR) << "Could not register mDNS service " << kADBDNSServices[index] << ", error ("
+       LOG_SAFE(ERROR) << "Could not register mDNS service " << kADBDNSServices[index] << ", error ("
                    << error << ").";
     } else {
         VLOG(MDNS) << "adbd mDNS service " << kADBDNSServices[index] << " registered";
@@ -222,7 +222,7 @@ class MdnsWorkerThread {
         android::base::SetProperty("ctl.start", "mdnsd");
 
         if (!android::base::WaitForProperty("init.svc.mdnsd", "running", 5s)) {
-            LOG(ERROR) << "Could not start mdnsd.";
+           LOG_SAFE(ERROR) << "Could not start mdnsd.";
         }
 #endif
     }
@@ -272,13 +272,13 @@ void register_adb_tcp_service(int tcp_port) {
 void register_adb_tls_service(int tls_port) {
     auto service_name = ReadDeviceGuid();
     if (service_name.empty()) {
-        LOG(WARNING) << "Unable to register TLS: DeviceGUID is empty";
+       LOG_SAFE(WARNING) << "Unable to register TLS: DeviceGUID is empty";
         return;
     }
 
     if (com_android_adbdauth_flags_use_tls_lifecycle()) {
         if (__builtin_available(android 37, *)) {
-            LOG(INFO) << "Registering TLS with framework '" << service_name << "'";
+           LOG_SAFE(INFO) << "Registering TLS with framework '" << service_name << "'";
             adbd_auth_register_service(auth_ctx, service_name.c_str(),
                                        kADBDNSServices[kADBSecureConnectServiceRefIndex], tls_port);
             return;
@@ -295,7 +295,7 @@ void register_adb_tls_service(int tls_port) {
 void unregister_adb_tls_service() {
     auto service_name = ReadDeviceGuid();
     if (service_name.empty()) {
-        LOG(WARNING) << "Unable to unregister TLS: DeviceGUID is empty";
+       LOG_SAFE(WARNING) << "Unable to unregister TLS: DeviceGUID is empty";
         return;
     }
 
@@ -321,12 +321,12 @@ void on_framework_connected() {
         MdnsWorkerThread::Get().AddTask([] {
             auto service_name = ReadDeviceGuid();
             if (service_name.empty()) {
-                LOG(WARNING) << "Unable to register TCP: DeviceGUID is empty";
+               LOG_SAFE(WARNING) << "Unable to register TCP: DeviceGUID is empty";
                 return;
             }
 
             if (__builtin_available(android 37, *)) {
-                LOG(INFO) << "Registering TCP with framework '" << service_name << "'";
+               LOG_SAFE(INFO) << "Registering TCP with framework '" << service_name << "'";
                 adbd_auth_register_service(auth_ctx, service_name.c_str(),
                                            kADBDNSServices[kADBTransportServiceRefIndex],
                                            adb_wifi_tcp_port);
diff --git a/vendor/adb/daemon/property_monitor_test.cpp b/vendor/adb/daemon/property_monitor_test.cpp
index 13cbc29..b23ec2c 100644
--- a/vendor/adb/daemon/property_monitor_test.cpp
+++ b/vendor/adb/daemon/property_monitor_test.cpp
@@ -65,7 +65,7 @@ static void RegisterCallback(PropertyMonitor* pm, PropertyChanges* output,
                              std::string property_name) {
     pm->Add(property_name, [output, property_name](std::string value) {
         std::lock_guard<std::mutex> lock(output->mutex);
-        LOG(INFO) << property_name << " = " << value;
+       LOG_SAFE(INFO) << property_name << " = " << value;
         output->changes[property_name].emplace_back(std::move(value));
         return true;
     });
diff --git a/vendor/adb/daemon/restart_service.cpp b/vendor/adb/daemon/restart_service.cpp
index 16d2627..2d4bb5e 100644
--- a/vendor/adb/daemon/restart_service.cpp
+++ b/vendor/adb/daemon/restart_service.cpp
@@ -38,7 +38,7 @@ void restart_root_service(unique_fd fd) {
         return;
     }
 
-    LOG(INFO) << "adbd restarting as root";
+   LOG_SAFE(INFO) << "adbd restarting as root";
     android::base::SetProperty("service.adb.root", "1");
     WriteFdExactly(fd.get(), "restarting adbd as root\n");
 }
@@ -49,7 +49,7 @@ void restart_unroot_service(unique_fd fd) {
         return;
     }
 
-    LOG(INFO) << "adbd restarting as nonroot";
+   LOG_SAFE(INFO) << "adbd restarting as nonroot";
     android::base::SetProperty("service.adb.root", "0");
     WriteFdExactly(fd.get(), "restarting adbd as non root\n");
 }
@@ -60,13 +60,13 @@ void restart_tcp_service(unique_fd fd, int port) {
         return;
     }
 
-    LOG(INFO) << "adbd restarting in TCP mode (port = " << port << ")";
+   LOG_SAFE(INFO) << "adbd restarting in TCP mode (port = " << port << ")";
     android::base::SetProperty("service.adb.tcp.port", android::base::StringPrintf("%d", port));
     WriteFdFmt(fd.get(), "restarting in TCP mode port: %d\n", port);
 }
 
 void restart_usb_service(unique_fd fd) {
-    LOG(INFO) << "adbd restarting in USB mode";
+   LOG_SAFE(INFO) << "adbd restarting in USB mode";
     android::base::SetProperty("service.adb.tcp.port", "0");
     WriteFdExactly(fd.get(), "restarting in USB mode\n");
 }
diff --git a/vendor/adb/daemon/services.cpp b/vendor/adb/daemon/services.cpp
index 75e8dcb..f583248 100644
--- a/vendor/adb/daemon/services.cpp
+++ b/vendor/adb/daemon/services.cpp
@@ -88,7 +88,7 @@ unique_fd reverse_service(std::string_view command, atransport* transport) {
 unique_fd ShellService(std::string_view args, const atransport* transport) {
     size_t delimiter_index = args.find(':');
     if (delimiter_index == std::string::npos) {
-        LOG(ERROR) << "No ':' found in shell service arguments: " << args;
+       LOG_SAFE(ERROR) << "No ':' found in shell service arguments: " << args;
         return unique_fd{};
     }
 
@@ -115,7 +115,7 @@ unique_fd ShellService(std::string_view args, const atransport* transport) {
             terminal_type = arg.substr(strlen("TERM="));
         } else if (!arg.empty()) {
             // This is not an error to allow for future expansion.
-            LOG(WARNING) << "Ignoring unknown shell service argument: " << arg;
+           LOG_SAFE(WARNING) << "Ignoring unknown shell service argument: " << arg;
         }
     }
 
@@ -204,10 +204,10 @@ struct ServiceSocket : public asocket {
 struct SinkSocket : public ServiceSocket {
     explicit SinkSocket(atransport* transport, size_t byte_count)
         : ServiceSocket(transport), bytes_left_(byte_count) {
-        LOG(INFO) << "Creating new SinkSocket with capacity " << byte_count;
+       LOG_SAFE(INFO) << "Creating new SinkSocket with capacity " << byte_count;
     }
 
-    virtual ~SinkSocket() { LOG(INFO) << "SinkSocket destroyed"; }
+    virtual ~SinkSocket() {LOG_SAFE(INFO) << "SinkSocket destroyed"; }
 
     virtual int Enqueue(apacket::payload_type data) override final {
         if (bytes_left_ <= data.size()) {
@@ -226,10 +226,10 @@ struct SinkSocket : public ServiceSocket {
 struct SourceSocket : public ServiceSocket {
     explicit SourceSocket(atransport* transport, size_t byte_count)
         : ServiceSocket(transport), bytes_left_(byte_count) {
-        LOG(INFO) << "Creating new SourceSocket with capacity " << byte_count;
+       LOG_SAFE(INFO) << "Creating new SourceSocket with capacity " << byte_count;
     }
 
-    virtual ~SourceSocket() { LOG(INFO) << "SourceSocket destroyed"; }
+    virtual ~SourceSocket() {LOG_SAFE(INFO) << "SourceSocket destroyed"; }
 
     void Ready() {
         size_t len = std::min(bytes_left_, get_max_payload());
@@ -281,7 +281,7 @@ unique_fd daemon_service_to_fd(std::string_view name, atransport* transport) {
     }
 
 #if defined(__ANDROID__)
-    LOG(INFO) << "adbd service requested '" << name << "'";
+   LOG_SAFE(INFO) << "adbd service requested '" << name << "'";
 #endif
 
 #if defined(__ANDROID__) && !defined(__ANDROID_RECOVERY__)
diff --git a/vendor/adb/daemon/shell_service.cpp b/vendor/adb/daemon/shell_service.cpp
index 0b71b03..0e62f48 100644
--- a/vendor/adb/daemon/shell_service.cpp
+++ b/vendor/adb/daemon/shell_service.cpp
@@ -377,12 +377,12 @@ bool Subprocess::ForkAndExec(std::string* error) {
         if (getcon(&con) == 0) {
             if (!strcmp(con, "u:r:adbd:s0")) {
                 if (selinux_android_setcon("u:r:shell:s0") < 0) {
-                    LOG(FATAL) << "Could not set SELinux context for subprocess";
+                   LOG_SAFE(FATAL) << "Could not set SELinux context for subprocess";
                 }
             }
             freecon(con);
         } else {
-            LOG(FATAL) << "Failed to get SELinux context";
+           LOG_SAFE(FATAL) << "Failed to get SELinux context";
         }
 #endif
 
@@ -517,7 +517,7 @@ int Subprocess::OpenPtyChildFd(const char* pts_name, unique_fd* error_sfd) {
         for (const char* message : messages) {
             WriteFdExactly(*error_sfd, message);
         }
-        LOG(FATAL) << "child failed to open pts " << pts_name << ": " << strerror(saved_errno);
+       LOG_SAFE(FATAL) << "child failed to open pts " << pts_name << ": " << strerror(saved_errno);
     }
 
     if (make_pty_raw_) {
@@ -526,7 +526,7 @@ int Subprocess::OpenPtyChildFd(const char* pts_name, unique_fd* error_sfd) {
             int saved_errno = errno;
             WriteFdExactly(*error_sfd, "tcgetattr failed: ");
             WriteFdExactly(*error_sfd, strerror(saved_errno));
-            LOG(FATAL) << "tcgetattr() failed: " << strerror(saved_errno);
+           LOG_SAFE(FATAL) << "tcgetattr() failed: " << strerror(saved_errno);
         }
 
         cfmakeraw(&tattr);
@@ -534,7 +534,7 @@ int Subprocess::OpenPtyChildFd(const char* pts_name, unique_fd* error_sfd) {
             int saved_errno = errno;
             WriteFdExactly(*error_sfd, "tcsetattr failed: ");
             WriteFdExactly(*error_sfd, strerror(saved_errno));
-            LOG(FATAL) << "tcsetattr() failed: " << strerror(saved_errno);
+           LOG_SAFE(FATAL) << "tcsetattr() failed: " << strerror(saved_errno);
         }
     }
 
@@ -860,14 +860,14 @@ unique_fd StartSubprocess(std::string name, const char* terminal_type, Subproces
     auto subprocess = std::make_unique<Subprocess>(std::move(name), terminal_type, type, protocol,
                                                    make_pty_raw);
     if (!subprocess) {
-        LOG(ERROR) << "failed to allocate new subprocess";
+       LOG_SAFE(ERROR) << "failed to allocate new subprocess";
         *error_fd = ReportError(error_protocol, "failed to allocate new subprocess");
         return {};
     }
 
     std::string error;
     if (!subprocess->ForkAndExec(&error)) {
-        LOG(ERROR) << "failed to start subprocess: " << error;
+       LOG_SAFE(ERROR) << "failed to start subprocess: " << error;
         *error_fd = ReportError(error_protocol, error);
         return {};
     }
@@ -877,7 +877,7 @@ unique_fd StartSubprocess(std::string name, const char* terminal_type, Subproces
       subprocess->pid());
 
     if (!Subprocess::StartThread(std::move(subprocess), &error)) {
-        LOG(ERROR) << "failed to start subprocess management thread: " << error;
+       LOG_SAFE(ERROR) << "failed to start subprocess management thread: " << error;
         *error_fd = ReportError(error_protocol, error);
         return {};
     }
@@ -886,7 +886,7 @@ unique_fd StartSubprocess(std::string name, const char* terminal_type, Subproces
 }
 
 unique_fd StartCommandInProcess(std::string name, Command command, SubprocessProtocol protocol) {
-    LOG(INFO) << "StartCommandInProcess(" << dump_hex(name.data(), name.size()) << ")";
+   LOG_SAFE(INFO) << "StartCommandInProcess(" << dump_hex(name.data(), name.size()) << ")";
 
     constexpr auto terminal_type = "";
     constexpr auto type = SubprocessType::kRaw;
@@ -895,13 +895,13 @@ unique_fd StartCommandInProcess(std::string name, Command command, SubprocessPro
     auto subprocess = std::make_unique<Subprocess>(std::move(name), terminal_type, type, protocol,
                                                    make_pty_raw);
     if (!subprocess) {
-        LOG(ERROR) << "failed to allocate new subprocess";
+       LOG_SAFE(ERROR) << "failed to allocate new subprocess";
         return ReportError(protocol, "failed to allocate new subprocess");
     }
 
     std::string error;
     if (!subprocess->ExecInProcess(std::move(command), &error)) {
-        LOG(ERROR) << "failed to start subprocess: " << error;
+       LOG_SAFE(ERROR) << "failed to start subprocess: " << error;
         return ReportError(protocol, error);
     }
 
@@ -910,7 +910,7 @@ unique_fd StartCommandInProcess(std::string name, Command command, SubprocessPro
       subprocess->pid());
 
     if (!Subprocess::StartThread(std::move(subprocess), &error)) {
-        LOG(ERROR) << "failed to start inprocess management thread: " << error;
+       LOG_SAFE(ERROR) << "failed to start inprocess management thread: " << error;
         return ReportError(protocol, error);
     }
 
diff --git a/vendor/adb/daemon/usb.cpp b/vendor/adb/daemon/usb.cpp
index dedf9ac..e1e80c8 100644
--- a/vendor/adb/daemon/usb.cpp
+++ b/vendor/adb/daemon/usb.cpp
@@ -241,7 +241,7 @@ struct UsbFfsConnection : public Connection {
 
     virtual bool DoTlsHandshake(RSA* key, std::string* auth_key) override final {
         // TODO: support TLS for usb connections.
-        LOG(FATAL) << "Not supported yet.";
+       LOG_SAFE(FATAL) << "Not supported yet.";
         return false;
     }
 
@@ -278,7 +278,7 @@ struct UsbFfsConnection : public Connection {
                 if (rc == -1) {
                     PLOG(FATAL) << "poll on USB control fd failed";
                 } else if (rc == 0) {
-                    LOG(WARNING) << "timed out while waiting for FUNCTIONFS_BIND, trying again";
+                   LOG_SAFE(WARNING) << "timed out while waiting for FUNCTIONFS_BIND, trying again";
                     break;
                 }
 
@@ -292,10 +292,10 @@ struct UsbFfsConnection : public Connection {
                 if (rc == -1) {
                     PLOG(FATAL) << "failed to read functionfs event";
                 } else if (rc == 0) {
-                    LOG(WARNING) << "hit EOF on functionfs control fd";
+                   LOG_SAFE(WARNING) << "hit EOF on functionfs control fd";
                     break;
                 } else if (rc != sizeof(event)) {
-                    LOG(FATAL) << "read functionfs event of unexpected size, expected "
+                   LOG_SAFE(FATAL) << "read functionfs event of unexpected size, expected "
                                << sizeof(event) << ", got " << rc;
                 }
 
@@ -305,13 +305,13 @@ struct UsbFfsConnection : public Connection {
                 switch (event.type) {
                     case FUNCTIONFS_BIND:
                         if (bound) {
-                            LOG(WARNING) << "received FUNCTIONFS_BIND while already bound?";
+                           LOG_SAFE(WARNING) << "received FUNCTIONFS_BIND while already bound?";
                             running = false;
                             break;
                         }
 
                         if (enabled) {
-                            LOG(WARNING) << "received FUNCTIONFS_BIND while already enabled?";
+                           LOG_SAFE(WARNING) << "received FUNCTIONFS_BIND while already enabled?";
                             running = false;
                             break;
                         }
@@ -321,13 +321,13 @@ struct UsbFfsConnection : public Connection {
 
                     case FUNCTIONFS_ENABLE:
                         if (!bound) {
-                            LOG(WARNING) << "received FUNCTIONFS_ENABLE while not bound?";
+                           LOG_SAFE(WARNING) << "received FUNCTIONFS_ENABLE while not bound?";
                             running = false;
                             break;
                         }
 
                         if (enabled) {
-                            LOG(WARNING) << "received FUNCTIONFS_ENABLE while already enabled?";
+                           LOG_SAFE(WARNING) << "received FUNCTIONFS_ENABLE while already enabled?";
                             running = false;
                             break;
                         }
@@ -338,11 +338,11 @@ struct UsbFfsConnection : public Connection {
 
                     case FUNCTIONFS_DISABLE:
                         if (!bound) {
-                            LOG(WARNING) << "received FUNCTIONFS_DISABLE while not bound?";
+                           LOG_SAFE(WARNING) << "received FUNCTIONFS_DISABLE while not bound?";
                         }
 
                         if (!enabled) {
-                            LOG(WARNING) << "received FUNCTIONFS_DISABLE while not enabled?";
+                           LOG_SAFE(WARNING) << "received FUNCTIONFS_DISABLE while not enabled?";
                         }
 
                         enabled = false;
@@ -351,11 +351,11 @@ struct UsbFfsConnection : public Connection {
 
                     case FUNCTIONFS_UNBIND:
                         if (enabled) {
-                            LOG(WARNING) << "received FUNCTIONFS_UNBIND while still enabled?";
+                           LOG_SAFE(WARNING) << "received FUNCTIONFS_UNBIND while still enabled?";
                         }
 
                         if (!bound) {
-                            LOG(WARNING) << "received FUNCTIONFS_UNBIND when not bound?";
+                           LOG_SAFE(WARNING) << "received FUNCTIONFS_UNBIND when not bound?";
                         }
 
                         bound = false;
@@ -383,7 +383,7 @@ struct UsbFfsConnection : public Connection {
 
                             ssize_t rc = adb_read(control_fd_.get(), buf.data(), buf.size());
                             if (rc != event.u.setup.wLength) {
-                                LOG(ERROR)
+                               LOG_SAFE(ERROR)
                                         << "read " << rc
                                         << " bytes when trying to read control request, expected "
                                         << event.u.setup.wLength;
@@ -421,7 +421,7 @@ struct UsbFfsConnection : public Connection {
                 if (rc == -1) {
                     PLOG(FATAL) << "failed to read from eventfd";
                 } else if (rc == 0) {
-                    LOG(FATAL) << "hit EOF on eventfd";
+                   LOG_SAFE(FATAL) << "hit EOF on eventfd";
                 }
 
                 HandleEvents();
@@ -441,7 +441,7 @@ struct UsbFfsConnection : public Connection {
         while (true) {
             int rc = pthread_kill(worker_thread_handle, kInterruptionSignal);
             if (rc != 0) {
-                LOG(ERROR) << "failed to send interruption signal to worker: " << strerror(rc);
+               LOG_SAFE(ERROR) << "failed to send interruption signal to worker: " << strerror(rc);
                 break;
             }
 
@@ -453,7 +453,7 @@ struct UsbFfsConnection : public Connection {
             } else if (rc == ESRCH) {
                 break;
             } else {
-                LOG(ERROR) << "failed to send interruption signal to worker: " << strerror(rc);
+               LOG_SAFE(ERROR) << "failed to send interruption signal to worker: " << strerror(rc);
             }
         }
 
@@ -657,7 +657,7 @@ struct UsbFfsConnection : public Connection {
             HandleError(StringPrintf("failed to submit write requests: %s", strerror(errno)));
             return;
         } else if (rc != writes_to_submit) {
-            LOG(FATAL) << "failed to submit all writes: wanted to submit " << writes_to_submit
+           LOG_SAFE(FATAL) << "failed to submit all writes: wanted to submit " << writes_to_submit
                        << ", actually submitted " << rc;
         }
     }
diff --git a/vendor/adb/daemon/usb_ffs.cpp b/vendor/adb/daemon/usb_ffs.cpp
index e538ca8..fd0c5bd 100644
--- a/vendor/adb/daemon/usb_ffs.cpp
+++ b/vendor/adb/daemon/usb_ffs.cpp
@@ -272,7 +272,7 @@ bool open_functionfs(android::base::unique_fd* out_control, android::base::uniqu
     v2_descriptor.os_prop_values = os_prop_values;
 
     if (out_control->get() < 0) {  // might have already done this before
-        LOG(INFO) << "opening control endpoint " << USB_FFS_ADB_EP0;
+       LOG_SAFE(INFO) << "opening control endpoint " << USB_FFS_ADB_EP0;
         control.reset(adb_open(USB_FFS_ADB_EP0, O_RDWR));
         if (control < 0) {
             PLOG(ERROR) << "cannot open control endpoint " << USB_FFS_ADB_EP0;
diff --git a/vendor/adb/daemon/watchdog.cpp b/vendor/adb/daemon/watchdog.cpp
index 6e14297..005573b 100644
--- a/vendor/adb/daemon/watchdog.cpp
+++ b/vendor/adb/daemon/watchdog.cpp
@@ -47,7 +47,7 @@ static void UpdateWatchdog() REQUIRES(g_watchdog_mutex) {
     static std::once_flag once;
     std::call_once(once, []() {
         signal(SIGALRM, [](int) {
-            LOG(WARNING) << "adbd watchdog expired; rebooting...";
+           LOG_SAFE(WARNING) << "adbd watchdog expired; rebooting...";
             execl("/system/bin/reboot", "/system/bin/reboot", "bootloader", nullptr);
         });
     });
@@ -55,7 +55,7 @@ static void UpdateWatchdog() REQUIRES(g_watchdog_mutex) {
     static bool alarm_set = false;
 
     static auto Arm = []() {
-        LOG(INFO) << "adb watchdog armed, triggering in " << g_watchdog_timeout_seconds
+       LOG_SAFE(INFO) << "adb watchdog armed, triggering in " << g_watchdog_timeout_seconds
                   << " seconds";
         alarm(g_watchdog_timeout_seconds);
         alarm_set = true;
@@ -64,7 +64,7 @@ static void UpdateWatchdog() REQUIRES(g_watchdog_mutex) {
     static auto Disarm = []() {
         unsigned int previous = alarm(0);
         if (previous != 0) {
-            LOG(INFO) << "adb watchdog disarmed with " << previous << " seconds left";
+           LOG_SAFE(INFO) << "adb watchdog disarmed with " << previous << " seconds left";
         }
         alarm_set = false;
     };
@@ -104,7 +104,7 @@ void Stop() {
 void Initialize() {
     for (auto& property : {kAdbWatchdogProperty, kTestHarnessProperty}) {
         g_property_monitor.Add(property, [property](std::string value) {
-            LOG(INFO) << property << " set to '" << value << "'";
+           LOG_SAFE(INFO) << property << " set to '" << value << "'";
 
             std::lock_guard<std::mutex> lock(g_watchdog_mutex);
             UpdateWatchdog();
@@ -122,7 +122,7 @@ void Initialize() {
             }
         }
 
-        LOG(INFO) << "adb watchdog timeout set to " << g_watchdog_timeout_seconds << " seconds";
+       LOG_SAFE(INFO) << "adb watchdog timeout set to " << g_watchdog_timeout_seconds << " seconds";
         return true;
     });
 
diff --git a/vendor/adb/fdevent/fdevent.cpp b/vendor/adb/fdevent/fdevent.cpp
index 6ae120b..8aaeefd 100644
--- a/vendor/adb/fdevent/fdevent.cpp
+++ b/vendor/adb/fdevent/fdevent.cpp
@@ -80,7 +80,7 @@ fdevent* fdevent_context::Create(unique_fd fd, std::variant<fd_func, fd_func2> f
         // Here is not proper to handle the error. If it fails here, some error is
         // likely to be detected by poll(), then we can let the callback function
         // to handle it.
-        LOG(ERROR) << "failed to set non-blocking mode for fd " << fde->fd.get();
+       LOG_SAFE(ERROR) << "failed to set non-blocking mode for fd " << fde->fd.get();
     }
 
     this->fdevent_set_.insert(fde);
diff --git a/vendor/adb/fdevent/fdevent_epoll.cpp b/vendor/adb/fdevent/fdevent_epoll.cpp
index 54471a5..889755e 100644
--- a/vendor/adb/fdevent/fdevent_epoll.cpp
+++ b/vendor/adb/fdevent/fdevent_epoll.cpp
@@ -179,7 +179,7 @@ void fdevent_context_epoll::Loop() {
                     auto deadline = fde.last_active + *fde.timeout;
                     if (deadline < post_poll) {
                         events |= FDE_TIMEOUT;
-                        LOG(DEBUG) << dump_fde(&fde) << " timed out";
+                       LOG_SAFE(DEBUG) << dump_fde(&fde) << " timed out";
                         fde_events.emplace_back(&fde, events);
                         fde.last_active = post_poll;
                     }
diff --git a/vendor/adb/libs/adbconnection/adbconnection_client.cpp b/vendor/adb/libs/adbconnection/adbconnection_client.cpp
index 1d06a97..bbc1864 100644
--- a/vendor/adb/libs/adbconnection/adbconnection_client.cpp
+++ b/vendor/adb/libs/adbconnection/adbconnection_client.cpp
@@ -69,7 +69,7 @@ bool SocketPeerIsTrusted(int fd) {
 
   passwd* shell = getpwnam("shell");
   if (cr.uid != 0 && cr.uid != shell->pw_uid) {
-    LOG(ERROR) << "untrusted uid " << cr.uid << " on other end of socket";
+   LOG_SAFE(ERROR) << "untrusted uid " << cr.uid << " on other end of socket";
     return false;
   }
 
@@ -79,24 +79,24 @@ bool SocketPeerIsTrusted(int fd) {
 static void send_app_info(const AdbConnectionClientContext* ctx) {
   std::lock_guard<std::mutex> lock(app_info.mutex);
   if (!ctx) {
-    LOG(WARNING) << "Can't send app_info: No connection to adbd";
+   LOG_SAFE(WARNING) << "Can't send app_info: No connection to adbd";
     return;
   }
 
   if (!app_info.has_pending_update) {
-    LOG(WARNING) << "adbconnection_client: No pending updates";
+   LOG_SAFE(WARNING) << "adbconnection_client: No pending updates";
     return;
   }
 
   auto protobufProcess = app_info.process.toProtobuf();
   std::string serialized_message;
   if (!protobufProcess.SerializeToString(&serialized_message)) {
-    LOG(ERROR) << "Unable to build ART -> adbd message";
+   LOG_SAFE(ERROR) << "Unable to build ART -> adbd message";
     return;
   }
 
   if (serialized_message.size() > MAX_APP_MESSAGE_LENGTH) {
-    LOG(ERROR) << "adbd appinfo message too big (> " << MAX_APP_MESSAGE_LENGTH << ")";
+   LOG_SAFE(ERROR) << "adbd appinfo message too big (> " << MAX_APP_MESSAGE_LENGTH << ")";
     return;
   }
 
@@ -124,7 +124,7 @@ AdbConnectionClientContext* adbconnection_client_new(
     switch (info->type) {
       case AdbConnectionClientInfoType::pid:
         if (pid) {
-          LOG(ERROR) << "multiple pid entries in AdbConnectionClientInfo, ignoring";
+         LOG_SAFE(ERROR) << "multiple pid entries in AdbConnectionClientInfo, ignoring";
           continue;
         }
         pid = info->data.pid;
@@ -132,7 +132,7 @@ AdbConnectionClientContext* adbconnection_client_new(
 
       case AdbConnectionClientInfoType::debuggable:
         if (debuggable) {
-          LOG(ERROR) << "multiple debuggable entries in AdbConnectionClientInfo, ignoring";
+         LOG_SAFE(ERROR) << "multiple debuggable entries in AdbConnectionClientInfo, ignoring";
           continue;
         }
         debuggable = info->data.debuggable;
@@ -140,7 +140,7 @@ AdbConnectionClientContext* adbconnection_client_new(
 
       case AdbConnectionClientInfoType::profileable:
         if (profileable) {
-          LOG(ERROR) << "multiple profileable entries in AdbConnectionClientInfo, ignoring";
+         LOG_SAFE(ERROR) << "multiple profileable entries in AdbConnectionClientInfo, ignoring";
           continue;
         }
         profileable = info->data.profileable;
@@ -148,7 +148,7 @@ AdbConnectionClientContext* adbconnection_client_new(
 
       case AdbConnectionClientInfoType::architecture:
         if (architecture) {
-          LOG(ERROR) << "multiple architecture entries in AdbConnectionClientInfo, ignoring";
+         LOG_SAFE(ERROR) << "multiple architecture entries in AdbConnectionClientInfo, ignoring";
           continue;
         }
         architecture = std::string(info->data.architecture.name, info->data.architecture.size);
@@ -157,12 +157,12 @@ AdbConnectionClientContext* adbconnection_client_new(
   }
 
   if (!pid) {
-    LOG(ERROR) << "AdbConnectionClientInfo missing required field pid";
+   LOG_SAFE(ERROR) << "AdbConnectionClientInfo missing required field pid";
     return nullptr;
   }
 
   if (!debuggable) {
-    LOG(ERROR) << "AdbConnectionClientInfo missing required field debuggable";
+   LOG_SAFE(ERROR) << "AdbConnectionClientInfo missing required field debuggable";
     return nullptr;
   }
 
@@ -172,12 +172,12 @@ AdbConnectionClientContext* adbconnection_client_new(
 #endif
   if (expectProfileableAndArch) {
     if (!profileable) {
-      LOG(ERROR) << "AdbConnectionClientInfo missing required field profileable";
+     LOG_SAFE(ERROR) << "AdbConnectionClientInfo missing required field profileable";
       return nullptr;
     }
 
     if (!architecture) {
-      LOG(ERROR) << "AdbConnectionClientInfo missing required field architecture";
+     LOG_SAFE(ERROR) << "AdbConnectionClientInfo missing required field architecture";
       return nullptr;
     }
   }
@@ -193,7 +193,7 @@ AdbConnectionClientContext* adbconnection_client_new(
   // We don't want to just blindly connect, because if there's nothing listening, we'll end up
   // waking up every second and preventing the CPU from going to sleep.
   if (!android::base::WaitForProperty("init.svc.adbd", "running")) {
-    LOG(ERROR) << "adbd isn't running";
+   LOG_SAFE(ERROR) << "adbd isn't running";
     return nullptr;
   }
 #endif
@@ -209,7 +209,7 @@ AdbConnectionClientContext* adbconnection_client_new(
     if (errno == ECONNREFUSED) {
       // On userdebug devices, every Java process is debuggable, so if adbd is explicitly turned
       // off, this would spew enormous amounts of red-herring errors.
-      LOG(DEBUG) << "failed to connect to jdwp control socket, adbd not running?";
+     LOG_SAFE(DEBUG) << "failed to connect to jdwp control socket, adbd not running?";
     } else {
       PLOG(ERROR) << "failed to connect to jdwp control socket";
     }
@@ -218,7 +218,7 @@ AdbConnectionClientContext* adbconnection_client_new(
 
   bool trusted = SocketPeerIsTrusted(ctx->control_socket_.get());
   if (!trusted) {
-    LOG(ERROR) << "adb socket is not trusted, aborting connection";
+   LOG_SAFE(ERROR) << "adb socket is not trusted, aborting connection";
     return nullptr;
   }
 
diff --git a/vendor/adb/libs/adbconnection/adbconnection_server.cpp b/vendor/adb/libs/adbconnection/adbconnection_server.cpp
index 2413ee6..2260b8a 100644
--- a/vendor/adb/libs/adbconnection/adbconnection_server.cpp
+++ b/vendor/adb/libs/adbconnection/adbconnection_server.cpp
@@ -39,7 +39,7 @@ std::optional<ProcessInfo> readProcessInfoFromSocket(int socket) {
   ssize_t rc = TEMP_FAILURE_RETRY(recv(socket, proto.data(), proto.length(), MSG_PEEK));
 
   if (rc == 0) {
-    LOG(INFO) << "Remote process closed the socket (on MSG_PEEK)";
+   LOG_SAFE(INFO) << "Remote process closed the socket (on MSG_PEEK)";
     return {};
   }
 
@@ -53,7 +53,7 @@ std::optional<ProcessInfo> readProcessInfoFromSocket(int socket) {
   rc = TEMP_FAILURE_RETRY(recv(socket, proto.data(), message_size, 0));
 
   if (rc == 0) {
-    LOG(INFO) << "Remote process closed the socket (on recv)";
+   LOG_SAFE(INFO) << "Remote process closed the socket (on recv)";
     return {};
   }
 
@@ -63,7 +63,7 @@ std::optional<ProcessInfo> readProcessInfoFromSocket(int socket) {
   }
 
   if (rc != message_size) {
-    LOG(ERROR) << "adbconnection_server: Unexpected ProcessInfo size " << message_size
+   LOG_SAFE(ERROR) << "adbconnection_server: Unexpected ProcessInfo size " << message_size
                << " bytes but got " << rc;
     return {};
   }
@@ -133,7 +133,7 @@ void adbconnection_listen(void (*callback)(int fd, ProcessInfo process)) {
                                [&](const unique_fd& fd) { return fd.get() == event.data.fd; });
 
         if (it == pending_connections.end()) {
-          LOG(FATAL) << "failed to find JDWP client (" << event.data.fd
+         LOG_SAFE(FATAL) << "failed to find JDWP client (" << event.data.fd
                      << ") in pending connections";
         }
 
@@ -141,7 +141,7 @@ void adbconnection_listen(void (*callback)(int fd, ProcessInfo process)) {
         if (process_info) {
           callback(it->release(), *process_info);
         } else {
-          LOG(ERROR) << "Unable to read ProcessInfo from app startup";
+         LOG_SAFE(ERROR) << "Unable to read ProcessInfo from app startup";
         }
 
         if (epoll_ctl(epfd.get(), EPOLL_CTL_DEL, event.data.fd, nullptr) != 0) {
diff --git a/vendor/adb/pairing_auth/aes_128_gcm.cpp b/vendor/adb/pairing_auth/aes_128_gcm.cpp
index 2d5f86f..8e584ac 100644
--- a/vendor/adb/pairing_auth/aes_128_gcm.cpp
+++ b/vendor/adb/pairing_auth/aes_128_gcm.cpp
@@ -54,7 +54,7 @@ std::optional<size_t> Aes128Gcm::Encrypt(const uint8_t* in, size_t in_len, uint8
     size_t written_sz;
     if (!EVP_AEAD_CTX_seal(context_.get(), out, &written_sz, out_len, nonce.data(), nonce.size(),
                            in, in_len, nullptr, 0)) {
-        LOG(ERROR) << "Failed to encrypt (in_len=" << in_len << ", out_len=" << out_len
+       LOG_SAFE(ERROR) << "Failed to encrypt (in_len=" << in_len << ", out_len=" << out_len
                    << ", out_len_needed=" << EncryptedSize(in_len) << ")";
         return std::nullopt;
     }
@@ -70,7 +70,7 @@ std::optional<size_t> Aes128Gcm::Decrypt(const uint8_t* in, size_t in_len, uint8
     size_t written_sz;
     if (!EVP_AEAD_CTX_open(context_.get(), out, &written_sz, out_len, nonce.data(), nonce.size(),
                            in, in_len, nullptr, 0)) {
-        LOG(ERROR) << "Failed to decrypt (in_len=" << in_len << ", out_len=" << out_len
+       LOG_SAFE(ERROR) << "Failed to decrypt (in_len=" << in_len << ", out_len=" << out_len
                    << ", out_len_needed=" << DecryptedSize(in_len) << ")";
         return std::nullopt;
     }
diff --git a/vendor/adb/pairing_auth/pairing_auth.cpp b/vendor/adb/pairing_auth/pairing_auth.cpp
index 0cf43a3..ffe83d7 100644
--- a/vendor/adb/pairing_auth/pairing_auth.cpp
+++ b/vendor/adb/pairing_auth/pairing_auth.cpp
@@ -115,7 +115,7 @@ PairingAuthCtx::PairingAuthCtx(Role role, const Data& pswd) : role_(role) {
     }
     spake2_ctx_.reset(SPAKE2_CTX_new(spake_role, my_name, my_len, their_name, their_len));
     if (spake2_ctx_ == nullptr) {
-        LOG(ERROR) << "Unable to create a SPAKE2 context.";
+       LOG_SAFE(ERROR) << "Unable to create a SPAKE2 context.";
         return;
     }
 
@@ -125,7 +125,7 @@ PairingAuthCtx::PairingAuthCtx(Role role, const Data& pswd) : role_(role) {
     int status = SPAKE2_generate_msg(spake2_ctx_.get(), key, &key_size, SPAKE2_MAX_MSG_SIZE,
                                      pswd.data(), pswd.size());
     if (status != 1 || key_size == 0) {
-        LOG(ERROR) << "Unable to generate the SPAKE2 public key.";
+       LOG_SAFE(ERROR) << "Unable to generate the SPAKE2 public key.";
         return;
     }
     our_msg_.assign(key, key + key_size);
@@ -142,7 +142,7 @@ bool PairingAuthCtx::InitCipher(const PairingAuthCtx::Data& their_msg) {
 
     // Don't even try to process a message over the SPAKE2_MAX_MSG_SIZE
     if (their_msg.size() > SPAKE2_MAX_MSG_SIZE) {
-        LOG(ERROR) << "their_msg size [" << their_msg.size() << "] greater then max size ["
+       LOG_SAFE(ERROR) << "their_msg size [" << their_msg.size() << "] greater then max size ["
                    << SPAKE2_MAX_MSG_SIZE << "].";
         return false;
     }
@@ -152,7 +152,7 @@ bool PairingAuthCtx::InitCipher(const PairingAuthCtx::Data& their_msg) {
     int status = SPAKE2_process_msg(spake2_ctx_.get(), key_material, &key_material_len,
                                     sizeof(key_material), their_msg.data(), their_msg.size());
     if (status != 1) {
-        LOG(ERROR) << "Unable to process their public key";
+       LOG_SAFE(ERROR) << "Unable to process their public key";
         return false;
     }
 
@@ -171,7 +171,7 @@ PairingAuthCtx::Data PairingAuthCtx::Encrypt(const PairingAuthCtx::Data& data) {
     Data encrypted(cipher_->EncryptedSize(data.size()));
     auto out_size = cipher_->Encrypt(data.data(), data.size(), encrypted.data(), encrypted.size());
     if (!out_size.has_value() || *out_size == 0) {
-        LOG(ERROR) << "Unable to encrypt data";
+       LOG_SAFE(ERROR) << "Unable to encrypt data";
         return Data();
     }
     encrypted.resize(*out_size);
@@ -188,7 +188,7 @@ PairingAuthCtx::Data PairingAuthCtx::Decrypt(const PairingAuthCtx::Data& data) {
     size_t decrypted_size = decrypted.size();
     auto out_size = cipher_->Decrypt(data.data(), data.size(), decrypted.data(), decrypted_size);
     if (!out_size.has_value() || *out_size == 0) {
-        LOG(ERROR) << "Unable to decrypt data";
+       LOG_SAFE(ERROR) << "Unable to decrypt data";
         return Data();
     }
     decrypted.resize(*out_size);
diff --git a/vendor/adb/pairing_connection/pairing_connection.cpp b/vendor/adb/pairing_connection/pairing_connection.cpp
index ffe49a9..f1c41dc 100644
--- a/vendor/adb/pairing_connection/pairing_connection.cpp
+++ b/vendor/adb/pairing_connection/pairing_connection.cpp
@@ -173,7 +173,7 @@ bool PairingConnectionCtx::SetupTlsConnection() {
             fd_);
 
     if (tls_ == nullptr) {
-        LOG(ERROR) << "Unable to start TlsConnection. Unable to pair fd=" << fd_.get();
+       LOG_SAFE(ERROR) << "Unable to start TlsConnection. Unable to pair fd=" << fd_.get();
         return false;
     }
 
@@ -183,7 +183,7 @@ bool PairingConnectionCtx::SetupTlsConnection() {
     // SSL doesn't seem to behave correctly with fdevents so just do a blocking
     // read for the pairing data.
     if (tls_->DoHandshake() != TlsError::Success) {
-        LOG(ERROR) << "Failed to handshake with the peer fd=" << fd_.get();
+       LOG_SAFE(ERROR) << "Failed to handshake with the peer fd=" << fd_.get();
         return false;
     }
 
@@ -191,7 +191,7 @@ bool PairingConnectionCtx::SetupTlsConnection() {
     // exported key material from the tls connection to the password.
     std::vector<uint8_t> exportedKeyMaterial = tls_->ExportKeyingMaterial(kExportedKeySize);
     if (exportedKeyMaterial.empty()) {
-        LOG(ERROR) << "Failed to export key material";
+       LOG_SAFE(ERROR) << "Failed to export key material";
         return false;
     }
     pswd_.insert(pswd_.end(), std::make_move_iterator(exportedKeyMaterial.begin()),
@@ -208,7 +208,7 @@ bool PairingConnectionCtx::WriteHeader(const PairingPacketHeader* header,
     if (!tls_->WriteFully(std::string_view(reinterpret_cast<const char*>(&network_header),
                                            sizeof(PairingPacketHeader))) ||
         !tls_->WriteFully(payload)) {
-        LOG(ERROR) << "Failed to write out PairingPacketHeader";
+       LOG_SAFE(ERROR) << "Failed to write out PairingPacketHeader";
         state_ = State::Stopped;
         return false;
     }
@@ -227,13 +227,13 @@ bool PairingConnectionCtx::ReadHeader(PairingPacketHeader* header) {
     ++p;
     if (header->version < kMinSupportedKeyHeaderVersion ||
         header->version > kMaxSupportedKeyHeaderVersion) {
-        LOG(ERROR) << "PairingPacketHeader version mismatch (us=" << kCurrentKeyHeaderVersion
+       LOG_SAFE(ERROR) << "PairingPacketHeader version mismatch (us=" << kCurrentKeyHeaderVersion
                    << " them=" << header->version << ")";
         return false;
     }
     // Next byte is the PairingPacket::Type
     if (!adb::proto::PairingPacket::Type_IsValid(*p)) {
-        LOG(ERROR) << "Unknown PairingPacket type=" << static_cast<uint32_t>(*p);
+       LOG_SAFE(ERROR) << "Unknown PairingPacket type=" << static_cast<uint32_t>(*p);
         return false;
     }
     header->type = *p;
@@ -241,7 +241,7 @@ bool PairingConnectionCtx::ReadHeader(PairingPacketHeader* header) {
     // Last, the payload size
     header->payload = ntohl(*(reinterpret_cast<uint32_t*>(p)));
     if (header->payload == 0 || header->payload > kMaxPayloadSize) {
-        LOG(ERROR) << "header payload not within a safe payload size (size=" << header->payload
+       LOG_SAFE(ERROR) << "header payload not within a safe payload size (size=" << header->payload
                    << ")";
         return false;
     }
@@ -262,7 +262,7 @@ bool PairingConnectionCtx::CheckHeaderType(adb::proto::PairingPacket::Type expec
                                            uint8_t actual) {
     uint8_t expected = *reinterpret_cast<uint8_t*>(&expected_type);
     if (actual != expected) {
-        LOG(ERROR) << "Unexpected header type (expected=" << static_cast<uint32_t>(expected)
+       LOG_SAFE(ERROR) << "Unexpected header type (expected=" << static_cast<uint32_t>(expected)
                    << " actual=" << static_cast<uint32_t>(actual) << ")";
         return false;
     }
@@ -303,13 +303,13 @@ bool PairingConnectionCtx::DoExchangeMsgs() {
     // Write our SPAKE2 msg
     if (!WriteHeader(&header,
                      std::string_view(reinterpret_cast<const char*>(msg.data()), msg.size()))) {
-        LOG(ERROR) << "Failed to write SPAKE2 msg.";
+       LOG_SAFE(ERROR) << "Failed to write SPAKE2 msg.";
         return false;
     }
 
     // Read the peer's SPAKE2 msg header
     if (!ReadHeader(&header)) {
-        LOG(ERROR) << "Invalid PairingPacketHeader.";
+       LOG_SAFE(ERROR) << "Invalid PairingPacketHeader.";
         return false;
     }
     if (!CheckHeaderType(adb::proto::PairingPacket::SPAKE2_MSG, header.type)) {
@@ -321,7 +321,7 @@ bool PairingConnectionCtx::DoExchangeMsgs() {
     auto their_msg = tls_->ReadFully(header.payload);
     if (their_msg.empty() ||
         !pairing_auth_init_cipher(auth_.get(), their_msg.data(), their_msg.size())) {
-        LOG(ERROR) << "Unable to initialize pairing cipher [their_msg.size=" << their_msg.size()
+       LOG_SAFE(ERROR) << "Unable to initialize pairing cipher [their_msg.size=" << their_msg.size()
                    << "]";
         return false;
     }
@@ -338,7 +338,7 @@ bool PairingConnectionCtx::DoExchangePeerInfo() {
     CHECK(!outbuf.empty());
     size_t outsize;
     if (!pairing_auth_encrypt(auth_.get(), buf.data(), buf.size(), outbuf.data(), &outsize)) {
-        LOG(ERROR) << "Failed to encrypt peer info";
+       LOG_SAFE(ERROR) << "Failed to encrypt peer info";
         return false;
     }
     outbuf.resize(outsize);
@@ -350,21 +350,21 @@ bool PairingConnectionCtx::DoExchangePeerInfo() {
     out_header.payload = htonl(outbuf.size());
     if (!tls_->WriteFully(
                 std::string_view(reinterpret_cast<const char*>(&out_header), sizeof(out_header)))) {
-        LOG(ERROR) << "Unable to write PairingPacketHeader";
+       LOG_SAFE(ERROR) << "Unable to write PairingPacketHeader";
         return false;
     }
 
     // Write out the encrypted payload
     if (!tls_->WriteFully(
                 std::string_view(reinterpret_cast<const char*>(outbuf.data()), outbuf.size()))) {
-        LOG(ERROR) << "Unable to write encrypted peer info";
+       LOG_SAFE(ERROR) << "Unable to write encrypted peer info";
         return false;
     }
 
     // Read in the peer's packet header
     PairingPacketHeader header;
     if (!ReadHeader(&header)) {
-        LOG(ERROR) << "Invalid PairingPacketHeader.";
+       LOG_SAFE(ERROR) << "Invalid PairingPacketHeader.";
         return false;
     }
 
@@ -381,19 +381,19 @@ bool PairingConnectionCtx::DoExchangePeerInfo() {
     // Try to decrypt the certificate
     outbuf.resize(pairing_auth_safe_decrypted_size(auth_.get(), buf.data(), buf.size()));
     if (outbuf.empty()) {
-        LOG(ERROR) << "Unsupported payload while decrypting peer info.";
+       LOG_SAFE(ERROR) << "Unsupported payload while decrypting peer info.";
         return false;
     }
 
     if (!pairing_auth_decrypt(auth_.get(), buf.data(), buf.size(), outbuf.data(), &outsize)) {
-        LOG(ERROR) << "Failed to decrypt";
+       LOG_SAFE(ERROR) << "Failed to decrypt";
         return false;
     }
     outbuf.resize(outsize);
 
     // The decrypted message should contain the PeerInfo.
     if (outbuf.size() != sizeof(PeerInfo)) {
-        LOG(ERROR) << "Got size=" << outbuf.size() << "PeerInfo.size=" << sizeof(PeerInfo);
+       LOG_SAFE(ERROR) << "Got size=" << outbuf.size() << "PeerInfo.size=" << sizeof(PeerInfo);
         return false;
     }
 
@@ -429,7 +429,7 @@ void PairingConnectionCtx::StartWorker() {
                 return;
             case State::Ready:
             case State::Stopped:
-                LOG(FATAL) << __func__ << ": Got invalid state";
+               LOG_SAFE(FATAL) << __func__ << ": Got invalid state";
                 return;
         }
     }
diff --git a/vendor/adb/pairing_connection/pairing_server.cpp b/vendor/adb/pairing_connection/pairing_server.cpp
index a18ac74..1e06420 100644
--- a/vendor/adb/pairing_connection/pairing_server.cpp
+++ b/vendor/adb/pairing_connection/pairing_server.cpp
@@ -173,17 +173,17 @@ uint16_t PairingServerCtx::Start(pairing_server_result_cb cb, void* opaque) {
     opaque_ = opaque;
 
     if (state_ != State::Ready) {
-        LOG(ERROR) << "PairingServerCtx already running or stopped";
+       LOG_SAFE(ERROR) << "PairingServerCtx already running or stopped";
         return 0;
     }
 
     port_ = SetupServer();
     if (port_ == 0) {
-        LOG(ERROR) << "Unable to start PairingServer";
+       LOG_SAFE(ERROR) << "Unable to start PairingServer";
         state_ = State::Stopped;
         return 0;
     }
-    LOG(INFO) << "Pairing server started on port " << port_;
+   LOG_SAFE(INFO) << "Pairing server started on port " << port_;
 
     state_ = State::Running;
     return port_;
@@ -227,7 +227,7 @@ void PairingServerCtx::StopServer() {
         // This can happen if the server didn't start.
         PLOG(ERROR) << "write to eventfd failed";
     } else if (rc != sizeof(value)) {
-        LOG(FATAL) << "write to event returned short (" << rc << ")";
+       LOG_SAFE(FATAL) << "write to event returned short (" << rc << ")";
     }
 }
 
@@ -286,7 +286,7 @@ void PairingServerCtx::ServerWorker() {
             PLOG(ERROR) << "epoll_wait failed";
             return;
         } else if (rc == 0) {
-            LOG(ERROR) << "epoll_wait returned 0";
+           LOG_SAFE(ERROR) << "epoll_wait returned 0";
             return;
         }
 
@@ -318,7 +318,7 @@ void PairingServerCtx::PairingConnectionCallback(const PeerInfo* peer_info, int
             event = std::make_tuple(fd, peer_info->type,
                                     std::string(reinterpret_cast<const char*>(peer_info->data)));
         } else {
-            LOG(WARNING) << "Ignoring successful pairing because of unknown "
+           LOG_SAFE(WARNING) << "Ignoring successful pairing because of unknown "
                          << "PeerInfo type=" << peer_info->type;
         }
     } else {
@@ -375,7 +375,7 @@ void PairingServerCtx::ConnectionEventsWorker() {
                 bool started = pairing_connection_start(connection.get(), fd,
                                                         PairingConnectionCallback, this);
                 if (!started) {
-                    LOG(ERROR) << "PairingServer unable to start a PairingConnection fd=" << fd;
+                   LOG_SAFE(ERROR) << "PairingServer unable to start a PairingConnection fd=" << fd;
                     ufd.reset(fd);
                 } else {
                     connections_[fd] = std::move(connection);
@@ -423,7 +423,7 @@ bool PairingServerCtx::HandleNewClientConnection(int fd) {
     }
     auto connection = CreatePairingConnection(pswd_, peer_info_, cert_, priv_key_);
     if (connection == nullptr) {
-        LOG(ERROR) << "PairingServer unable to create a PairingConnection fd=" << fd;
+       LOG_SAFE(ERROR) << "PairingServer unable to create a PairingConnection fd=" << fd;
         return false;
     }
     // send the new connection to the connection thread for further processing
diff --git a/vendor/adb/services.cpp b/vendor/adb/services.cpp
index de9f628..46e67dc 100644
--- a/vendor/adb/services.cpp
+++ b/vendor/adb/services.cpp
@@ -88,7 +88,7 @@ unique_fd service_to_fd(std::string_view name, atransport* transport) {
     if (is_socket_spec(name)) {
         std::string error;
         if (!socket_spec_connect(&ret, name, nullptr, nullptr, &error)) {
-            LOG(ERROR) << "failed to connect to socket '" << name << "': " << error;
+           LOG_SAFE(ERROR) << "failed to connect to socket '" << name << "': " << error;
         }
     } else {
 #if !ADB_HOST
diff --git a/vendor/adb/sockets.cpp b/vendor/adb/sockets.cpp
index acb3de1..3d916f4 100644
--- a/vendor/adb/sockets.cpp
+++ b/vendor/adb/sockets.cpp
@@ -85,7 +85,7 @@ void install_local_socket(asocket* s) {
 
     // Socket ids should never be 0.
     if (local_socket_next_id == 0) {
-        LOG(FATAL) << "local socket id overflow";
+       LOG_SAFE(FATAL) << "local socket id overflow";
     }
 
     local_socket_list.push_back(s);
@@ -301,13 +301,13 @@ static void deferred_close(unique_fd fd) {
                 // There's potentially more data to read.
                 auto duration = std::chrono::steady_clock::now() - socket_info->begin;
                 if (duration > 1s) {
-                    LOG(WARNING) << "timeout expired while reading data after flushing socket, closing";
+                   LOG_SAFE(WARNING) << "timeout expired while reading data after flushing socket, closing";
                 } else {
                     return;
                 }
             }
         } else if (event & FDE_TIMEOUT) {
-            LOG(WARNING) << "timeout expired while flushing socket, closing";
+           LOG_SAFE(WARNING) << "timeout expired while flushing socket, closing";
         }
 
         // Either there was an error, we hit the end of the socket, or our timeout expired.
@@ -421,7 +421,7 @@ void local_socket_ack(asocket* s, std::optional<int32_t> acked_bytes) {
     // In the future, we can use this to preemptively supply backpressure, instead
     // of waiting for the writer to hit its limit.
     if (s->available_send_bytes.has_value() != acked_bytes.has_value()) {
-        LOG(ERROR) << "delayed ack mismatch: socket = " << s->available_send_bytes.has_value()
+       LOG_SAFE(ERROR) << "delayed ack mismatch: socket = " << s->available_send_bytes.has_value()
                    << ", payload = " << acked_bytes.has_value();
         return;
     }
@@ -543,7 +543,7 @@ static void remote_socket_close(asocket* s) {
 // Returns a new non-NULL asocket handle.
 asocket* create_remote_socket(unsigned id, atransport* t) {
     if (id == 0) {
-        LOG(FATAL) << "invalid remote socket id (0)";
+       LOG_SAFE(FATAL) << "invalid remote socket id (0)";
     }
     asocket* s = new asocket();
     s->id = id;
@@ -826,16 +826,16 @@ static int smart_socket_enqueue(asocket* s, apacket::payload_type data) {
     if (android::base::ConsumePrefix(&service, "host-serial:")) {
         // serial number should follow "host:" and could be a host:port string.
         if (!internal::parse_host_service(&serial, &service, service)) {
-            LOG(ERROR) << "SS(" << s->id << "): failed to parse host service: " << service;
+           LOG_SAFE(ERROR) << "SS(" << s->id << "): failed to parse host service: " << service;
             goto fail;
         }
     } else if (android::base::ConsumePrefix(&service, "host-transport-id:")) {
         if (!ParseUint(&transport_id, service, &service)) {
-            LOG(ERROR) << "SS(" << s->id << "): failed to parse host transport id: " << service;
+           LOG_SAFE(ERROR) << "SS(" << s->id << "): failed to parse host transport id: " << service;
             return -1;
         }
         if (!android::base::ConsumePrefix(&service, ":")) {
-            LOG(ERROR) << "SS(" << s->id << "): host-transport-id without command";
+           LOG_SAFE(ERROR) << "SS(" << s->id << "): host-transport-id without command";
             return -1;
         }
     } else if (android::base::ConsumePrefix(&service, "host-usb:")) {
diff --git a/vendor/adb/sysdeps/errno.cpp b/vendor/adb/sysdeps/errno.cpp
index e6af68b..686030d 100644
--- a/vendor/adb/sysdeps/errno.cpp
+++ b/vendor/adb/sysdeps/errno.cpp
@@ -78,7 +78,7 @@ static std::unordered_map<int, int>& wire_to_host = *generate_wire_to_host();
 int errno_to_wire(int error) {
     auto it = host_to_wire.find(error);
     if (it == host_to_wire.end()) {
-        LOG(ERROR) << "failed to convert errno " << error << " (" << strerror(error) << ") to wire";
+       LOG_SAFE(ERROR) << "failed to convert errno " << error << " (" << strerror(error) << ") to wire";
 
         // Return EIO;
         return 5;
@@ -89,7 +89,7 @@ int errno_to_wire(int error) {
 int errno_from_wire(int error) {
     auto it = host_to_wire.find(error);
     if (it == host_to_wire.end()) {
-        LOG(ERROR) << "failed to convert errno " << error << " from wire";
+       LOG_SAFE(ERROR) << "failed to convert errno " << error << " from wire";
         return EIO;
     }
     return it->second;
diff --git a/vendor/adb/sysdeps/posix/network.cpp b/vendor/adb/sysdeps/posix/network.cpp
index ec4c7c8..e66e256 100644
--- a/vendor/adb/sysdeps/posix/network.cpp
+++ b/vendor/adb/sysdeps/posix/network.cpp
@@ -143,11 +143,11 @@ int network_connect(const std::string& host, int port, int type, int timeout, st
     if (getaddrinfo_error != 0) {
         *error = android::base::StringPrintf("failed to resolve host: '%s': %s", host.c_str(),
                                              gai_strerror(getaddrinfo_error));
-        LOG(WARNING) << *error;
+       LOG_SAFE(WARNING) << *error;
     } else {
         *error = android::base::StringPrintf("failed to connect to '%s:%d': %s", host.c_str(), port,
                                              strerror(errno));
-        LOG(WARNING) << *error;
+       LOG_SAFE(WARNING) << *error;
     }
     return -1;
 }
diff --git a/vendor/adb/sysdeps_win32.cpp b/vendor/adb/sysdeps_win32.cpp
index a2f7dab..6b0e9f8 100644
--- a/vendor/adb/sysdeps_win32.cpp
+++ b/vendor/adb/sysdeps_win32.cpp
@@ -765,7 +765,7 @@ static void _init_winsock() {
         WSADATA wsaData;
         int rc = WSAStartup(MAKEWORD(2, 2), &wsaData);
         if (rc != 0) {
-            LOG(FATAL) << "could not initialize Winsock: "
+           LOG_SAFE(FATAL) << "could not initialize Winsock: "
                        << android::base::SystemErrorCodeToString(rc);
         }
 
@@ -797,7 +797,7 @@ static int GetSocketProtocolFromSocketType(int type) {
         case SOCK_DGRAM:
             return IPPROTO_UDP;
         default:
-            LOG(FATAL) << "Unknown socket type: " << type;
+           LOG_SAFE(FATAL) << "Unknown socket type: " << type;
             return 0;
     }
 }
@@ -837,7 +837,7 @@ int adb_bind(borrowed_fd fd, const sockaddr* addr, socklen_t addrlen) {
 
     if (bind(fh->fh_socket, addr, addrlen) == SOCKET_ERROR) {
         const DWORD err = WSAGetLastError();
-        LOG(ERROR) << "adb_bind: bind on fd " << fd.get()
+       LOG_SAFE(ERROR) << "adb_bind: bind on fd " << fd.get()
                    << " failed: " + android::base::SystemErrorCodeToString(err);
         _socket_set_errno(err);
         return -1;
@@ -894,7 +894,7 @@ ssize_t adb_sendmsg(borrowed_fd fd, const struct adb_msghdr* msg, int flags) {
     auto ret = WSASendMsg(fh->fh_socket, &wmsg, 0, &num_bytes, NULL, NULL);
     if (ret == SOCKET_ERROR) {
         const DWORD err = WSAGetLastError();
-        LOG(ERROR) << "WSASendMsg() failed " << android::base::SystemErrorCodeToString(err);
+       LOG_SAFE(ERROR) << "WSASendMsg() failed " << android::base::SystemErrorCodeToString(err);
         _socket_set_errno(err);
         return -1;
     }
@@ -956,7 +956,7 @@ ssize_t adb_recvmsg(borrowed_fd fd, struct adb_msghdr* msg, int flags) {
         const auto ret = WSARecvMsgFunc(fh->fh_socket, &wmsg, &num_bytes, NULL, NULL);
         if (ret == SOCKET_ERROR) {
             const DWORD err = WSAGetLastError();
-            LOG(ERROR) << "WSARecvMsg() failed " << android::base::SystemErrorCodeToString(err);
+           LOG_SAFE(ERROR) << "WSARecvMsg() failed " << android::base::SystemErrorCodeToString(err);
             _socket_set_errno(err);
             return -1;
         }
@@ -1210,7 +1210,7 @@ std::optional<ssize_t> network_peek(borrowed_fd fd) {
     unsigned long sz_bytes = -1;
     if (ioctlsocket(fh->fh_socket, FIONREAD, &sz_bytes) != 0) {
         const DWORD err = WSAGetLastError();
-        LOG(ERROR) << "ioctlsocket() failed " << android::base::SystemErrorCodeToString(err);
+       LOG_SAFE(ERROR) << "ioctlsocket() failed " << android::base::SystemErrorCodeToString(err);
         _socket_set_errno(err);
         return std::nullopt;
     }
@@ -1244,7 +1244,7 @@ int adb_socket_accept(borrowed_fd serverfd, struct sockaddr* addr, socklen_t* ad
     fh->fh_socket = accept(serverfh->fh_socket, addr, addrlen);
     if (fh->fh_socket == INVALID_SOCKET) {
         const DWORD err = WSAGetLastError();
-        LOG(ERROR) << "adb_socket_accept: accept on fd " << serverfd.get()
+       LOG_SAFE(ERROR) << "adb_socket_accept: accept on fd " << serverfd.get()
                    << " failed: " + android::base::SystemErrorCodeToString(err);
         _socket_set_errno(err);
         return -1;
@@ -1534,11 +1534,11 @@ static bool _get_key_event_record(const HANDLE console, INPUT_RECORD* const inpu
         }
 
         if (read_count == 0) {   // should be impossible
-            LOG(FATAL) << "ReadConsoleInputA returned 0";
+           LOG_SAFE(FATAL) << "ReadConsoleInputA returned 0";
         }
 
         if (read_count != 1) {   // should be impossible
-            LOG(FATAL) << "ReadConsoleInputA did not return one input record";
+           LOG_SAFE(FATAL) << "ReadConsoleInputA did not return one input record";
         }
 
         // If the console window is resized, emulate SIGWINCH by breaking out
@@ -1556,7 +1556,7 @@ static bool _get_key_event_record(const HANDLE console, INPUT_RECORD* const inpu
         if ((input_record->EventType == KEY_EVENT) &&
             (input_record->Event.KeyEvent.bKeyDown)) {
             if (input_record->Event.KeyEvent.wRepeatCount == 0) {
-                LOG(FATAL) << "ReadConsoleInputA returned a key event with zero repeat count";
+               LOG_SAFE(FATAL) << "ReadConsoleInputA returned a key event with zero repeat count";
             }
 
             // Got an interesting INPUT_RECORD, so return
@@ -2937,7 +2937,7 @@ static void _init_env() {
         // If _wenviron is null, then -municode probably wasn't used. That
         // linker flag will cause the entry point to setup _wenviron. It will
         // also require an implementation of wmain() (which we provide above).
-        LOG(FATAL) << "_wenviron is not set, did you link with -municode?";
+       LOG_SAFE(FATAL) << "_wenviron is not set, did you link with -municode?";
     }
 
     // Read name/value pairs from UTF-16 _wenviron and write new name/value
@@ -3149,7 +3149,7 @@ static void _init_console() {
     // Below's direct manipulation of env vars and not g_environ_utf8 assumes that _init_env() has
     // not yet been called. If this fails, _init_env() should be called after _init_console().
     if (g_environ_utf8.size() > 0) {
-        LOG(FATAL) << "environment variables have already been converted to UTF-8";
+       LOG_SAFE(FATAL) << "environment variables have already been converted to UTF-8";
     }
 
 #pragma push_macro("getenv")
diff --git a/vendor/adb/tls/adb_ca_list.cpp b/vendor/adb/tls/adb_ca_list.cpp
index 36afe42..bcb85b3 100644
--- a/vendor/adb/tls/adb_ca_list.cpp
+++ b/vendor/adb/tls/adb_ca_list.cpp
@@ -124,7 +124,7 @@ std::optional<std::string> SHA256HexStringToBits(std::string_view sha256_str) {
     for (size_t i = 0; i < SHA256_DIGEST_LENGTH; ++i) {
         auto bytestr = std::string(sha256_str.substr(i * 2, 2));
         if (!IsHexDigit(bytestr[0]) || !IsHexDigit(bytestr[1])) {
-            LOG(ERROR) << "SHA256 string has invalid non-hex chars";
+           LOG_SAFE(ERROR) << "SHA256 string has invalid non-hex chars";
             return std::nullopt;
         }
         result += static_cast<char>(std::stol(bytestr, nullptr, 16));
diff --git a/vendor/adb/tls/tls_connection.cpp b/vendor/adb/tls/tls_connection.cpp
index 26dc927..0f54db2 100644
--- a/vendor/adb/tls/tls_connection.cpp
+++ b/vendor/adb/tls/tls_connection.cpp
@@ -89,7 +89,7 @@ TlsConnectionImpl::TlsConnectionImpl(Role role, std::string_view cert, std::stri
                                      borrowed_fd fd)
     : role_(role), fd_(fd) {
     CHECK(!cert.empty() && !priv_key.empty());
-    LOG(INFO) << RoleToString() << "Initializing adbwifi TlsConnection";
+   LOG_SAFE(INFO) << RoleToString() << "Initializing adbwifi TlsConnection";
     cert_ = BufferFromPEM(cert);
     CHECK(cert_);
     priv_key_ = EvpPkeyFromPEM(priv_key);
@@ -121,7 +121,7 @@ bssl::UniquePtr<CRYPTO_BUFFER> TlsConnectionImpl::BufferFromPEM(std::string_view
     long data_len = 0;
 
     if (!PEM_read_bio(bio.get(), &name, &header, &data, &data_len)) {
-        LOG(ERROR) << "Failed to read certificate";
+       LOG_SAFE(ERROR) << "Failed to read certificate";
         return nullptr;
     }
     OPENSSL_free(name);
@@ -153,7 +153,7 @@ bool TlsConnectionImpl::AddTrustedCertificate(std::string_view cert) {
     // Create X509 buffer from the certificate string
     auto buf = X509FromBuffer(BufferFromPEM(cert));
     if (buf == nullptr) {
-        LOG(ERROR) << RoleToString() << "Failed to create a X509 buffer for the certificate.";
+       LOG_SAFE(ERROR) << RoleToString() << "Failed to create a X509 buffer for the certificate.";
         return false;
     }
     known_certificates_.push_back(std::move(buf));
@@ -209,7 +209,7 @@ TlsConnection::TlsError TlsConnectionImpl::GetFailureReason(int err) {
 }
 
 TlsConnection::TlsError TlsConnectionImpl::DoHandshake() {
-    LOG(INFO) << RoleToString() << "Starting adbwifi tls handshake";
+   LOG_SAFE(INFO) << RoleToString() << "Starting adbwifi tls handshake";
     ssl_ctx_.reset(SSL_CTX_new(TLS_method()));
     // TODO: Remove set_max_proto_version() once external/boringssl is updated
     // past
@@ -217,14 +217,14 @@ TlsConnection::TlsError TlsConnectionImpl::DoHandshake() {
     if (ssl_ctx_.get() == nullptr ||
         !SSL_CTX_set_min_proto_version(ssl_ctx_.get(), TLS1_3_VERSION) ||
         !SSL_CTX_set_max_proto_version(ssl_ctx_.get(), TLS1_3_VERSION)) {
-        LOG(ERROR) << RoleToString() << "Failed to create SSL context";
+       LOG_SAFE(ERROR) << RoleToString() << "Failed to create SSL context";
         return TlsError::UnknownFailure;
     }
 
     // Register user-supplied known certificates
     for (auto const& cert : known_certificates_) {
         if (X509_STORE_add_cert(SSL_CTX_get_cert_store(ssl_ctx_.get()), cert.get()) == 0) {
-            LOG(ERROR) << RoleToString() << "Unable to add certificates into the X509_STORE";
+           LOG_SAFE(ERROR) << RoleToString() << "Unable to add certificates into the X509_STORE";
             return TlsError::UnknownFailure;
         }
     }
@@ -251,7 +251,7 @@ TlsConnection::TlsError TlsConnectionImpl::DoHandshake() {
     };
     if (!SSL_CTX_set_chain_and_key(ssl_ctx_.get(), cert_chain.data(), cert_chain.size(),
                                    priv_key_.get(), nullptr)) {
-        LOG(ERROR) << RoleToString()
+       LOG_SAFE(ERROR) << RoleToString()
                    << "Unable to register the certificate chain file and private key ["
                    << SSLErrorString() << "]";
         Invalidate();
@@ -263,7 +263,7 @@ TlsConnection::TlsError TlsConnectionImpl::DoHandshake() {
     // Okay! Let's try to do the handshake!
     ssl_.reset(SSL_new(ssl_ctx_.get()));
     if (!SSL_set_fd(ssl_.get(), fd_.get())) {
-        LOG(ERROR) << RoleToString() << "SSL_set_fd failed. [" << SSLErrorString() << "]";
+       LOG_SAFE(ERROR) << RoleToString() << "SSL_set_fd failed. [" << SSLErrorString() << "]";
         return TlsError::UnknownFailure;
     }
 
@@ -276,7 +276,7 @@ TlsConnection::TlsError TlsConnectionImpl::DoHandshake() {
             break;
     }
     if (SSL_do_handshake(ssl_.get()) != 1) {
-        LOG(ERROR) << RoleToString() << "Handshake failed in SSL_accept/SSL_connect ["
+       LOG_SAFE(ERROR) << RoleToString() << "Handshake failed in SSL_accept/SSL_connect ["
                    << SSLErrorString() << "]";
         auto sslerr = ERR_get_error();
         Invalidate();
@@ -288,7 +288,7 @@ TlsConnection::TlsError TlsConnectionImpl::DoHandshake() {
         // Try to peek one byte for any failures. This assumes on success that
         // the server actually sends something.
         if (SSL_peek(ssl_.get(), &check, 1) <= 0) {
-            LOG(ERROR) << RoleToString() << "Post-handshake SSL_peek failed [" << SSLErrorString()
+           LOG_SAFE(ERROR) << RoleToString() << "Post-handshake SSL_peek failed [" << SSLErrorString()
                        << "]";
             auto sslerr = ERR_get_error();
             Invalidate();
@@ -296,7 +296,7 @@ TlsConnection::TlsError TlsConnectionImpl::DoHandshake() {
         }
     }
 
-    LOG(INFO) << RoleToString() << "Handshake succeeded.";
+   LOG_SAFE(INFO) << RoleToString() << "Handshake succeeded.";
     return TlsError::Success;
 }
 
@@ -373,7 +373,7 @@ std::string TlsConnectionImpl::SSL_IO_Error(int error) {
 bool TlsConnectionImpl::ReadFully(void* buf, size_t size) {
     CHECK_GT(size, 0U);
     if (!ssl_) {
-        LOG(ERROR) << RoleToString() << "Tried to read on a null SSL connection";
+       LOG_SAFE(ERROR) << RoleToString() << "Tried to read on a null SSL connection";
         return false;
     }
 
@@ -384,7 +384,7 @@ bool TlsConnectionImpl::ReadFully(void* buf, size_t size) {
                 SSL_read(ssl_.get(), p8 + offset, std::min(static_cast<size_t>(INT_MAX), size));
         if (bytes_read <= 0) {
             int error = SSL_get_error(ssl_.get(), bytes_read);
-            LOG(ERROR) << RoleToString() << "SSL_read failed [bytes_read=" << bytes_read
+           LOG_SAFE(ERROR) << RoleToString() << "SSL_read failed [bytes_read=" << bytes_read
                        << ", io_error=" << SSL_IO_Error(error) << "]";
 
             return false;
@@ -398,7 +398,7 @@ bool TlsConnectionImpl::ReadFully(void* buf, size_t size) {
 bool TlsConnectionImpl::WriteFully(std::string_view data) {
     CHECK(!data.empty());
     if (!ssl_) {
-        LOG(ERROR) << RoleToString() << "Tried to read on a null SSL connection";
+       LOG_SAFE(ERROR) << RoleToString() << "Tried to read on a null SSL connection";
         return false;
     }
 
@@ -407,7 +407,7 @@ bool TlsConnectionImpl::WriteFully(std::string_view data) {
                                   std::min(static_cast<size_t>(INT_MAX), data.size()));
         if (bytes_out <= 0) {
             int error = SSL_get_error(ssl_.get(), bytes_out);
-            LOG(ERROR) << RoleToString() << "SSL_write failed [bytes_out=" << bytes_out
+           LOG_SAFE(ERROR) << RoleToString() << "SSL_write failed [bytes_out=" << bytes_out
                        << ", io_error=" << SSL_IO_Error(error) << "]";
             return false;
         }
@@ -441,7 +441,7 @@ bool TlsConnection::SetCertAndKey(SSL* ssl, std::string_view cert, std::string_v
     };
     if (!SSL_set_chain_and_key(ssl, cert_chain.data(), cert_chain.size(), evp_pkey.get(),
                                nullptr)) {
-        LOG(ERROR) << "SSL_set_chain_and_key failed";
+       LOG_SAFE(ERROR) << "SSL_set_chain_and_key failed";
         return false;
     }
 
diff --git a/vendor/adb/transport.cpp b/vendor/adb/transport.cpp
index 4659717..98e3e6f 100644
--- a/vendor/adb/transport.cpp
+++ b/vendor/adb/transport.cpp
@@ -289,7 +289,7 @@ TransportId NextTransportId() {
 }
 
 void Connection::Reset() {
-    LOG(INFO) << "Connection::Reset(): stopping";
+   LOG_SAFE(INFO) << "Connection::Reset(): stopping";
     Stop();
 }
 
@@ -308,7 +308,7 @@ BlockingConnectionAdapter::~BlockingConnectionAdapter() {
 bool BlockingConnectionAdapter::Start() {
     std::lock_guard<std::mutex> lock(mutex_);
     if (started_) {
-        LOG(FATAL) << "BlockingConnectionAdapter(" << Serial() << "): started multiple times";
+       LOG_SAFE(FATAL) << "BlockingConnectionAdapter(" << Serial() << "): started multiple times";
     }
 
     StartReadThread();
@@ -515,7 +515,7 @@ bool FdConnection::Write(apacket* packet) {
 bool FdConnection::DoTlsHandshake(RSA* key, std::string* auth_key) {
     bssl::UniquePtr<EVP_PKEY> evp_pkey(EVP_PKEY_new());
     if (!EVP_PKEY_set1_RSA(evp_pkey.get(), key)) {
-        LOG(ERROR) << "EVP_PKEY_set1_RSA failed";
+       LOG_SAFE(ERROR) << "EVP_PKEY_set1_RSA failed";
         return false;
     }
     auto x509 = GenerateX509Certificate(evp_pkey.get());
@@ -579,7 +579,7 @@ void send_packet(apacket* p, atransport* t) {
     VLOG(TRANSPORT) << dump_packet(t->name.c_str(), "to remote", p);
 
     if (t == nullptr) {
-        LOG(FATAL) << "Transport is null";
+       LOG_SAFE(FATAL) << "Transport is null";
     }
 
     if (t->Write(p) != 0) {
@@ -683,7 +683,7 @@ static void device_tracker_ready(asocket* socket) {
 
 asocket* create_device_tracker(TrackerOutputType output_type) {
     device_tracker* tracker = new device_tracker();
-    if (tracker == nullptr) LOG(FATAL) << "cannot allocate device tracker";
+    if (tracker == nullptr)LOG_SAFE(FATAL) << "cannot allocate device tracker";
 
     D("device tracker %p created", tracker);
 
@@ -1062,14 +1062,14 @@ int atransport::Write(apacket* p) {
 
 void atransport::Reset() {
     if (!kicked_.exchange(true)) {
-        LOG(INFO) << "resetting transport " << this << " " << this->name;
+       LOG_SAFE(INFO) << "resetting transport " << this << " " << this->name;
         this->connection()->Reset();
     }
 }
 
 void atransport::Kick() {
     if (!kicked_.exchange(true)) {
-        LOG(INFO) << "kicking transport " << this << " " << this->name;
+       LOG_SAFE(INFO) << "kicking transport " << this << " " << this->name;
         this->connection()->Stop();
     }
 }
@@ -1168,7 +1168,7 @@ bool atransport::HandleRead(std::unique_ptr<apacket> p) {
 }
 
 void atransport::HandleError(const std::string& error) {
-    LOG(INFO) << serial_name() << ": connection terminated: " << error;
+   LOG_SAFE(INFO) << serial_name() << ": connection terminated: " << error;
     fdevent_run_on_looper([this]() {
         handle_offline(this);
         transport_destroy(this);
@@ -1675,7 +1675,7 @@ void atransport::UpdateReverseConfig(std::string_view service_addr) {
 
         if (norebind && reverse_forwards_.contains(remote)) {
             // This will fail, don't update the map.
-            LOG(DEBUG) << "ignoring reverse forward that will fail due to norebind";
+           LOG_SAFE(DEBUG) << "ignoring reverse forward that will fail due to norebind";
             return;
         }
 
@@ -1691,9 +1691,9 @@ void atransport::UpdateReverseConfig(std::string_view service_addr) {
     } else if (service_addr == "killforward-all") {
         reverse_forwards_.clear();
     } else if (service_addr == "list-forward") {
-        LOG(DEBUG) << __func__ << " ignoring --list";
+       LOG_SAFE(DEBUG) << __func__ << " ignoring --list";
     } else {  // Anything else we need to know about?
-        LOG(FATAL) << "unhandled reverse service: " << service_addr;
+       LOG_SAFE(FATAL) << "unhandled reverse service: " << service_addr;
     }
 }
 
diff --git a/vendor/adb/transport_benchmark.cpp b/vendor/adb/transport_benchmark.cpp
index 022808f..1ade10f 100644
--- a/vendor/adb/transport_benchmark.cpp
+++ b/vendor/adb/transport_benchmark.cpp
@@ -55,7 +55,7 @@ template <typename ConnectionType>
 void BM_Connection_Unidirectional(benchmark::State& state) {
     int fds[2];
     if (adb_socketpair(fds) != 0) {
-        LOG(FATAL) << "failed to create socketpair";
+       LOG_SAFE(FATAL) << "failed to create socketpair";
     }
 
     auto client = MakeConnection<ConnectionType>(unique_fd(fds[0]));
@@ -70,9 +70,9 @@ void BM_Connection_Unidirectional(benchmark::State& state) {
     });
 
     client->SetErrorCallback(
-        [](Connection*, const std::string& error) { LOG(INFO) << "client closed: " << error; });
+        [](Connection*, const std::string& error) {LOG_SAFE(INFO) << "client closed: " << error; });
     server->SetErrorCallback(
-        [](Connection*, const std::string& error) { LOG(INFO) << "server closed: " << error; });
+        [](Connection*, const std::string& error) {LOG_SAFE(INFO) << "server closed: " << error; });
 
     client->Start();
     server->Start();
@@ -110,7 +110,7 @@ template <typename ConnectionType, enum ThreadPolicy Policy>
 void BM_Connection_Echo(benchmark::State& state) {
     int fds[2];
     if (adb_socketpair(fds) != 0) {
-        LOG(FATAL) << "failed to create socketpair";
+       LOG_SAFE(FATAL) << "failed to create socketpair";
     }
 
     auto client = MakeConnection<ConnectionType>(unique_fd(fds[0]));
@@ -144,9 +144,9 @@ void BM_Connection_Echo(benchmark::State& state) {
     });
 
     client->SetErrorCallback(
-        [](Connection*, const std::string& error) { LOG(INFO) << "client closed: " << error; });
+        [](Connection*, const std::string& error) {LOG_SAFE(INFO) << "client closed: " << error; });
     server->SetErrorCallback(
-        [](Connection*, const std::string& error) { LOG(INFO) << "server closed: " << error; });
+        [](Connection*, const std::string& error) {LOG_SAFE(INFO) << "server closed: " << error; });
 
     client->Start();
     server->Start();
diff --git a/vendor/adb/transport_fd.cpp b/vendor/adb/transport_fd.cpp
index d26adc5..843fa53 100644
--- a/vendor/adb/transport_fd.cpp
+++ b/vendor/adb/transport_fd.cpp
@@ -79,7 +79,7 @@ struct NonblockingFdConnection : public Connection {
                 *error = android::base::StringPrintf("poll failed: %s", strerror(errno));
                 return;
             } else if (rc == 0) {
-                LOG(FATAL) << "poll timed out with an infinite timeout?";
+               LOG_SAFE(FATAL) << "poll timed out with an infinite timeout?";
             }
 
             if (pfds[0].revents) {
@@ -139,7 +139,7 @@ struct NonblockingFdConnection : public Connection {
 
     bool Start() override final {
         if (started_.exchange(true)) {
-            LOG(FATAL) << "Connection started multiple times?";
+           LOG_SAFE(FATAL) << "Connection started multiple times?";
         }
 
         thread_ = std::thread([this]() {
@@ -157,14 +157,14 @@ struct NonblockingFdConnection : public Connection {
     }
 
     bool DoTlsHandshake(RSA* key, std::string* auth_key) override final {
-        LOG(FATAL) << "Not supported yet";
+       LOG_SAFE(FATAL) << "Not supported yet";
         return false;
     }
 
     void WakeThread() {
         uint64_t buf = 0;
         if (TEMP_FAILURE_RETRY(adb_write(wake_fd_write_.get(), &buf, sizeof(buf))) != sizeof(buf)) {
-            LOG(FATAL) << "failed to wake up thread";
+           LOG_SAFE(FATAL) << "failed to wake up thread";
         }
     }
 
diff --git a/vendor/libbase/include/android-base/logging.h b/vendor/libbase/include/android-base/logging.h
index 66b6fd4..e119065 100644
--- a/vendor/libbase/include/android-base/logging.h
+++ b/vendor/libbase/include/android-base/logging.h
@@ -230,6 +230,7 @@ struct LogAbortAfterFullExpr {
 //
 //     LOG(FATAL) << "We didn't expect to reach here";
 #define LOG(severity) LOGGING_PREAMBLE(severity) && LOG_STREAM(severity)
+#define LOG_SAFE(severity) LOGGING_PREAMBLE(severity) && LOG_STREAM(severity)
 
 // Conditionally logs a message based on a specified severity level and a boolean condition.
 // Logs to logcat on Android, or to stderr on host. See also LOG(severity) above.
