# yaml-language-server: $schema=/usr/share/ypkg/schema/schema.json
name       : apr-util
version    : 1.6.5
release    : 22
source     :
    - https://dlcdn.apache.org//apr/apr-util-1.6.5.tar.bz2 : 96de1dd6f6a0476d2d2e7964926d8c1ddc3bb0e210e1b1812d3ba5a454a392e2
homepage   : https://apr.apache.org/
license    : Apache-2.0
component  : programming
summary    : apr-util provides a number of abstractions for Apache apr
description: |
    apr-util provides a number of abstractions for Apache apr
builddeps  :
    - pkgconfig(apr-1)
    - pkgconfig(expat)
    - pkgconfig(ldap)
    - pkgconfig(libpq)
    - pkgconfig(libxcrypt)
    - pkgconfig(nss)
    - pkgconfig(openssl)
    - pkgconfig(sqlite3)
    - db5-devel
    - gdbm-devel
setup      : |
    %patch -p1 -i ${pkgfiles}/0001-Prevent-broken-test-from-running.patch
    %patch -p1 -i ${pkgfiles}/disable-failing-nss-tests.patch
    %configure \
        --with-apr=/usr \
        --with-crypto \
        --with-gdbm=/usr \
        --with-ldap \
        --with-nss=/usr \
        --with-openssl=/usr \
        --with-pgsql=/usr \
        --with-sqlite3=/usr \
build      : |
    %make
install    : |
    %make_install
    %install_license LICENSE
check      : |
    %make check -j1
