name       : avidemux
version    : 2.7.3
release    : 21
source     :
    - https://github.com/mean00/avidemux2/archive/2.7.3.tar.gz : aeb4275086577d7a37d6f62fded1fc3dd394f8b4f1a22d3bf64d0740639eba3c
license    : GPL-2.0-or-later
component  : multimedia.video
libsplit   : no
summary    : Avidemux is a simple platform video editor
description: |
    Avidemux is a simple platform video editor
builddeps  :
    - pkgconfig(Qt5Designer)
    - pkgconfig(fdk-aac)
    - pkgconfig(ffnvcodec)
    - pkgconfig(glu)
    - pkgconfig(jack)
    - pkgconfig(libass)
    - pkgconfig(libavcodec)
    - pkgconfig(libpulse)
    - pkgconfig(libxslt)
    - pkgconfig(libva)
    - pkgconfig(opus)
    - pkgconfig(sqlite3)
    - pkgconfig(twolame)
    - pkgconfig(vapoursynth)
    - pkgconfig(vorbis)
    - pkgconfig(vdpau)
    - pkgconfig(vpx)
    - pkgconfig(x264)
    - pkgconfig(x265)
    - pkgconfig(xv)
    - faad-devel
    - lame-devel
    - liba52dec-devel
    - libmad-devel
    - yasm
setup      : |
    chmod +x bootStrap.bash
build      : |
    ./bootStrap.bash --with-core --without-cli --with-plugins --rebuild
install    : |
    make -C buildCore DESTDIR=$installdir install
    make -C buildPluginsCommon DESTDIR=$installdir install
    make -C buildQt5 DESTDIR=$installdir install
    make -C buildPluginsQt5 DESTDIR=$installdir install
