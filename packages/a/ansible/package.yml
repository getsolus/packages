name       : ansible
version    : 2.5.5
release    : 12
source     :
    - https://github.com/ansible/ansible/archive/v2.5.5.tar.gz : 4fd01af1ee6f6ca7e8ea148fc40aae2f9007a4bcf30fc80710ab41eef61d8cac
license    : GPL-3.0-or-later
component  : system.utils
summary    : Ansible is a radically simple IT automation system
description: |
    Ansible is a radically simple IT automation system
builddeps  :
    - pkgconfig(python-2.7)
    - asciidoc
    - python-jinja
    - python-setuptools
rundeps    :
    - python-jinja
    - python-paramiko
    - pyyaml
build      : |
    %python_setup
    %make docs
install    : |
    %python_install
    install -d $installdir/usr/share/man/man1
    install -Dm 00644 docs/man/man1/*.1 $installdir/usr/share/man/man1/
    install -Dm 00644 examples/ansible.cfg $installdir/etc/ansible/ansible.cfg
    install -Dm 00644 examples/hosts $installdir/etc/ansible/hosts
