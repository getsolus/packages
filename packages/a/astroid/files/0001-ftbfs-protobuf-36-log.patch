---
 src/astroid.hh                                     | 3 +++
 src/modes/thread_view/page_client.hh               | 5 +++++
 src/modes/thread_view/webextension/ae_protocol.hh  | 5 +++++
 src/modes/thread_view/webextension/tvextension.hh  | 5 +++++
 4 files changed, 18 insertions(+)

diff --git a/src/astroid.hh b/src/astroid.hh
index 1111111..2222222 100644
--- a/src/astroid.hh
+++ b/src/astroid.hh
@@ -9,6 +9,9 @@
 # include <boost/program_options.hpp>
 # include <boost/log/trivial.hpp>
 
+# ifdef LOG
+#  undef LOG
+# endif
 # define LOG(x) BOOST_LOG_TRIVIAL(x)
 # define warn warning
 
diff --git a/src/modes/thread_view/page_client.hh b/src/modes/thread_view/page_client.hh
index 1111111..2222222 100644
--- a/src/modes/thread_view/page_client.hh
+++ b/src/modes/thread_view/page_client.hh
@@ -13,6 +13,11 @@
 
 # include "messages.pb.h"
 
+/* protobuf 36+ includes absl/log/log.h, which redefines LOG(). Restore Boost.Log. */
+# undef LOG
+# define LOG(x) BOOST_LOG_TRIVIAL(x)
+# define warn warning
+
 namespace Astroid {
   extern "C" void PageClient_init_web_extensions (
       WebKitWebContext *,
diff --git a/src/modes/thread_view/webextension/ae_protocol.hh b/src/modes/thread_view/webextension/ae_protocol.hh
index 1111111..2222222 100644
--- a/src/modes/thread_view/webextension/ae_protocol.hh
+++ b/src/modes/thread_view/webextension/ae_protocol.hh
@@ -5,6 +5,11 @@
 
 # include "messages.pb.h"
 
+/* protobuf 36+ includes absl/log/log.h, which redefines LOG(). Restore Boost.Log. */
+# undef LOG
+# define LOG(x) BOOST_LOG_TRIVIAL(x)
+# define warn warning
+
 namespace Astroid {
   class AeProtocol {
     public:
diff --git a/src/modes/thread_view/webextension/tvextension.hh b/src/modes/thread_view/webextension/tvextension.hh
index 1111111..2222222 100644
--- a/src/modes/thread_view/webextension/tvextension.hh
+++ b/src/modes/thread_view/webextension/tvextension.hh
@@ -12,6 +12,11 @@
 
 # include "messages.pb.h"
 
+/* protobuf 36+ includes absl/log/log.h, which redefines LOG(). Restore Boost.Log. */
+# undef LOG
+# define LOG(x) BOOST_LOG_TRIVIAL(x)
+# define warn warning
+
 # define refptr Glib::RefPtr
 typedef Glib::ustring ustring;
 
