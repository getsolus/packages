When updating `yt-dlp` you must look at the source for the pyproject.toml to see the version of `yt-dlp-ejs` required.

Example:

```
    "yt-dlp-ejs==0.5.0",
```

`yt-dlp` will only work with a specific version and they do not make note of this version changing in their changelog.
