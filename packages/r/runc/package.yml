name       : runc
version    : 1.0.0_rc93
release    : 16
source     :
    - https://github.com/opencontainers/runc/archive/v1.0.0-rc93.tar.gz : e42456078d2f76c925cdd656e4f423b918525d8188521de05e893b6bb473a6f8
license    : Apache-2.0
component  : virt
summary    : OCI complaint CLI tool to run containers
description: |
    runc is a CLI tool for spawning and running containers according to the OCI specification.
builddeps  :
    - pkgconfig(libseccomp)
    - golang
setup      : |
    mkdir -p src/github.com/opencontainers/runc
    find ./* -prune ! -name src -exec mv {} src/github.com/opencontainers/runc/ \;
environment: |
    export GOPATH="`pwd`"
build      : |
    %make -C src/github.com/opencontainers/runc
install    : |
    install -Dm00755 src/github.com/opencontainers/runc/runc $installdir/usr/bin/runc
