name       : runc
version    : 1.0.0_rc2
release    : 4
source     :
    - git|https://github.com/opencontainers/runc.git : 51371867a01c467f08af739783b8beafc154c4d7
license    : Apache-2.0
component  : virt
summary    : OCI complaint CLI tool to run containers
strip      : no
description: |
    runc is a CLI tool for spawning and running containers according to the OCI specification.
builddeps  :
    - golang
    - pkgconfig(libseccomp)
build      : |
    mkdir -p src/github.com/opencontainers
    ln -s `pwd` src/github.com/opencontainers/runc
    export GOPATH="`pwd`:%libdir%/golang"
    %make LDFLAGS=
install    : |
    install -D -m 00755 runc $installdir/usr/bin/runc
