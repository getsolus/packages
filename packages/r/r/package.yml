name       : r
version    : 3.4.3
release    : 22
source     :
    - https://cran.r-project.org/src/base/R-3/R-3.4.3.tar.gz : 7a3cb831de5b4151e1f890113ed207527b7d4b16df9ec6b35e0964170007f426
license    : GPL-2.0
component  : programming
summary    : R is a system for statistical computation and graphics.
description: |
    R is a system for statistical computation and graphics.
optimize   : speed
patterns   :
    - /usr/include/R
    - /usr/lib64/pkgconfig
builddeps  :
    - pkgconfig(cairo-gl)
    - pkgconfig(icu-io)
    - pkgconfig(libcurl)
    - pkgconfig(liblzma)
    - pkgconfig(libpng)
    - pkgconfig(libtiff-4)
    - pkgconfig(pangocairo)
    - pkgconfig(x11)
    - pkgconfig(xmu)
    - pkgconfig(zlib)
    - bzip2-devel
    - gfortran
    - libiconv-devel
    - libjpeg-turbo-devel
    - openblas-devel
    - readline-devel
    - xdg-utils
setup      : |
    unset LD_AS_NEEDED
    %configure \
        --enable-R-shlib \
        --with-blas="-lopenblas" \
        rdocdir=/usr/share/doc/R \
        rincludedir=/usr/include/R \
        rsharedir=/usr/share/R
build      : |
    unset LD_AS_NEEDED
    %make
install    : |
    %make_install
check      : |
    %make check
