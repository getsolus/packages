name       : rust
version    : 1.14.0
release    : 26
source     :
    - https://static.rust-lang.org/dist/rustc-1.14.0-src.tar.gz : c790edd2e915bd01bea46122af2942108479a2fda9a6f76d1094add520ac3b6b
license    :
    - Apache 2.0
    - MIT
summary    : Rust is a systems programming language that runs blazingly fast, prevents nearly all segfaults, and guarantees thread safety.
description: |
    Rust is a systems programming language that runs blazingly fast, prevents nearly all segfaults, and guarantees thread safety.
component  : programming
builddeps  :
    - llvm-devel
    - clang
    - rust
    - cargo
rundeps    :
    - binutils
    - glibc-devel
patterns   :
    - [/usr/bin/*, /usr/lib/*, /usr/share/bash-completion/*, /usr/share/man/*]
    - docs : [/usr/share/doc/*]
libsplit   : no
setup      : |
    export CFLAGS="$CFLAGS -Wno-misleading-indentation -Wno-error=misleading-indentation"
    ./configure --prefix=/usr \
                --libdir=%libdir% \
                --sysconfdir=/etc \
                --datadir=/usr/share \
                --llvm-root=/usr \
                --release-channel=stable
                
build      : |
    export CFLAGS="$CFLAGS -Wno-misleading-indentation -Wno-error=misleading-indentation"
    %make
install    : |
    %make_install
