name       : imake
version    : 1.0.8
release    : 9
source     :
    - https://www.x.org/releases/individual/util/imake-1.0.8.tar.gz : 8178a09bfef33ad5f61cb5cb62283df7d3a5682f014507d2e7cfd922485a5c00
license    : MIT
component  : programming
summary    : Imake is used to generate Makefiles from a template, a set of cpp macro functions, and a per-directory input file called an Imakefile.
description: |
    Imake is used to generate Makefiles from a template, a set of cpp macro functions, and a per-directory input file called an Imakefile.
builddeps  :
    - pkgconfig(xproto)
rundeps    :
    - xorg-cf-files
setup      : |
    %configure
build      : |
    %make
install    : |
    %make_install
