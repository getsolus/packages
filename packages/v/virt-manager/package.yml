name       : virt-manager
version    : 1.4.2
release    : 8
source     :
    - https://virt-manager.org/download/sources/virt-manager/virt-manager-1.4.2.tar.gz : 43e440bb099facf59b59c27c2fc4eb2c42ef0d4ed8d67d93c9e3d98538b6d574
license    : GPL-2.0
summary    : Manage virtual machines with virt-manager
component  : virt
description: |
    The virt-manager application is a desktop user interface for managing virtual machines through libvirt. It primarily targets KVM VMs, but also manages Xen and LXC (linux containers). It presents a summary view of running domains, their live performance & resource utilization statistics. Wizards enable the creation of new domains, and configuration & adjustment of a domain’s resource allocation & virtual hardware. An embedded VNC and SPICE client viewer presents a full graphical console to the guest domain.
rundeps    :
    - spice-gtk
    - python-ipaddr
    - libosinfo
    - libvirt-python
    - libvirt-glib
    - gtk-vnc
    - qemu
builddeps  :
    - pkgconfig(gtk-vnc-2.0)
    - pkgconfig(librsvg-2.0)
    - pkgconfig(libuser)
    - pkgconfig(libvirt)
    - pkgconfig(libvirt-glib-1.0)
    - pkgconfig(libxml-2.0)
    - pkgconfig(vte-2.91)
    - pkgconfig(yajl)
build      : |
    %python_setup
install    : |
    %python_install
    rm -f $installdir/usr/share/glib-2.0/schemas/gschemas.compiled
    rm -f $installdir/usr/share/icons/hicolor/icon-theme.cache
