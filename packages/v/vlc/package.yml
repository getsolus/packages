name       : vlc
version    : 3.0.0
release    : 49
source     :
    - https://download.videolan.org/pub/videolan/vlc/3.0.0/vlc-3.0.0.tar.xz : 68d587999f50d58df5ca3d69998bba910bdb5a82e5a1a39247179932fae0c19c
license    :
    - LGPL-2.1
    - GPL-2.0
component  : multimedia.video
summary    : Open source multimedia player
description: |
    VLC is a free and open source cross-platform multimedia player and framework that plays most multimedia files as well as DVDs, Audio CDs, VCDs, and various streaming protocols.
builddeps  :
    - pkgconfig(alsa)
    - pkgconfig(avahi-client)
    - pkgconfig(dbus-1)
    - pkgconfig(dcadec)
    - pkgconfig(dvdnav)
    - pkgconfig(flac)
    - pkgconfig(fribidi)
    - pkgconfig(gl)
    - pkgconfig(gnutls)
    - pkgconfig(gstreamer-app-1.0)
    - pkgconfig(gtk+-3.0)
    - pkgconfig(kate)
    - pkgconfig(libaacs)
    - pkgconfig(libass)
    - pkgconfig(libavcodec)
    - pkgconfig(libbluray)
    - pkgconfig(libcddb)
    - pkgconfig(libchromaprint)
    - pkgconfig(libdc1394-2)
    - pkgconfig(libdvbpsi)
    - pkgconfig(libidn)
    - pkgconfig(libmatroska)
    - pkgconfig(libmodplug)
    - pkgconfig(libmpeg2)
    - pkgconfig(libmtp)
    - pkgconfig(libnotify)
    - pkgconfig(libpulse)
    - pkgconfig(librsvg-2.0)
    - pkgconfig(libsecret-1)
    - pkgconfig(libssh2)
    - pkgconfig(libturbojpeg)
    - pkgconfig(libupnp)
    - pkgconfig(libva-drm)
    - pkgconfig(lua-5.3)
    - pkgconfig(opus)
    - pkgconfig(protobuf-lite)
    - pkgconfig(Qt5Svg)
    - pkgconfig(Qt5X11Extras)
    - pkgconfig(samplerate)
    - pkgconfig(SDL_image)
    - pkgconfig(shout)
    - pkgconfig(smbclient)
    - pkgconfig(sndfile)
    - pkgconfig(speexdsp)
    - pkgconfig(taglib)
    - pkgconfig(theora)
    - pkgconfig(twolame)
    - pkgconfig(vdpau)
    - pkgconfig(vorbis)
    - pkgconfig(vpx)
    - pkgconfig(wayland-client)
    - pkgconfig(wayland-protocols)
    - pkgconfig(x264)
    - pkgconfig(x265)
    - pkgconfig(xcb-keysyms)
    - pkgconfig(xext)
    - pkgconfig(xi)
    - pkgconfig(xpm)
    - pkgconfig(xproto)
    - faad-devel
    - liba52dec-devel
    - libgcrypt-devel
    - libmad-devel
    - live-media-devel
setup      : |
    export BUILDCC="$CC"
    export CFLAGS="$CFLAGS -std=c99"
    %patch -p1 < $pkgfiles/lua53_compat.patch
    %configure --disable-rpath \
               --enable-a52 \
               --enable-bluray \
               --enable-dvbpsi \
               --enable-dvdnav \
               --enable-dvdread \
               --enable-faad \
               --enable-fontconfig \
               --enable-gst-decode \
               --enable-kate \
               --enable-libass \
               --enable-libmpeg2 \
               --enable-libva \
               --enable-mad \
               --enable-realrtsp \
               --enable-sftp \
               --enable-shout \
               --enable-twolame \
               --enable-vpx \
               --enable-x264 \
               --enable-x265
build      : |
    %make
install    : |
    %make_install
