name       : deja-dup
version    : 34.2
release    : 4
source     :
    - https://launchpad.net/deja-dup/34/34.2/+download/deja-dup-34.2.tar.xz : ff30732f28233e2422d069f1769a725d4a39c91a279c3bf885c4162fd85818bb
license    : GPL-3.0
component  : desktop
summary    : Déjà Dup backup tool
description: |
    Déjà Dup is a simple backup tool. It hides the complexity of backing up the Right Way (encrypted, off-site, and regular) and uses duplicity as the backend.
builddeps  :
    - pkgconfig(gtk+-3.0)
    - pkgconfig(libnautilus-extension)
    - pkgconfig(libnotify)
    - pkgconfig(libpeas-1.0)
    - pkgconfig(libsecret-1)
    - pkgconfig(packagekit-glib2)
    - itstool
    - vala
rundeps    :
     - duplicity
setup      : |
    %cmake .
build      : |
    %make
install    : |
    %make_install
