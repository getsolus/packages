# yaml-language-server: $schema=/usr/share/ypkg/schema/schema.json
name       : duplicity
version    : 3.2.0.1
release    : 37
source     :
    - https://gitlab.com/duplicity/duplicity/-/archive/rel.3.2.0.1/duplicity-rel.3.2.0.1.tar.gz : b9cad3e560a0576c070bc045af637a3a51e756db0be9ab33edeeb5a613132e55
license    : GPL-2.0-or-later
homepage   : https://gitlab.com/duplicity/duplicity
summary    : Duplicity backs directories by producing encrypted tar-format volumes and uploading them to a remote or local file server
component  : system.utils
description: |
    Duplicity backs directories by producing encrypted tar-format volumes and uploading them to a remote or local file server. Because duplicity uses librsync, the incremental archives are space efficient and only record the parts of files that have changed since the last backup. Because duplicity uses GnuPG to encrypt and/or sign these archives, they will be safe from spying and/or modification by the server.
builddeps  :
    - pkgconfig(python3)
    - librsync-devel
    - python-build
    - python-installer
    - python-setuptools
    - python-setuptools-scm
    - python-wheel
rundeps    :
    - python-fasteners
    - python-future
build      : |
    %pyproject_build --skip-dependency-check
install    : |
    %pyproject_install
    rm -rf $installdir/usr/share/doc
