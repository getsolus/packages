name       : gnupg
version    : 2.2.26
release    : 36
source     :
    - https://gnupg.org/ftp/gcrypt/gnupg/gnupg-2.2.26.tar.bz2 : 517569e6c9fad22175df16be5900f94c991c41e53612db63c14493e814cfff6d
license    : GPL-3.0-or-later
summary    : Complete and free implementation of OpenPGP
component  : security
description: |
    GnuPG is a complete and free implementation of OpenPGP standard as defined by RFC4880. It allows encryption and signing of data and communications, a key management system, and access modules for public key directories.
builddeps  :
    - pkgconfig(gnutls)
    - pkgconfig(ksba)
    - pkgconfig(libassuan)
    - pkgconfig(libgcrypt)
    - pkgconfig(sqlite3)
    - bzip2-devel
    - libgpg-error-devel
    - npth-devel
    - pinentry
rundeps    :
    - pinentry
setup      : |
    %configure \
    --enable-symcryptrun \
    --enable-gpgtar \
    --enable-all-tests
build      : |
    %make
install    : |
    %make_install DESTDIR=$installdir
    ln -s /usr/bin/gpg $installdir/usr/bin/gpg2
check      : |
    # If tests last too long, you can remove --enable-all-tests
    %make check
