# yaml-language-server: $schema=/usr/share/ypkg/schema/schema.json
name       : gssdp
version    : 1.6.6
release    : 25
source     :
    - https://download.gnome.org/sources/gssdp/1.6/gssdp-1.6.6.tar.xz : 767d2275254ce0efeaeac64419ff9f4f0ad470d134ef672f5c556b2abb786bcb
homepage   : https://gitlab.gnome.org/GNOME/gssdp
license    : LGPL-2.0-only
component  : network.util
summary    : A GObject-based API for handling resource discovery and announcement over SSDP.
description: |
    A GObject-based API for handling resource discovery and announcement over SSDP.
builddeps  :
    - pkgconfig(gi-docgen)
    - pkgconfig(gtk4)
    - pkgconfig(libsoup-3.0)
    - pandoc
    - vala
setup      : |
    %meson_configure
build      : |
    %ninja_build
install    : |
    %ninja_install
# Horiffic workaround otherwise we'd have a cyclic dependency which eopkg can't handle yet
autodep    : false
