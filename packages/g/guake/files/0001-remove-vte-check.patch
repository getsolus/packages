From: Shawn W Dunn <sfalken@cloverleaf-linux.org>
Date: Mon, 27 Jun 2016
Subject: [PATCH] Remove vte check in configure.ac
See:  http://pkgs.fedoraproject.org/cgit/rpms/guake.git/tree/0001-Remove-vte-check-in-the-configure.ac.patch


diff -rupN guake-0.8.5.old/configure.ac guake-0.8.5/configure.ac
--- guake-0.8.5.old/configure.ac	2016-06-06 03:29:17.000000000 -0700
+++ guake-0.8.5/configure.ac	2016-06-27 21:05:03.836986584 -0700
@@ -55,22 +55,6 @@ AS_AC_EXPAND(LIBDIR, ${libdir})
 AS_AC_EXPAND(BINDIR, ${bindir})
 AS_AC_EXPAND(DATADIR, ${datarootdir})
 
-dnl Checking python-vte
-AC_MSG_CHECKING([for the python-vte package])
-ac_pvte_result=`$PYTHON -c 'try:
-    import vte
-except Exception, e:
-    print str(e)' 2> /dev/null`
-
-if test -z "$ac_pvte_result"; then
-    AC_MSG_RESULT([yes])
-else
-    AC_MSG_RESULT([no])
-    AC_MSG_ERROR([cannot import Python module "vte".
-    Please check if you have python-vte installed. The error was:
-    $ac_pvte_result])
-fi
-
 dnl gconf
 AC_PATH_PROG(GCONFTOOL, gconftool-2, no)
 if test x"$GCONFTOOL" = xno; then
