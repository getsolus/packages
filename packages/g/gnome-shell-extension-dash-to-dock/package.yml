name       : gnome-shell-extension-dash-to-dock
version    : '67'
release    : 14
source     :
    - git|https://github.com/3v1n0/dash-to-dock.git : 800dc0abd65323f158b33a6c883329c036bb7bcb
license    : GPL-2.0-only
component  : desktop.gnome
summary    : A dock for the Gnome Shell.
description: |
    A dock for the Gnome Shell. This extension moves the dash out of the overview transforming it in a dock for an easier launching of applications and a faster switching between windows and desktops.
rundeps    :
    - gnome-shell
    - gnome-shell-extensions
setup      : |
    %patch -p1 < $pkgfiles/0001-Padding-tweaks.patch
    %patch -p1 < $pkgfiles/0002-Disable-trash-and-mounts-by-default.patch
    %patch -p1 < $pkgfiles/docking-Delay-hide-requests-if-were-currently-showing.patch
build      : |
    %make
install    : |
    install -dm00755 $installdir/usr/share/glib-2.0/schemas/
    install -Dm00644 schemas/org.gnome.shell.extensions.dash-to-dock.gschema.xml $installdir/usr/share/glib-2.0/schemas/org.gnome.shell.extensions.dash-to-dock.gschema.xml
    %make_install
