name       : gstreamer-1.0-plugins-base
version    : 1.14.1
release    : 22
source     :
    - https://gstreamer.freedesktop.org/src/gst-plugins-base/gst-plugins-base-1.14.1.tar.xz : 1026c7c3082d825d9b5d034c1a6dd8a4ebab60eb3738b0a0afde4ad2dc0b0db5
license    : LGPL-2.0-or-later
component  : multimedia.gstreamer
summary    : GStreamer base plug-ins and elements
description: |
    The GStreamer Base Plug-ins is a well-groomed and well-maintained collection of GStreamer plug-ins and elements, spanning the range of possible types of elements one would want to write for GStreamer. You will need at least one of Good, Bad, Ugly or Libav plugins for GStreamer applications to function properly.
builddeps  :
    - pkgconfig(alsa)
    - pkgconfig(cairo)
    - pkgconfig(gobject-introspection-1.0)
    - pkgconfig(gstreamer-1.0)
    - pkgconfig(ogg)
    - pkgconfig(opus)
    - pkgconfig(orc-0.4)
    - pkgconfig(pangocairo)
    - pkgconfig(theora)
    - pkgconfig(vorbis)
    - pkgconfig(x11)
    - pkgconfig(xext)
    - pkgconfig(xv)
setup      : |
    %configure --disable-static \
               --enable-experimental \
               --with-package-name="GStreamer Base Plugins ${version} Solus" \
               --with-package-origin="https://solus-project.com"
build      : |
    %make
install    : |
    %make_install
check      : |
    %make check || :
