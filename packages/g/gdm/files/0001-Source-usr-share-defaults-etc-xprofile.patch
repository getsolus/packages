From faf965f905b73f2e885cdee1e2c16b5702ba37d9 Mon Sep 17 00:00:00 2001
From: Joshua Strobl <joshua@streambits.io>
Date: Fri, 17 Jul 2020 14:01:42 +0300
Subject: [PATCH 1/1] Source /usr/share/defaults/etc/xprofile

---
 data/Xsession.in | 4 +++-
 1 file changed, 3 insertions(+), 1 deletion(-)

diff --git a/data/Xsession.in b/data/Xsession.in
index 9d79558c..084ce606 100755
--- a/data/Xsession.in
+++ b/data/Xsession.in
@@ -32,7 +32,9 @@ echo "$0: Beginning session setup..."
 # First read /etc/profile and .profile
 test -f /etc/profile && . /etc/profile
 test -f "$HOME/.profile" && . "$HOME/.profile"
-# Second read /etc/xprofile and .xprofile for X specific setup
+# Stateless xprofile
+test -f /usr/share/defaults/etc/xprofile && . /usr/share/defaults/etc/xprofile
+# Read /etc/xprofile and .xprofile for X specific setup
 test -f /etc/xprofile && . /etc/xprofile
 test -f "$HOME/.xprofile" && . "$HOME/.xprofile"
 
-- 
2.27.0

