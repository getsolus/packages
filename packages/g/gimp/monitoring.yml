releases:
  id: 1161
  # 2.99.* is the development tag (until version 3 exists)
  ignore: "2.99.*"
  rss: https://gitlab.gnome.org/GNOME/gimp/-/tags?format=atom
security:
  cpe:
    - vendor: gimp
      product: gimp
