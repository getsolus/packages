From 9908a62d23f1c7083f131848019a2a7dba7ceefa Mon Sep 17 00:00:00 2001
From: Campbell Jones <git@serebit.com>
Date: Fri, 8 May 2020 20:14:09 -0400
Subject: [PATCH] add additional terminals to gio

---
 gio/gdesktopappinfo.c | 16 ++++++++++++++++
 1 file changed, 16 insertions(+)

diff --git a/gio/gdesktopappinfo.c b/gio/gdesktopappinfo.c
index ffd2ca317..de4528059 100644
--- a/gio/gdesktopappinfo.c
+++ b/gio/gdesktopappinfo.c
@@ -2565,6 +2565,22 @@ prepend_terminal_to_vector (int    *argc,
         }
       else
         {
+          if (check == NULL)
+            check = g_find_program_in_path ("konsole");
+          if (check == NULL)
+            check = g_find_program_in_path ("guake");
+          if (check == NULL)
+            check = g_find_program_in_path ("tilix");
+          if (check == NULL)
+            check = g_find_program_in_path ("alacritty");
+          if (check == NULL)
+            check = g_find_program_in_path ("kitty");
+          if (check == NULL)
+            check = g_find_program_in_path ("roxterm");
+          if (check == NULL)
+            check = g_find_program_in_path ("lilyterm");
+          if (check == NULL)
+            check = g_find_program_in_path ("cool-retro-term");
           if (check == NULL)
             check = g_find_program_in_path ("nxterm");
           if (check == NULL)
-- 
2.26.2

