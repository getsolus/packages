name       : glib2
version    : 2.68.2
release    : 71
source     :
    - https://download.gnome.org/sources/glib/2.68/glib-2.68.2.tar.xz : ecc7798a9cc034eabdfd7f246e6dd461cdbf1175fcc2e9867cc7da7b7309e0fb
license    : LGPL-2.1-or-later
summary    : Low level C library containing data structure handling and interfaces
description: |
    GLib is the low-level core library that forms the basis for projects such as GTK+ and GNOME. It provides data structure handling for C, portability wrappers, and interfaces for such runtime functionality as an event loop, threads, dynamic loading, and an object system.
component  : system.base
emul32     : yes
builddeps  :
    - pkgconfig32(dbus-1)
    - pkgconfig32(libelf)
    - pkgconfig32(libffi)
    - pkgconfig32(liblzma)
    - pkgconfig32(libpcre)
    - pkgconfig32(mount)
    - pkgconfig32(zlib)
    - libgcrypt-32bit-devel
setup      : |
    %patch -p1 < $pkgfiles/0001-Support-stateless-XDG-and-snap-directories.patch
    %patch -p1 < $pkgfiles/0001-add-additional-terminals-to-gio.patch
    %meson_configure -Dgtk_doc=false -Dtests=false
build      : |
    %ninja_build
install    : |
    %ninja_install
    # Allow emul32 to work correctly.
    install -Dm00644 $pkgfiles/glibconfig.h $installdir/usr/include/glib-2.0/glibconfig.h
