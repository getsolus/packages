name       : git-lfs
version    : 2.0.2
release    : 4
source     :
    - https://github.com/git-lfs/git-lfs/archive/v2.0.2.tar.gz : e266bdffa53e947ba1d0bf8944d73029384bad2ed05af92bc10918d07eec6b63
homepage   : https://git-lfs.github.com
license    : MIT
component  : programming.tools
summary    : Git extension for versioning large files
description: |
    Git LFS is a command line extension for managing large files with Git
networking : yes
builddeps  :
    - golang
    - ruby-devel
rundeps    :
    - git
setup      : |
    export GEM_HOME=$workdir/gems
    gem install ronn
build      : |
    export GEM_PATH=$workdir/gems
    export PATH=$PATH:$GEM_PATH/bin
    ./script/man
    ./script/bootstrap
install    : |
    install -Dm755 bin/git-lfs $installdir/usr/bin/git-lfs
    install -dm644 $installdir/usr/share/man/man1
    cp man/*.1 $installdir/usr/share/man/man1
