# yaml-language-server: $schema=/usr/share/ypkg/schema/schema.json
name       : xed
version    : 3.8.5
release    : 7
source     :
    - https://github.com/linuxmint/xed/archive/refs/tags/3.8.5.tar.gz : 3594a455982c4dfd9d9d969e456ce021ececcba45e1035c8bbafd2433835b83c
homepage   : https://github.com/linuxmint/xed
license    : GPL-2.0-or-later
component  : desktop
summary    : X-Apps Text Editor (Cross-DE, backward-compatible, GTK3, traditional UI)
description: |
    xed is a small and lightweight text editor.

    xed supports most standard editing features, plus several not found in your average text editor (plugins being the most notable of these).
builddeps  :
    - pkgconfig(gspell-1)
    - pkgconfig(gtk+-3.0)
    - pkgconfig(gtksourceview-4)
    - pkgconfig(libgnomekbdui)
    - pkgconfig(libpeas-1.0)
    - pkgconfig(xapp)
    - pkgconfig(xkbfile)
    - itstool
setup      : |
    %patch -p1 -i $pkgfiles/libpeas_libgirepository2.patch

    %meson_configure
build      : |
    %ninja_build
install    : |
    %ninja_install
