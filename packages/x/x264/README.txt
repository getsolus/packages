# Notes on updating x264

- The project doesn't tag releases, they number commits monotonically
- This page shows the "version" (commit number), along with the abbreviated commit hash, and date.  https://artifacts.videolan.org/x264/release-debian-amd64/
- You can use the commit hash to update the package.yml source
- You can also run version.sh, which will return the version
