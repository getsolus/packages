name       : unison
version    : 2.51.2
release    : 3
source     :
    - https://github.com/bcpierce00/unison/archive/v2.51.2.tar.gz : a2efcbeab651be6df69cc9b253011a07955ecb91fb407a219719451197849d5e
license    : GPL-3.0-or-later
component  : network.util
summary    : Unison is a file-synchronization tool for Unix and Windows.
description: |
    Unison is a file-synchronization tool for Unix and Windows. It allows two replicas of a collection of files and directories to be stored on different hosts (or different disks on the same host), modified separately, and then brought up to date by propagating the changes in each replica to the other.
builddeps  :
    - ocaml
build      : |
    CFLAGS=""
    make -j1
install    : |
    install -d $installdir/usr/bin/
    install -m 00755 src/unison $installdir/usr/bin/unison
    install -m 00755 src/unison-fsmonitor $installdir/usr/bin/unison-fsmonitor
