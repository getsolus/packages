name       : terraform
version    : 0.12.3
release    : 7
source     :
    - https://github.com/hashicorp/terraform/archive/v0.12.3.tar.gz : 7114326641fd5b1ab52d0d3e55a876fdc2bbc5e6869b25b291503faa68c875be
license    : MPL-2.0
component  : programming.tools
summary    : Terraform is a tool for building, changing, and combining infrastructure safely and efficiently.
description: |
    Terraform is a tool for building, changing, and versioning infrastructure safely and efficiently.
    Terraform can manage existing and popular service providers as well as custom in-house solutions.
builddeps  :
    - golang
build      : |
    mkdir -p src/github.com/hashicorp
    export GOPATH=`pwd`
    ln -sv `pwd` src/github.com/hashicorp/terraform
    cd src/github.com/hashicorp/terraform
    go build -o terraform-binary
install    : |
    cd src/github.com/hashicorp/terraform
    install -Dm 00755 terraform-binary $installdir/usr/bin/terraform
