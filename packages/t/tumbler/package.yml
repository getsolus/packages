name       : tumbler
version    : 0.2.7
release    : 14
source     :
    - https://archive.xfce.org/src/xfce/tumbler/0.2/tumbler-0.2.7.tar.bz2 : f704c35f16716cbee3c94883cee2a8d865e9cf2535d0e84f57e1c8cce00314e4
license    :
    - GPL-2.0-or-later
    - LGPL-2.0-or-later
component  : multimedia.library
summary    : D-Bus service for applications to request thumbnails
description: |
    The Tumbler package contains a D-Bus thumbnailing service based on the thumbnail management D-Bus specification. This is useful for generating thumbnail images of files.
builddeps  :
    - pkgconfig(gdk-pixbuf-2.0)
    - pkgconfig(libffmpegthumbnailer)
    - pkgconfig(libgsf-1)
    - pkgconfig(libturbojpeg)
    - pkgconfig(poppler-glib)
setup      : |
    %configure \
        --disable-static \
        --disable-debug \
        --disable-gstreamer-thumbnailer
build      : |
    %make
install    : |
    %make_install
