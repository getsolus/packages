# yaml-language-server: $schema=/usr/share/ypkg/schema/schema.json
name       : mutt
version    : 2.4.1
release    : 38
source     :
    - https://gitlab.com/muttmua/mutt/-/archive/mutt-2-4-1-rel/mutt-mutt-2-4-1-rel.tar.gz : 9ae090bddeecf5692d3b2ae47fb76dac72b14b9c6955c735deb863dc2c2bef43
homepage   : https://gitlab.com/muttmua/mutt
license    : GPL-2.0-or-later
component  : network.mail
summary    : Mutt (Small, powerful text based email client)
description: |
    Mutt is a small but powerful text-based mail client for Unix operating systems. It was originally based on ELM, and was written by Michael Elkins.
builddeps  :
    - pkgconfig(gpgme)
    - pkgconfig(libassuan)
    - pkgconfig(libsasl2)
    - pkgconfig(ncursesw)
    - gdbm-devel
    - gnupg
rundeps    :
    - vim
setup      : |
    %reconfigure \
        --sysconfdir=/etc/mutt \
        --enable-compressed \
        --enable-gpgme \
        --enable-hcache \
        --enable-imap \
        --enable-locales-fix \
        --enable-pop \
        --enable-sidebar \
        --enable-smtp \
        --with-curses \
        --with-homespool=/var/spool/ \
        --with-regex \
        --with-sasl \
        --with-ssl
build      : |
    %make
install    : |
    %make_install
    %install_license GPL
