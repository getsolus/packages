name       : man-db
version    : 2.8.5
release    : 16
source     :
    - https://download.savannah.gnu.org/releases/man-db/man-db-2.8.5.tar.xz : b64d52747534f1fe873b2876eb7f01319985309d5d7da319d2bc52ba1e73f6c1
license    : GPL-3.0-or-later
component  : system.utils
summary    : Application to read Linux man pages
description: |
    man-db is an implementation of the standard Unix documentation system accessed using the man command. It uses a Berkeley DB database in place of the traditional flat-text whatis databases.
builddeps  :
    - pkgconfig(libpipeline)
    - pkgconfig(zlib)
    - gdbm-devel
    - groff
rundeps    :
    - groff
setup      : |
    %patch -p1 < $pkgfiles/stateless.patch

    %reconfigure --disable-static \
        --disable-setuid \
        --enable-cache-owner=root
build      : |
    %make
install    : |
    %make_install
check      : |
    %make check
