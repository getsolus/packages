diff --git a/Makefile b/Makefile
index fd0e916..27fdd85 100644
--- a/Makefile
+++ b/Makefile
@@ -34,9 +34,9 @@ else ifneq ($(findstring win,$(shell uname -a)),)
 endif
 endif
 
-ifeq ($(platform), win)
-	LDFLAGS += -liphlpapi -lws2_32 
-endif
+# ifeq ($(platform), win)
+#	LDFLAGS += -liphlpapi -lws2_32 
+# endif
 
 CORE_DIR = .
 export DEPSDIR := $(CURDIR)/
