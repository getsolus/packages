# yaml-language-server: $schema=/usr/share/ypkg/schema/schema.json
name       : mgba
version    : 0.10.5
release    : 60
source     :
    - https://github.com/mgba-emu/mgba/archive/0.10.5.tar.gz : 91d6fbd32abcbdf030d58d3f562de25ebbc9d56040d513ff8e5c19bee9dacf14
homepage   : https://mgba.io/
license    : MPL-2.0
component  : games.emulator
summary    : Game Boy Advance emulator written in C
description: mGBA is a new Game Boy Advance emulator written in C.
builddeps  :
    - pkgconfig(Qt5Multimedia)
    - pkgconfig(Qt5UiTools)
    - pkgconfig(epoxy)
    - pkgconfig(libavcodec)
    - pkgconfig(libedit)
    - pkgconfig(libelf)
    - pkgconfig(libpng)
    - pkgconfig(libzip)
    - pkgconfig(lua-5.4)
    - pkgconfig(sdl2)
    - pkgconfig(sqlite3)
    - desktop-file-utils
clang      : true
setup      : |
    %cmake_ninja \
        -DCMAKE_POLICY_VERSION_MINIMUM=3.5 \
        -DBUILD_LTO=ON
build      : |
    %ninja_build
install    : |
    %ninja_install
    %install_license LICENSE
    # Install appstream metainfo
    %install_file ${pkgfiles}/io.mgba.mGBA.appdata.xml -t ${installdir}/usr/share/metainfo/
