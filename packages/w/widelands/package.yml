name       : widelands
version    : '1.1'
release    : 25
source     :
    - https://github.com/widelands/widelands/archive/refs/tags/v1.1.tar.gz : 6853fcf3daec9b66005691e5bcb00326634baf0985ad89a7e6511502612f6412
license    : GPL-2.0-or-later
homepage   : https://www.widelands.org/
component  : games.strategy
summary    : Widelands is a free, open source real-time strategy game with singleplayer campaigns and a multiplayer mode
description: |
    Widelands is a free, open source real-time strategy game with singleplayer campaigns and a multiplayer mode.
builddeps  :
    - pkgconfig(SDL2_image)
    - pkgconfig(SDL2_mixer)
    - pkgconfig(SDL2_ttf)
    - pkgconfig(asio)
    - pkgconfig(glew)
    - pkgconfig(harfbuzz)
    - pkgconfig(icu-i18n)
    - pkgconfig(libpng)
    - pkgconfig(minizip)
    - dejavu-fonts-ttf
setup      : |
    %cmake_ninja -DCMAKE_BUILD_TYPE=Release \
                 -DOPTION_BUILD_TESTS=OFF \
                 -DWL_INSTALL_BASEDIR=/usr/share/widelands \
                 -DWL_INSTALL_BINDIR=/usr/bin \
                 -DWL_INSTALL_DATADIR=/usr/share/widelands
build      : |
    %ninja_build
install    : |
    %ninja_install
