name       : wayland
version    : 1.10.0
release    : 16
source     :
    - https://wayland.freedesktop.org/releases/wayland-1.10.0.tar.xz : 4bf6e790aa6f50ab3825676282ecd75850ec9c4767af96ecb7127b1f3c3d60dc
license    :
    - MIT
summary    : Wayland is intended as a simpler replacement for X, easier to develop and maintain.
description: |
    Wayland is a protocol for a compositor to talk to its clients as well as a C library implementation of that protocol. The compositor can be a standalone display server running on Linux kernel modesetting and evdev input devices, an X application, or a wayland client itself. The clients can be traditional applications, X servers (rootless or fullscreen) or other display servers.
builddeps  :
    - pkgconfig32(expat)
    - pkgconfig32(libffi)
    - pkgconfig32(libxml-2.0)
    - glibc-32bit-devel
    - libgcc-32bit
emul32     : yes
component  : desktop.wayland
patterns   :
    - devel :
        - /usr/bin
        - /usr/share/wayland
        - /usr/share/aclocal
setup      : |
    %configure --prefix=/usr \
               --disable-static \
               --disable-documentation
build      : |
    %make
install    : |
    %make_install
    %make clean
