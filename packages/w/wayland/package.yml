name       : wayland
version    : 1.19.0
release    : 24
source     :
    - https://gitlab.freedesktop.org/wayland/wayland/-/archive/1.19.0/wayland-1.19.0.tar.bz2 : a84410f22bd44e993c3285187b1632f93fc9b5ffeb460e8929406c69ae70bdea
license    :
    - MIT
component  : desktop.wayland
emul32     : yes
summary    : Wayland is intended as a simpler replacement for X, easier to develop
    and maintain.
description: |
    Wayland is a protocol for a compositor to talk to its clients as well as a C library implementation of that protocol. The compositor can be a standalone display server running on Linux kernel modesetting and evdev input devices, an X application, or a wayland client itself. The clients can be traditional applications, X servers (rootless or fullscreen) or other display servers.
builddeps  :
    - pkgconfig32(expat)
    - pkgconfig32(libffi)
    - pkgconfig32(libxml-2.0)
patterns   :
    - devel :
        - /usr/bin
        - /usr/share/wayland
        - /usr/share/aclocal
setup      : |
    %meson_configure -Ddocumentation=false
build      : |
    %ninja_build
install    : |
    %ninja_install
