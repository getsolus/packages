name       : ffmpeg
homepage   : https://www.ffmpeg.org/
version    : 2.8.6
release    : 22
source     :
    - http://ffmpeg.org/releases/ffmpeg-2.8.6.tar.xz : 25bcedbdafadac3d09c325c1d46a51f53d858b26a260d5aed6b4f17fea6e07fa
license    : LGPL-3.0
component  : multimedia.codecs
summary    : A complete, cross-platform solution to record, convert and stream audio
    and video.
description: |
    A complete, cross-platform solution to record, convert and stream audio and video.
builddeps  :
    - pkgconfig(alsa)
    - pkgconfig(freetype2)
    - pkgconfig(fribidi)
    - pkgconfig(gl)
    - pkgconfig(gnutls)
    - pkgconfig(libaacs)
    - pkgconfig(libass)
    - pkgconfig(libbluray)
    - pkgconfig(libpulse)
    - pkgconfig(libv4l2)
    - pkgconfig(ogg)
    - pkgconfig(openssl)
    - pkgconfig(sdl)
    - pkgconfig(theora)
    - pkgconfig(twolame)
    - pkgconfig(vdpau)
    - pkgconfig(vorbis)
    - pkgconfig(vpx)
    - pkgconfig(x11)
    - pkgconfig(x264)
    - pkgconfig(x265)
    - pkgconfig(xv)
    - faac-devel
    - lame-devel
    - xvidcore-devel
patterns   :
    - devel : [/usr/share/man/man3*]
setup      : |
    # Revisit to enable All The Things
    ./configure --prefix=/usr \
        --libdir=%libdir% \
        --enable-shared \
        --disable-static \
        --enable-avisynth \
        --enable-avresample \
        --enable-gnutls \
        --enable-gpl \
        --enable-libass \
        --enable-libbluray \
        --enable-libfaac \
        --enable-libfreetype \
        --enable-libmp3lame \
        --enable-libpulse \
        --enable-libtheora \
        --enable-libtwolame \
        --enable-libv4l2 \
        --enable-libvorbis \
        --enable-libvpx \
        --enable-libx264 \
        --enable-libx265 \
        --enable-libxvid \
        --enable-nonfree \
        --enable-opengl \
        --enable-openssl \
        --enable-vdpau \
        --enable-version3 \
        --enable-x11grab
build      : |
    %make
install    : |
    %make_install
