name       : ffmpeg
homepage   : https://www.ffmpeg.org/
version    : 2.8
release    : 6
source     :
    - http://ffmpeg.org/releases/ffmpeg-2.8.tar.xz : 993ca7216aa0eac899a85fe1119dac12ef0655d67f932de6a32acc00188f8182
license    : LGPL-2.0
summary    : A complete, cross-platform solution to record, convert and stream audio and video.
description: |
    A complete, cross-platform solution to record, convert and stream audio and video.
builddeps  :
    - pkgconfig(x264)
    - pkgconfig(vdpau)
    - pkgconfig(freetype2)
    - pkgconfig(ogg)
    - pkgconfig(openssl)
    - pkgconfig(gnutls)
    - pkgconfig(theora)
    - pkgconfig(x11)
    - pkgconfig(vpx)
    - pkgconfig(fribidi)
    - pkgconfig(gl)
setup      : |
    # Revisit to enable All The Things
    ./configure --prefix=/usr \
        --libdir=%libdir% \
        --enable-shared \
        --disable-static \
        --enable-libx264 \
        --enable-libtheora \
        --enable-libfreetype \
        --enable-gpl \
        --enable-x11grab \
        --enable-libvpx \
        --enable-vdpau \
        --enable-opengl \
        --enable-nonfree \
        --enable-openssl \
        --enable-gnutls
build      : |
    %make
install    : |
    %make_install
