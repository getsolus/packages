--- a/src/file.cpp
+++ b/src/file.cpp
@@ -32,6 +32,11 @@
 #include "utils/endian.h"
 #include "utils/memory.h"
 #include "utils/md5.h"
+/* Include zlib.h before minizip so minizip-ng compat headers do not pull in
+ * zlib-ng.h (both APIs cannot be used in the same translation unit). */
+#ifndef WIN32
+#include <zlib.h>
+#endif
 #ifdef _SYSTEM_MINIZIP
 #ifdef __linux
 #include <minizip/unzip.h>
@@ -49,10 +54,6 @@
 #include "driver.h"
 #include "utils/xstring.h"
 
-#ifndef WIN32
-#include <zlib.h>
-#endif
-
 using namespace std;
 
 bool bindSavestate = true;	//Toggle that determines if a savestate filename will include the movie filename
