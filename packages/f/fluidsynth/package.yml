name       : fluidsynth
version    : 1.1.11
release    : 5
source     :
    - https://github.com/FluidSynth/fluidsynth/archive/v1.1.11.tar.gz : da8878ff374d12392eecf87e96bad8711b8e76a154c25a571dd8614d1af80de8
license    : LGPL-2.1-or-later
component  : multimedia.audio
summary    : A real-time software synthesizer based on the SoundFont 2 specifications.
description: |
    A real-time software synthesizer based on the SoundFont 2 specifications. It can be used for live audio synthesis of MIDI events or for rendering MIDI files to disk, using SoundFont instruments. It can also be embedded in other applications.
builddeps  :
    - pkgconfig(alsa)
    - pkgconfig(libpulse-simple)
    - pkgconfig(portaudio-2.0)
    - pkgconfig(sndfile)
rundeps    :
    - ladspa
setup      : |
    mkdir build && pushd build
    %cmake -Denable-portaudio=1 \
           -Denable-ladspa=1 \
           -Denable-jack=0 \
           ..
build      : |
    pushd build
    %make
install    : |
    pushd build
    %make_install
    popd
