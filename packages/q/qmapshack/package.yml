# yaml-language-server: $schema=/usr/share/ypkg/schema/schema.json
name       : qmapshack
version    : 1.20.3
release    : 24
source     :
    - https://github.com/Maproom/qmapshack/archive/refs/tags/V_1.20.3.tar.gz : faae6d10c8aa3fd00efad5f571697aa56ae71543cebbc1a52130ec545a2208b0
homepage   : https://github.com/Maproom/qmapshack
license    : GPL-3.0-or-later
component  : desktop
summary    : GPS mapping and management tool
description: |
    Use QMapShack to plan your next outdoor trip or to visualize and archive all the GPS recordings of your past exciting adventures. QMapShack is the next generation of the famous QLandkarte GT application. And of course it's even better and easier to use.
builddeps  :
    - pkgconfig(Qt6Core5Compat)
    - pkgconfig(Qt6UiTools)
    - pkgconfig(Qt6WebEngineCore)
    - pkgconfig(gdal)
    - pkgconfig(libturbojpeg)
    - pkgconfig(proj)
    - pkgconfig(quazip1-qt6)
    - alglib-devel
    - routino-devel
setup      : |
    %install_file -t ${workdir}/src/qmaptool/pic ${pkgfiles}/line_3px_horizontal.png
    %install_file -t ${workdir}/src/qmaptool/pic ${pkgfiles}/line_3px_vertical.png
    %install_file -t ${workdir}/src/qmaptool/pic ${pkgfiles}/splash.png

    %cmake_ninja
build      : |
    %cmake_build
install    : |
    %cmake_install
    %install_license LICENSE
    %install_file ${pkgfiles}/io.github.maproom.qmapshack.metainfo.xml -t ${installdir}/usr/share/metainfo/
