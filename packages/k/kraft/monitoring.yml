releases:
  id: 10821
  ignore:
    # Tagging continuously is bad, mkay?
    - "Continuous build"
    # We don't care about pre-release versions
    - "Release v.+rc.+"
  rss: https://github.com/dragotin/kraft/tags.atom
# No known CPE, checked 2024-10-03
security:
  cpe: ~
