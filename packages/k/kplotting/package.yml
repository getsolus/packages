name       : kplotting
version    : 5.85.0
release    : 52
source     :
    - https://cdn.download.kde.org/stable/frameworks/5.85/kplotting-5.85.0.tar.xz : aafdcac762a9ce52ba0ea2040a99a919439a043752d7f4b78aa0fce84460134a
homepage   : https://community.kde.org/Frameworks
license    :
    - GPL-2.0.or-later
    - LGPL-2.0-or-later
component  : desktop.library
summary    : Data plotting
description: |
    KPlotting provides KPlotWidget, a QWidget-derived class that provides a virtual base class for easy data-plotting. The idea behind KPlotWidget is that you only have to specify information in "data units"; i.e., the natural units of the data being plotted. KPlotWidget automatically converts everything to screen pixel units.
optimize   : lto
builddeps  :
    - pkgconfig(Qt5Designer)
    - extra-cmake-modules
rundeps    :
    - devel :
        - extra-cmake-modules
        - qt5-tools-devel
setup      : |
    %cmake_ninja -DBUILD_QCH=ON \
                -DBUILD_TESTING=OFF
build      : |
    %ninja_build
install    : |
    %ninja_install
