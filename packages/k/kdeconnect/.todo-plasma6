# See if we still need that vcard package
# See if we need this:
-- The following RUNTIME packages have not been found:

 * org.kde.kirigamiaddons.labs.mobileform-QMLModule, QML module 'org.kde.kirigamiaddons.labs.mobileform' is a runtime dependency.
