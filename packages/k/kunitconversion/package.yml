name       : kunitconversion
version    : 5.100.0
release    : 63
source     :
    - https://cdn.download.kde.org/stable/frameworks/5.100/kunitconversion-5.100.0.tar.xz : 1c5b8c30db9faeb4a997e5676b62676df80d8cc887f11ac68a0f2848651c141b
homepage   : https://community.kde.org/Frameworks
license    : LGPL-2.0-or-later
component  : programming.library
summary    : Converting physical units
description: |
    KUnitConversion provides functions to convert values in different physical units. It supports converting different prefixes (e.g. kilo, mega, giga) as well as converting between different unit systems (e.g. liters, gallons).
optimize   : lto
builddeps  :
    - ki18n-devel
setup      : |
    %cmake_ninja -DBUILD_QCH=ON \
                -DBUILD_TESTING=OFF
build      : |
    %ninja_build
install    : |
    %ninja_install
