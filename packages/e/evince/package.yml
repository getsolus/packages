name       : evince
version    : 3.20.1
release    : 20
source     :
    - http://ftp.gnome.org/pub/gnome/sources/evince/3.20/evince-3.20.1.tar.xz : fc7ac23036939c24f02e9fed6dd6e28a85b4b00b60fa4b591b86443251d20055
license    :
    - GPL-2.0
component  : office.viewers
summary    : GNOME Platform Document Viewer
description: |
    Evince is a document viewer for multiple document formats. The goal of evince is to replace the multiple document viewers that exist on the GNOME Desktop with a single simple application.
builddeps  :
    - pkgconfig(adwaita-icon-theme)
    - pkgconfig(cairo-pdf)
    - pkgconfig(cairo-ps)
    - pkgconfig(ddjvuapi)
    - pkgconfig(gsettings-desktop-schemas)
    - pkgconfig(gtk+-3.0)
    - pkgconfig(libgxps)
    - pkgconfig(libnautilus-extension)
    - pkgconfig(libsecret-1)
    - pkgconfig(libspectre)
    - pkgconfig(libtiff-4)
    - pkgconfig(poppler-glib)
    - itstool
setup      : |
    %patch -p1 < $pkgfiles/0001-Ensure-we-build-with-Clang.patch
    %configure --disable-browser-plugin \
               --disable-static \
               --enable-introspection \
               --enable-djvu
build      : |
    %make
install    : |
    %make_install
