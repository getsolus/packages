name       : lldb
version    : 10.0.1
release    : 6
source     :
    - https://github.com/llvm/llvm-project/releases/download/llvmorg-10.0.1/lldb-10.0.1.src.tar.xz : 07abe87c25876aa306e73127330f5f37d270b6b082d50cc679e31b4fc02a3714
license    : NCSA
component  : programming.tools
summary    : High performance debugger, part of the LLVM project.
description: |
    LLDB is a next generation, high-performance debugger. It is built as a set of reusable components which highly leverage existing libraries in the larger LLVM Project, such as the Clang expression parser and LLVM disassembler.
clang      : yes
builddeps  :
    - pkgconfig(libedit)
    - swig
rundeps    :
    - python-six
setup      : |
    %cmake_ninja \
        -DLLDB_USE_SYSTEM_SIX=ON \
        -DLLVM_LINK_LLVM_DYLIB=ON
build      : |
    %ninja_build
install    : |
    %ninja_install
    %python_compile
