@@ -, +, @@ 
 gtk/gtkwindow.c | 1 +
 1 file changed, 1 insertion(+)
--- a/gtk/gtkwindow.c	
+++ a/gtk/gtkwindow.c	
@@ -5644,6 +5644,7 @@ gtk_window_move (GtkWindow *window,
   else
     {
       /* Save this position to apply on mapping */
+      gtk_widget_queue_resize (widget);
       info->initial_x = x;
       info->initial_y = y;
       info->initial_pos_set = TRUE;
