#!/bin/bash

# liberation-circuit start script
# Currently there is no way to explicitly specify the asset folder location
# so we have to switch the working directory before starting the game.

cd /usr/share/libcirc
./libcirc
