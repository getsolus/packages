name       : libdrm
version    : 2.4.89
release    : 21
source     :
    - https://dri.freedesktop.org/libdrm/libdrm-2.4.89.tar.gz : c376b9ba0974700632939139397a12d4e3c93c31835a27dda855159441713e70
license    : MIT
component  : xorg.display
summary    : Direct Rendering Manager API + Library
description: |
    libdrm provides core library routines for the X Window System to directly interface with video hardware using the Linux kernel's Direct Rendering Manager (DRM).
builddeps  :
    - pkgconfig32(pciaccess)
    - pkgconfig(pciaccess)
    - pkgconfig(xorg-macros)
    - glibc-32bit-devel
    - libgcc-32bit
emul32     : yes
optimize   : speed
setup      : |
    # sed -e /pthread-stubs/d -i configure.ac
    %reconfigure --prefix=/usr --disable-static --enable-udev
build      : |
    %make
install    : |
    %make_install
check      : |
    %make check
