name       : libdrm
version    : 2.4.94
release    : 24
source     :
    - https://dri.freedesktop.org/libdrm/libdrm-2.4.94.tar.gz : 9b3d3509fe496839c4c52dfcfe6cc24f46f3dc272eab776ca6aa4d4a5c381597
license    : MIT
component  : xorg.display
summary    : Direct Rendering Manager API + Library
description: |
    libdrm provides core library routines for the X Window System to directly interface with video hardware using the Linux kernel's Direct Rendering Manager (DRM).
emul32     : yes
optimize   : speed
builddeps  :
    - pkgconfig32(pciaccess)
    - pkgconfig(pciaccess)
    - pkgconfig(xorg-macros)
setup      : |
    %patch -p1 < $pkgfiles/0001-xf86drm-fallback-to-normal-path-when-realpath-fails.patch
    # sed -e /pthread-stubs/d -i configure.ac
    %reconfigure --prefix=/usr --disable-static --enable-udev
build      : |
    %make
install    : |
    %make_install
check      : |
    %make check
