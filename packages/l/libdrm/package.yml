name       : libdrm
version    : 2.4.68
release    : 11
source     :
    - https://dri.freedesktop.org/libdrm/libdrm-2.4.68.tar.gz : 092e870b7ecf64e07df9b8f96dee78adbfe3310d562105fddbb3ee7f35a76eff
license    :
    - MIT
summary    : Direct Rendering Manager API + Library
component  : xorg.display
builddeps  :
    - pkgconfig32(pciaccess)
    - pkgconfig(pciaccess)
    - pkgconfig(xorg-macros)
    - glibc-32bit-devel
    - libgcc-32bit
emul32     : yes
optimize   : speed
description: |
    libdrm provides core library routines for the X Window System to directly interface with video hardware using the Linux kernel's Direct Rendering Manager (DRM).
setup      : |
    sed -e /pthread-stubs/d -i configure.ac
    autoreconf -vfi
    %configure --prefix=/usr --disable-static --enable-udev
build      : |
    %make
install    : |
    %make_install
