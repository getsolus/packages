--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -331,6 +331,7 @@ if (MSVC OR CMAKE_CONFIGURATION_TYPES)
 endif ()
 
 ADD_LIBRARY(${GEOTIFF_LIBRARY_TARGET}
+    SHARED
     ${GEOTIFF_LIB_SOURCES} ${XTIFF_SOURCES})
 
 IF(WIN32 AND MSVC)
