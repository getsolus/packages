name       : libboost
version    : 1.78.0
release    : 31
source     :
    - https://boostorg.jfrog.io/artifactory/main/release/1.78.0/source/boost_1_78_0.tar.gz : 94ced8b72956591c4775ae2207a9763d3600b30d9d7446562c552f0a14a63be7
homepage   : https://www.boost.org/
license    : BSL-1.0
component  : programming.library
summary    : Useful C++ source libraries
description: |
    Boost provides a set of free peer-reviewed portable C++ source libraries. It includes libraries for linear algebra, pseudorandom number generation, multithreading, image processing, regular expressions and unit testing.
builddeps  :
    - pkgconfig(icu-i18n)
    - pkgconfig(libzstd)
    - pkgconfig(python3)
    - bzip2-devel
    - numpy
setup      : |
    %patch -p1 < $pkgfiles/0001-Fix-python3-include-dir-in-bootstrap.patch
    %patch -Np1 -d tools/build < $pkgfiles/b2-fix-lib-install.patch
build      : |
    ./bootstrap.sh --prefix=$installdir/usr \
        --libdir=$installdir/%libdir% \
        --with-python=/usr/bin/python3
install    : |
    ./b2 install \
        variant=release \
        threading=multi \
        link=shared,static \
        runtime-link=shared \
        -j %YJOBS%
