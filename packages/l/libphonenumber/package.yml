# yaml-language-server: $schema=/usr/share/ypkg/schema/schema.json
name       : libphonenumber
version    : 9.0.36
release    : 17
source     :
    - https://github.com/google/libphonenumber/archive/refs/tags/v9.0.36.tar.gz : 43b8fa34f80f84dddc591406d97fbe7f81cf35ce5d83621e67a1b6fa6afac548
homepage   : https://github.com/googlei18n/libphonenumber
license    :
    - Apache-2.0
    - BSD-3-Clause
component  : desktop.library
summary    : library for working with international phone numbers
description: |
    Google's common library for parsing, formatting, and validating international phone numbers
builddeps  :
    - pkgconfig(absl_base)
    - pkgconfig(protobuf)
    - libboost-devel
    - openjdk-25-devel
checkdeps  :
    - pkgconfig(gtest)
rundeps    :
    - devel :
        - abseil-cpp-devel
        - libboost-devel
        - protobuf-devel
clang      : true
optimize   :
    - thin-lto
environment: |
    export JAVA_HOME=/usr/lib64/openjdk-25
    export PATH=$JAVA_HOME/bin:$PATH
    export CLANG_IGNORE_WERROR=1
setup      : |
    %patch -p1 -i ${pkgfiles}/0001-Fix-BUILD_STATIC_LIB-OFF.patch
    %patch -p1 -i ${pkgfiles}/0002-Do-not-require-Boost.System.patch

    %cmake_ninja -S cpp -DBUILD_STATIC_LIB=OFF
build      : |
    %ninja_build
install    : |
    %ninja_install
    %install_license LICENSE LICENSE.Chromium
check      : |
    cmake --build solusBuildDir --target tests -j %YJOBS%
