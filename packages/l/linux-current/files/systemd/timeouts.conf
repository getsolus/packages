[Manager]
# Set all of these to 90 just in case the hardware is really slow
# These values only take effect during the initrd, the rootfs has a separate configuration
DefaultTimeoutStartSec=90
DefaultDeviceTimeoutSec=90
DefaultTimeoutStopSec=90
