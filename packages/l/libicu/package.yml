# yaml-language-server: $schema=/usr/share/ypkg/schema/schema.json
name       : libicu
version    : '78.3'
release    : 31
source     :
    - https://github.com/unicode-org/icu/releases/download/release-78.3/icu4c-78.3-sources.tgz : 3a2e7a47604ba702f345878308e6fefeca612ee895cf4a5f222e7955fabfe0c0
homepage   : https://icu.unicode.org/home
license    : MIT
component  : system.base
emul32     : true
summary    : International Components for Unicode
description: |
    The International Components for Unicode (ICU) package is a mature, widely used set of C/C++ libraries providing Unicode and Globalization support for software applications. ICU is widely portable and gives applications the same results on all platforms.
patterns   :
    - devel :
        - /usr/share/icu
setup      : |
    cd source
    %configure --disable-static
build      : |
    %make -C source
install    : |
    %make_install -C source
check      : |
    %make check -C source || :
