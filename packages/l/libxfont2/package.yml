# yaml-language-server: $schema=/usr/share/ypkg/schema/schema.json
name       : libxfont2
version    : 2.0.9
release    : 15
source     :
    - https://www.x.org/releases/individual/lib/libXfont2-2.0.9.tar.gz : 8564b4df365bc5a6cb0c15900dc688f6e8f47b00a8571c6708c916dfb85066ba
homepage   : https://www.x.org/
license    :
    - BSD-3-Clause
    - BSD-4-Clause-UC
    - HPND-sell-variant
    - MIT-open-group
    - SMLNJ
    - X11
component  : xorg.library
summary    : libXfont provides the core of the legacy X11 font system, handling the index files (fonts.dir, fonts.alias, fonts.scale), the various font file formats, and rasterizing them.
description: |
    libXfont provides the core of the legacy X11 font system, handling the index files (fonts.dir, fonts.alias, fonts.scale), the various font file formats, and rasterizing them.
optimize   :
    - speed
    - no-symbolic
emul32     : true
builddeps  :
    - pkgconfig32(fontenc)
    - pkgconfig32(freetype2)
    - pkgconfig32(zlib)
    - pkgconfig(xproto)
    - pkgconfig(xtrans)
setup      : |
    %configure --disable-static
build      : |
    %make
install    : |
    %make_install
    %install_license COPYING
