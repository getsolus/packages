name       : libwebp
version    : 0.6.0
release    : 7
source     :
    - https://storage.googleapis.com/downloads.webmproject.org/releases/webp/libwebp-0.6.0.tar.gz : c928119229d4f8f35e20113ffb61f281eda267634a8dc2285af4b0ee27cf2b40
homepage   : https://developers.google.com/speed/webp/
license    : BSD-2-Clause
component  : multimedia.codecs
summary    : A new image format for the web
description: |
    WebP is a new image format that provides lossless and lossy compression for images on the web. WebP lossless images are 26% smaller in size compared to PNGs. WebP lossy images are 25-34% smaller in size compared to JPEG images at equivalent SSIM index. WebP supports lossless transparency (also known as alpha channel) with just 22% additional bytes. Transparency is also supported with lossy compression and typically provides 3x smaller file sizes compared to PNG when lossy compression is acceptable for the red/green/blue color channels.
builddeps  :
    - pkgconfig32(zlib)
    - pkgconfig(libpng15)
    - pkgconfig32(libpng15)
    - pkgconfig(libtiff-4)
    - pkgconfig32(libtiff-4)
    - freeglut-32bit-devel
    - freeglut-devel
    - libjpeg-turbo-devel
    - libjpeg-turbo-32bit-devel
    - glibc-32bit-devel
    - libgcc-32bit
patterns   :
    - devel : /usr/share/man
emul32     : yes
setup      : |
    %reconfigure --prefix=/usr --disable-static --enable-everything
build      : |
    %make
install    : |
    %make_install
