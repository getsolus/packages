name       : libsoup
version    : 2.62.3
release    : 22
source     :
    - https://download.gnome.org/sources/libsoup/2.62/libsoup-2.62.3.tar.xz : d312ade547495c2093ff8bda61f9b9727a98cfdae339f3263277dd39c0451172
license    : LGPL-2.1-only
component  : desktop.gnome.core
emul32     : yes
summary    : GNOME HTTP client/server library
description: |
    The libsoup is HTTP client/server library for GNOME. It uses GObject and the GLib main loop to integrate with GNOME applications and it also has asynchronous API for use in threaded applications.
builddeps  :
    - pkgconfig32(gio-2.0)
    - pkgconfig32(gnutls)
    - pkgconfig32(krb5)
    - pkgconfig32(libxml-2.0)
    - pkgconfig32(sqlite3)
    - pkgconfig(sqlite3)
    - glib-networking
    - glib-networking-32bit
    - vala
rundeps    :
    - glib-networking
    - 32bit :
        - glib-networking-32bit
setup      : |
    %configure --disable-static --prefix=/usr --disable-more-warnings --disable-tls-check
build      : |
    %make
install    : |
    %make_install
