--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -107,6 +107,19 @@ else(WIN32)
   set(CMAKE_ARCHIVE_OUTPUT_DIRECTORY "${LASZIP_OUTPUT_LIB_DIR}")
   set(CMAKE_RUNTIME_OUTPUT_DIRECTORY "${LASZIP_OUTPUT_BIN_DIR}")
   set(CMAKE_LIBRARY_OUTPUT_DIRECTORY "${LASZIP_OUTPUT_LIB_DIR}")
+
+  set(PKGCFG_PREFIX "${CMAKE_INSTALL_PREFIX}")
+  set(PKGCFG_INC_DIR "${CMAKE_INSTALL_PREFIX}")
+  set(PKGCFG_LIB_DIR "${CMAKE_INSTALL_PREFIX}")
+  set(PKGCFG_REQUIRES  "")
+  set(PKGCFG_VERSION "${LASZIP_API_VERSION_STRING}")
+  set(PKGCFG_LINK_FLAGS "-llaszip")
+  set(PKGCFG_COMPILE_FLAGS "$CMAKE_CXX_FLAGS")
+  configure_file(${CMAKE_CURRENT_SOURCE_DIR}/example/laszip.pc.in
+    ${CMAKE_CURRENT_BINARY_DIR}/laszip.pc @ONLY)
+  install(FILES ${CMAKE_CURRENT_BINARY_DIR}/laszip.pc
+    DESTINATION lib/pkgconfig/
+    PERMISSIONS OWNER_READ OWNER_WRITE GROUP_READ WORLD_READ)   
 endif(WIN32)
 
 set(LASZIP_HEADERS_DIR "${PROJECT_SOURCE_DIR}/include/laszip")
