name       : libnspr
version    : '4.29'
release    : 18
source     :
    - https://ftp.mozilla.org/pub/nspr/releases/v4.29/src/nspr-4.29.tar.gz : 22286bdb8059d74632cc7c2865c139e63953ecfb33bf4362ab58827e86e92582
homepage   : https://developer.mozilla.org/en-US/docs/Mozilla/Projects/NSPR
license    :
    - MPL-1.1
    - GPL-2.0-or-later
    - LGPL-2.1-or-later
component  : system.base
emul32     : yes
libsplit   : no
summary    : Netscape Portable Runtime
description: |
    Netscape Portable Runtime (NSPR) provides a platform-neutral API for system level and libc like functions.
setup      : |
    cd nspr
    sed -i 's#$(LIBRARY) ##' config/rules.mk

    if [[ ! -z "${EMUL32BUILD}" ]]; then
        %configure --with-mozilla --with-pthreads
    else
        %configure --with-mozilla --with-pthreads --enable-64bit
    fi
build      : |
    %make -C nspr
install    : |
    %make_install -C nspr

    # We don't need this
    rm -v $installdir/usr/bin/prerr.properties
    rm -v $installdir/usr/bin/compile-et.pl
    rm -r $installdir/usr/include/nspr/md
