releases:
  id: 12959
  rss: https://github.com/gohugoio/hugo/releases.atom
  ignore:
    # Purely for packager sanity, we track the n-1 major release; this line should be changed with each update.
    - 0.137.*
security:
  cpe:
    - vendor: gohugo
      product: hugo
