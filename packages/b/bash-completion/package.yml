name       : bash-completion
version    : '2.8'
release    : 15
source     :
    - https://github.com/scop/bash-completion/releases/download/2.8/bash-completion-2.8.tar.xz : c01f5570f5698a0dda8dc9cfb2a83744daa1ec54758373a6e349bd903375f54d
license    : GPL-2.0-only
component  :
    - system.base
    - devel : system.devel
summary    : Programmable Completion for Bash
description: |
    bash-completion is a collection of shell functions that take advantage
    of the programmable completion feature of Bash 2.04 and later
patterns   :
    - devel : /usr/share/pkgconfig
setup      : |
    %patch -p1 < $pkgfiles/0001-Use-a-stateless-configuration.patch
    %reconfigure
build      : |
    %make
install    : |
    install -d -D -m 00755 $installdir/usr/share/bash-completion/completions
    %make_install

    install -D -m 00644 $pkgfiles/eopkg $installdir/usr/share/bash-completion/completions/eopkg
