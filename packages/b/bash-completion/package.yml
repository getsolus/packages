name       : bash-completion
version    : '2.9'
release    : 16
source     :
    - https://github.com/scop/bash-completion/archive/2.9.tar.gz : fddd9e6739c972eea95d1f0e0e84fa9a17a5935ba21eab37308b27580cc4d790
license    : GPL-2.0-only
component  :
    - system.base
    - devel : system.devel
summary    : Programmable Completion for Bash
description: |
    bash-completion is a collection of shell functions that take advantage
    of the programmable completion feature of Bash 2.04 and later
patterns   :
    - devel : /usr/share/pkgconfig
setup      : |
    %patch -p1 < $pkgfiles/0001-Use-a-stateless-configuration.patch
    %reconfigure
build      : |
    %make
install    : |
    install -dDm00755 $installdir/usr/share/bash-completion/completions
    %make_install

    install -Dm00644 $pkgfiles/eopkg $installdir/usr/share/bash-completion/completions/eopkg
