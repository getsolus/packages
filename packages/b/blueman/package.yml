name       : blueman
version    : 2.0.8
release    : 8
source     :
    - https://github.com/blueman-project/blueman/releases/download/2.0.8/blueman-2.0.8.tar.xz : 04afd07b4c900a401f39e711dedbf8b9cf12000ddeda0d8ef3c3327caf34704e
license    : GPL-3.0-or-later
component  : desktop.mate
summary    : GTK+ Bluetooth manager
description: |
    GTK+ Bluetooth manager
builddeps  :
    - pkgconfig(bluez)
    - pkgconfig(gnome-icon-theme)
    - pkgconfig(polkit-agent-1)
    - pkgconfig(pygobject-3.0)
    - cython
rundeps    :
    - python-gobject
setup      : |
    %configure  --disable-static \
        --enable-settings-integration \
        --enable-thunar-sendto=no
build      : |
    %make
install    : |
    %make_install
    # Stateless.
    mv $installdir/etc/dbus-1/* $installdir/usr/share/dbus-1/.
    rmdir $installdir/etc/dbus-1
    mv $installdir/etc/xdg $installdir/usr/share/.
    rmdir $installdir/etc
    # Keyrules per: https://github.com/blueman-project/blueman/wiki/PolicyKit
    install -Dm00644 $pkgfiles/org.blueman.keyrules $installdir/usr/share/polkit-1/rules.d/org.blueman.keyrules
