# yaml-language-server: $schema=/usr/share/ypkg/schema/schema.json
name       : buildah
version    : 1.43.1
release    : 49
source     :
    - https://github.com/containers/buildah/archive/refs/tags/v1.43.1.tar.gz : 7980a8b4fb719a104d592b06de149cc18766de02f8c3dd94e727f8702a62f9cd
license    : Apache-2.0
homepage   : https://buildah.io/
component  : virt
summary    : A tool that facilitates building Open Container Initiative (OCI) container images
description: |
    The Buildah package provides a command line tool that can be used to
    - create a working container, either from scratch or using an image as a starting point
    - create an image, either from a working container or via the instructions in a Dockerfile
    - images can be built in either the OCI image format or the traditional upstream docker image format
    - mount a working container's root filesystem for manipulation
    - unmount a working container's root filesystem
    - use the updated contents of a container's root filesystem as a filesystem layer to create a new image
    - delete a working container or an image
    - rename a local container
builddeps  :
    - pkgconfig(gpgme)
    - pkgconfig(libseccomp)
    - pkgconfig(sqlite3)
    - pkgconfig(systemd)
    - btrfs-progs-devel
    - git
    - go-md2man
    - golang
    - shadow-devel
rundeps    :
    - netavark
    - passt
    - runc
    - skopeo
networking : true
environment: |
    export CGO_CPPFLAGS="${CPPFLAGS}"
    export CGO_CFLAGS="${CFLAGS}"
    export CGO_CXXFLAGS="${CXXFLAGS}"
    export CGO_LDFLAGS="${LDFLAGS}"
    export GOFLAGS="-buildmode=pie -trimpath -modcacherw"
    export BUILDTAGS="containers_image_ostree_stub libsqlite3 libsubid seccomp systemd"
    export EXTRA_LDFLAGS="-X github.com/containers/image/v5/signature.systemDefaultPolicyPath=/etc/containers/policy.json -X go.podman.io/image/v5/signature.systemDefaultPolicyPath=/etc/containers/policy.json"
build      : |
    set -e
    GIT_COMMIT=$(git ls-remote https://github.com/containers/buildah refs/tags/v$version | awk '{print $1}')
    %make GIT_COMMIT="$GIT_COMMIT"
install    : |
    %make_install install.completions PREFIX=%PREFIX%
    %install_license LICENSE
