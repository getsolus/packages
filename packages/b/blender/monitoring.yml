releases:
  id: 201
  # Uses gitea (or something similar) as the upstream which doesn't support git tags
  rss: https://github.com/blender/blender/tags.atom
security:
  cpe:
  - vendor: blender
    product: blender
