# yaml-language-server: $schema=/usr/share/ypkg/schema/schema.json
name       : zig
version    : 0.15.2
release    : 17
source     :
    - https://ziglang.org/download/0.15.2/zig-0.15.2.tar.xz : d9b30c7aa983fcff5eed2084d54ae83eaafe7ff3a84d8fb754d854165a6e521c
homepage   : https://ziglang.org
license    : MIT
component  : programming
summary    : General-purpose programming language and toolchain for maintaining robust, optimal, and reusable software.
description: |
    General-purpose programming language and toolchain for maintaining robust, optimal, and reusable software.
clang      : true
builddeps  :
    - clang-devel
    - lld-devel
    - llvm-devel
environment: |
    export CC=/usr/bin/clang
    export CXX=/usr/bin/clang++
    if [ -e /usr/bin/ccache ]; then
        export CMAKE_C_COMPILER_LAUNCHER=ccache
        export CMAKE_CXX_COMPILER_LAUNCHER=ccache
    fi
setup      : |
    %patch -p1 -i $pkgfiles/0001-add-zstd-compressed-section-support.patch
    # Note: Check the kernel minimum in glibc for the kernel version to use below
    %cmake_ninja \
        -DZIG_SHARED_LLVM=ON \
        -DZIG_TARGET_TRIPLE=x86_64-linux.6.1-gnu.2.42 \
        -DZIG_TARGET_MCPU=baseline \
        -DZIG_USE_LLVM_CONFIG=ON
build      : |
    %ninja_build
    %install_license LICENSE
install    : |
    %ninja_install
check      : |
    pushd `mktemp -d`
        $installdir/usr/bin/zig init
        $installdir/usr/bin/zig build run
        $installdir/usr/bin/zig build test
    popd
