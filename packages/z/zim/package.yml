name       : zim
version    : '0.68'
release    : 8
source     :
    - http://zim-wiki.org/downloads/zim-0.68.tar.gz : d91518e010f6a6e951a75314138b5545a4c51151fc99f513aa7768a18858df15
license    : GPL-2.0
component  : office.notes
summary    : A WYSIWYG text editor and desktop wiki
description: |
    Zim is a graphical text editor used to maintain a collection of wiki pages. Each page can contain links to other pages, simple formatting and images. Pages are stored in a folder structure, like in an outliner, and can have attachments. Creating a new page is as easy as linking to a nonexistent page. All data is stored in plain text files with wiki formatting. Various plugins provide additional functionality, like a task list manager, an equation editor, a tray icon, and support for version control.
builddeps  :
    - pkgconfig(pygobject-2.0)
    - python-setuptools
rundeps    :
    - gnuplot
    - graphviz
    - python-gobject2
    - python-gtk2
    - python-setuptools
    - pyxdg
    - scrot
    - xdg-utils
build      : |
    %python_setup
install    : |
    %python_install --skip-xdg-cmd --skip-build
    sed -i 's/Utility;TextEditor;GTK;/Office;/g' $installdir/usr/share/applications/zim.desktop
