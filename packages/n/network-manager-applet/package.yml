name       : network-manager-applet
version    : 1.8.11
release    : 27
source     :
    - git|https://git.gnome.org/browse/network-manager-applet : a37483c1a364ef3cc1cfa29e7ad51ca108d75674
license    : GPL-2.0
component  : desktop.gnome
summary    : NetworkManager Applet
description: |
    NetworkManager applet and GTK3 libraries
builddeps  :
    - pkgconfig(dbus-glib-1)
    - pkgconfig(gobject-introspection-1.0)
    - pkgconfig(gnome-keyring-1)
    - pkgconfig(gcr-3)
    - pkgconfig(gtk+-2.0)
    - pkgconfig(gtk+-3.0)
    - pkgconfig(jansson)
    - pkgconfig(libnm-glib)
    - pkgconfig(libnotify)
    - pkgconfig(libsecret-unstable)
    - pkgconfig(mm-glib)
    - gtk-doc
rundeps    :
    - mobile-broadband-provider-info
setup      : |
    %meson_configure -Dlibnm-gtk=true -Dselinux=false
build      : |
    %meson_build
install    : |
    %meson_install
    # Stateless XDG
    mv $installdir/etc/xdg $installdir/usr/share/xdg && rmdir $installdir/etc
