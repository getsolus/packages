name       : ne
version    : 3.3.0
release    : 6
source     :
    - https://github.com/vigna/ne/archive/3.3.0.tar.gz : 77a0c8e8564a29cd18069eebf04cee4855fae183f1e8f25d5fbb0c2651f07e6c
homepage   : http://ne.di.unimi.it/
license    : GPL-3.0-or-later
component  : editor
summary    : ne, the nice editor
description: |
    ne, the nice editor. ne is a free (GPL'd) text editor based on the POSIX standard.
builddeps  :
    - pkgconfig(ncursesw)
    - texinfo
setup      : |
    sed -i 's:CC=c99:CC=gcc:g' src/makefile
build      : |
    %make build PREFIX=/usr
    cd $workdir
    %make docs
install    : |
    %make_install PREFIX=/usr
    rm -f $installdir/usr/share/doc/ne/{CHANGES,COPYING,NEWS,README.md}
