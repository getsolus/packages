name       : nautilus-dropbox
version    : 2019.02.14
release    : 19
source     :
    - https://linux.dropbox.com/packages/nautilus-dropbox-2019.02.14.tar.bz2 : ae0c4979f2cae9b86dfc7a645047efdacca4c8f70b603e284294795d083ecf27
license    :
    - CC-BY-ND-3.0
    - GPL-3.0-or-later
component  :
    - desktop.gnome
    - ^dropbox : network.clients
summary    :
    - Dropbox extension for Nautilus
    - ^dropbox : Dropbox linux client
description: |
    - Dropbox allows users to create a special folder on each of their computers, which Dropbox then synchronizes so that it appears to be the same folder (with the same contents) regardless of which computer is used to view it.
builddeps  :
    - pkgconfig(libnautilus-extension)
    - pkgconfig(pygobject-3.0)
    - python-docutils
rundeps    :
    - dropbox
    - ^dropbox :
        - python-gpg
        - python3-cairo
patterns   :
    - ^dropbox :
        - /usr/bin/dropbox
        - /usr/share/applications
        - /usr/share/icons/hicolor
        - /usr/share/man
setup      : |
    %configure --disable-static
build      : |
    %make
install    : |
    %make_install
