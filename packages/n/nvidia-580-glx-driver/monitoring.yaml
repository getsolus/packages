releases:
  id: 5454
  rss: ~
  ignore:
    # Don't notify about newer versions, 580 is the legacy branch
    - .*
security:
  cpe:
    - vendor: nvidia
      product: gpu_driver
    - vendor: nvidia
      product: gpu_display_driver
